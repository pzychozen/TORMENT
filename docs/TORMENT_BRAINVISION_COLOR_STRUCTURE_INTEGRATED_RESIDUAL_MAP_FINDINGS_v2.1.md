# TORMENT Brainvision Color Structure Integrated Residual Map Findings v2.1

## 1. Status / quarantine and non-claims

**DOCS-ONLY findings receipt. Non-authorizing, non-implementing.** It records the v2.1 integrated residual
map result and prepares the project for a fresh-chat handoff and a next-direction decision. It **authorizes
no code and no tests**, invents no threshold, defines no replacement acceptance criteria, changes no
formula / §7 gate / §8 verdict, and deletes or weakens no control. Everything stays offline under
`research/brainvision/` + `tests/research/`, HELD per v0.6.

This note makes **no** vision claim, **no** "Brainvision sees" claim, and **no** temporal-order claim.
`first_pass_structure_validity_claim_allowed` remains **False** and `temporal_claim_allowed` remains
**False**. It touches no `torment_service/`, runtime, camera / sensor / live-capture / screen-capture /
streaming, or prompt / context / memory / action / render-body / autonomy paths, and makes **no real-clip /
local-clip move** and **no memory-system integration**. **No `§0` pointer; no tags.**

## 2. What was run

- v2.1 files:
  - `research/brainvision/run_color_structure_integrated_residual_map_v2_1.py`
  - `tests/research/test_brainvision_color_structure_integrated_residual_map_v2_1.py`
- Validation: v2.1 tests **9 passed**; v2.0 regression **10 passed**; full Brainvision research suite
  **192 passed**.
- The diagnostic is **reporting-only**; the frozen §8 verdict over the consolidated bank stays **HOLD**.

## 3. Core result

```text
classification: residual_failures_remain
verdict: HOLD
```

Windows repo truth (authoritative):

```text
by_std:             +0.036   pass / controlled_or_explained
spectral_centroid:  -0.050   pass / controlled_or_explained
non-winder S <= 0.2167
winder S = 1.0
```

## 4. Main finding

```text
Known spectral/std and by_std residuals are controlled/explained in the consolidated synthetic bank,
but frozen pooled §7 still HOLDs on directional / per-channel-spectral geometry.
```

Remaining failure axis:

```text
u_directional_delta_rms
angular_increment_mag
RG/BY centroid family
RG/BY spread family
null-relative variants
```

## 5. Interpretation (preserved)

- **v1.8** exposed the blocker map.
- **v1.9** showed `S`/`PSC` survives spectral/std blocker control.
- **v2.0** explained the `by_std` residual as a pool-composition artifact **in this synthetic diagnostic
  context**.
- **v2.1** shows the old `by_std` / `spectral_centroid` issues are **controlled/explained** in the
  consolidated bank.
- But the remaining **directional / per-channel-spectral geometry** residual is still **large and robust**
  (far from the 0.30 ceiling).
- Therefore the project **should not keep adding random fixtures just to make the gate pass**.
- The next direction should be a **Reading-A/B validity-surface decision on the remaining axis**.

## 6. Non-claims (unchanged)

This receipt makes **no** descriptor-validity claim, **no** temporal-order proof, **no** vision claim,
**no** "Brainvision sees" claim, and **no** functioning-memory-system-vision-layer claim. It authorizes
**no** real clips, **no** memory integration, **no** §7 edit, **no** §8 verdict edit, **no** threshold
invention, **no** replacement acceptance criteria, and **no** control deletion.
`first_pass_structure_validity_claim_allowed = False` and `temporal_claim_allowed = False` are unchanged.

## 7. Recommended next direction

After Codex accepts this receipt, **prepare a fresh-chat handoff.** The next work direction should be:

```text
v2.2 directional / per-channel-spectral residual decision frame
```

Purpose:

```text
Decide whether the remaining residual is:
A. legitimate descriptor limitation,
B. validity-surface mismatch,
C. control-bank composition artifact,
or unresolved.
```

This should be **docs-only first**, then implementation only after the decision. Possible next doc:

```text
docs/TORMENT_BRAINVISION_COLOR_STRUCTURE_DIRECTIONAL_SPECTRAL_RESIDUAL_DECISION_FRAME_v2.2.md
```

Expected sections for that decision frame:

1. Status / quarantine / non-claims
2. What v2.1 resolved
3. What still fails
4. Why this is now the main wall
5. Three readings — A legitimate descriptor limitation / B validity-surface mismatch / C control-bank
   composition artifact
6. Recommended next step
7. Fresh-chat handoff pointer content

The `v2.2` slice is recorded as the next **possible** step; it is **not opened here**. Real clips /
local-clip manifest and memory-system integration stay disallowed, and no §7/§8/threshold/control change may
be made without a fresh freeze and review.

Brainvision remains **offline / quarantined**, HELD per v0.6.
**No `§0` pointer; no tags.**

## 8. Recommended next

- **Codex review** of this findings receipt.
- **If accepted,** prepare the fresh-chat handoff and open the docs-only `v2.2 directional /
  per-channel-spectral residual decision frame`; otherwise HOLD.

*End — TORMENT Brainvision Color Structure Integrated Residual Map Findings v2.1. Docs-only, non-authorizing.
Opens no implementation lane; changes no frozen formula, gate, or verdict; deletes no control; invents no
threshold; no `§0` pointer added; no tags.*
