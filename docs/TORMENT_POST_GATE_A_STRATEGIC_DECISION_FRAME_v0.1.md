# TORMENT Post-Gate-A Strategic Decision Frame v0.1

## 0. Non-authorization statement

This artifact is a **map of choices, not a recommendation.** It selects nothing
and chooses nothing for the operator.

- No lane is selected.
- No implementation is authorized.
- No tests are authorized.
- No mechanics are authorized.
- No default next gate exists.
- Gate A remains **paused**.
- Gate D remains **parked**.
- Database / substrate remains **parked**.
- Every option below requires a **separate operator decision plus Codex review
  before any patch.**

This is a docs-only choice aid. It may shape context. It must not seize authority.

## 1. Current phase boundary

`[FACT]` Codex and Claude converged on the post-Gate-A reading:

- Gate A has reached the **safe non-mechanical edge**.
- No concrete remaining non-mechanical Gate A prerequisite is worth doing now.
- Another automatic Gate A wall slice would likely become **fake progress**
  unless it selects mechanics or opens candidate / admission / promotion /
  writer-authority territory.
- Gate A should remain **paused** until explicit operator authorization for
  mechanics, or until a different strategic gate is chosen.
- Current posture is **HOLD**. No active implementation lane is open.

`[FACT]` Gate A completed arc (compressed context; all docs-only or
tests/source-only, no mechanics):

- wall enforcement-path **proposal** (non-authorizing);
- resting-state **non-reachability** characterization;
- **no-tag-dependence** characterization (A-C2 resting state);
- **A-C1** ordinary ingest / fan-out **terrain inventory**;
- **A-I1** inspection / non-reentry **terrain inventory**;
- `TormentFabric.ingest` ingest-root **naming reconciliation** (no `MemoryFabric`
  class; prior wording read as shorthand).

The remaining Gate A obligations (A-C2 under a live producer, A-O2, A-D1/A-D2)
are **gated**: they need wall mechanics and/or writer-authority and/or a candidate
producer that do not exist and are not authorized here.

## 2. Doctrine filter

Every option in this frame is read under, and must preserve:

> Memory may shape context. Memory may not seize authority.
> Audit observes authority. Audit must not become authority.

An option that could only proceed by violating this filter is **gated** by
definition, not merely deferred.

## 3. Fork table

Each row is an **option / question only** — not a ranking, not an order.

| # | Option (as a question) | Status | Classification | Forbidden lines it crosses/approaches | Codex before patch |
|---|---|---|---|---|---|
| 1 | Authorize Gate A wall **mechanics**? | paused | requires separate authorization | wall mechanics, candidate store, governed admission, promotion crossing | required |
| 2 | Open Gate D / Layer-1 private cognition? | parked (DEFER/NO-OPEN) | gated | Gate D runtime, live Document B chamber, AgentRunner ownership | required |
| 3 | Build an Envelope Audit surface? | deferred | gated | Envelope Audit runtime (+ model-API / substrate) | required |
| 4 | Open Gate B / writer-authority reconciliation? | partial / parked | gated | writer fixes, the four parked non-conformances | required |
| 5 | Open P4 / source-sameness substrate-adjacent work? | deferred (substrate-dependent) | gated | P4 O1/O2 mechanics, carrier/schema/field selection, database/substrate | required |
| 6 | Continue broader cognition/thinking/dream roadmap framing? | available (docs-only) | docs-only | none if it selects nothing; risks pre-staging a gate | required for boundary-bearing framing |
| 7 | Open a smaller non-wall implementation lane? | none identified | not selected here | n/a (no lane currently exists) | required if one is later proposed |

## 4. Option notes

### Option 1 — Gate A mechanics authorization question
- **What it is:** a question of whether to authorize building the containment-wall
  enforcement mechanism the proposal frames.
- **Why it matters:** the resting-state Gate A work is exhausted; the remaining
  obligations need a mechanism.
- **Current status:** Gate A **paused** at the safe non-mechanical edge.
- **Known blockers:** no wall mechanics exist; no candidate producer exists; the
  enforcement path is a non-authorizing proposal only.
- **Forbidden lines crossed/approached:** wall mechanics, candidate store,
  governed admission, promotion crossing.
- **Codex required before patch:** yes. **Claude read-only inspection allowed:**
  yes. **Classification:** requires separate operator authorization.

### Option 2 — Gate D / Layer-1 private cognition question
- **What it is:** whether to open a Layer-1 private-thinking interior.
- **Why it matters:** it is the §0-named cognition doorway.
- **Current status:** **parked**; the A0 readiness review concluded DEFER/NO-OPEN.
- **Known blockers:** no live deliberation room exists (`AgentRunner` is
  test/demo-only, `/agent/query` consumes only MemoryPlan); no built Document A
  wall to host it.
- **Forbidden lines crossed/approached:** Gate D runtime, live Document B chamber
  runtime, AgentRunner ownership expansion.
- **Codex required before patch:** yes. **Claude read-only inspection allowed:**
  yes. **Classification:** gated.

### Option 3 — Envelope Audit question
- **What it is:** whether to build an Envelope-Audit observation surface.
- **Why it matters:** it is part of the unified-reflection family.
- **Current status:** **deferred** (shell-definition recorded posture: DEFER
  pre-substrate — a content-free shell either duplicates existing observability or
  requires a model API).
- **Known blockers:** model-API / substrate dependency; no worthwhile pre-substrate
  slice identified.
- **Forbidden lines crossed/approached:** Envelope Audit runtime (and, downstream,
  model/provider and substrate).
- **Codex required before patch:** yes. **Claude read-only inspection allowed:**
  yes. **Classification:** gated.

### Option 4 — Gate B / writer-authority question
- **What it is:** whether to open writer-authority reconciliation of the known
  hazards.
- **Why it matters:** the parked non-conformances are visible write-side targets.
- **Current status:** **partial / parked** — hazard inventory + decision frame +
  H3 force-route provenance landed; H1 and the four non-conformances remain parked.
- **Known blockers:** the remaining substantive work would enter writer-fix territory; framing layers already
  exist.
- **Forbidden lines crossed/approached:** writer fixes; and it must not silently
  fix/normalize/reclassify `gravity_correction` canon=True,
  `_maybe_emit_identity_anchor`, `/promote` force, `mood_drift → canon`.
- **Codex required before patch:** yes. **Claude read-only inspection allowed:**
  yes. **Classification:** gated.

### Option 5 — P4 / source-sameness / substrate-adjacent question
- **What it is:** whether to open P4 O1/O2 source-sameness work.
- **Why it matters:** it is the read-side write-side matched pair for durable memory.
- **Current status:** **deferred / substrate-dependent**; the substrate-independent
  P4 read-side (O3/O4) is already characterized.
- **Known blockers:** carriers / lineage identity are absent; O1/O2 cannot be met
  presence-only.
- **Forbidden lines crossed/approached:** P4 O1/O2 mechanics, carrier/schema/field
  selection, database/substrate mechanics.
- **Codex required before patch:** yes. **Claude read-only inspection allowed:**
  yes. **Classification:** gated.

### Option 6 — Broader cognition/thinking/dream roadmap question
- **What it is:** whether to continue docs-only roadmap framing/sequencing of the
  cognition / thinking / dream programme.
- **Why it matters:** it can keep the map honest without crossing a line.
- **Current status:** **available only as a separately reviewed docs-only question** (this artifact is one such piece).
- **Known blockers:** risk of boundary-layer proliferation / pre-staging a gate; a
  framing doc must select nothing.
- **Forbidden lines crossed/approached:** none if it selects no mechanics and opens
  no gate; the risk is wording that implies authorization.
- **Codex required before patch:** required for any boundary-bearing framing. **Claude
  read-only inspection allowed:** yes. **Classification:** docs-only.

### Option 7 — Smaller non-wall implementation lane question
- **What it is:** whether a genuinely safe, non-wall, non-writer, non-substrate
  implementation lane exists.
- **Why it matters:** it would allow forward code motion without a heavy gate.
- **Current status:** **none identified.** The non-control / observation lanes are
  already hardened-for-now or tested (audit observe-authority; MemoryPlan shaping
  non-control; read-side projection-safety; participation guidance v1; ReflectionTrace;
  `/thinking/debug`; spine alignment buffer; Gate A resting-state inventories).
  Remaining lanes are paused/deferred/gated.
- **Known blockers:** any candidate is either a duplicate characterization or a
  behavior change in a paused lane (fake-progress risk).
- **Forbidden lines crossed/approached:** n/a — no lane is selected here.
- **Codex required before patch:** required if a lane is later proposed. **Claude
  read-only inspection allowed:** yes. **Classification:** not selected here.

## 5. Forbidden crossings

This frame neither crosses nor softens any of: wall mechanics; candidate store;
governed admission; promotion crossing; Gate D runtime; Envelope Audit runtime;
private-owner live wiring; Shape B; writer fixes; P4 O1/O2 mechanics; Seed-Gov
mechanics; endpoint/schema/API changes; prompt exposure; AgentRunner ownership
expansion; retrieval feedback; persistence; autonomy; audit-to-control feedback;
database/substrate mechanics; carrier/schema/field selection; live Document B
chamber runtime; A-C2 live-producer proof; A-O2 writer-authority slice;
A-D1/A-D2 admission/promotion mechanics.

The parked writer non-conformances — `gravity_correction` canon=True,
`_maybe_emit_identity_anchor`, `/promote` force, `mood_drift → canon` — remain
**known-parked** and **outside this frame's authority**: this artifact does not
fix, normalize, or reclassify them.

## 6. What each option would require before any patch

- **Option 1:** explicit operator authorization to open wall mechanics + a Codex
  review of the mechanism design against Document A (A-C1/A-C2/A-C3/A-O2/A-O3/A-I1,
  admission/promotion separation) before any code.
- **Option 2:** a built Document A wall and a live deliberation room first, then a
  separate Gate D authorization + Codex review; ephemeral-only posture.
- **Option 3:** an operator decision on the model-API/substrate dependency, then a
  separate Envelope-Audit authorization + Codex review.
- **Option 4:** an explicit operator authorization to enter writer-authority
  fixes + Codex review; the parked non-conformances stay parked until then.
- **Option 5:** substrate/carrier authorization (Stage-B-to-mechanics) + Codex
  review; O1/O2 cannot be opened presence-only.
- **Option 6:** Codex review of any framing doc for boundary-bearing wording; it
  must select nothing.
- **Option 7:** a read-only inspection that actually surfaces a safe lane, then
  Codex review; none exists at this time.

## 7. Operator / trio decision questions

Posed as questions only (decision-ready as questions; nothing is chosen here):

1. Does the operator authorize opening **Gate A wall mechanics**, or does Gate A
   stay paused?
2. Does the operator open **Gate D / Layer-1 private cognition**, accepting that a
   wall and a deliberation room do not yet exist?
3. Does the operator open an **Envelope Audit** track, accepting the
   model-API/substrate dependency?
4. Does the operator open **Gate B / writer-authority** fixes (which would touch
   the parked non-conformances), or keep them parked?
5. Does the operator open **P4 / source-sameness** work, which requires
   substrate/carrier authorization?
6. Does the operator want **further docs-only roadmap framing**, or a HOLD?
7. Does the operator want Claude to run a **read-only inspection** to test whether
   any **smaller non-wall lane** genuinely exists?

Roles for whichever fork is chosen: Claude proposes from source (read-only here);
GPT steers; Codex challenges boundary-bearing choices; Hilmir resolves the fork.

## 8. Anti-drift footer

CHOICE MAP / NON-AUTHORIZING / SELECTS NOTHING. This artifact records options and
questions only. It opens no gate, authorizes no implementation/tests/mechanics,
and names no default path. Gate A paused; Gate D parked; database/substrate parked;
the parked writer non-conformances stay parked. Every option requires a separate
operator decision plus Codex review before any patch. Guidance not control; audit
observes authority and does not become authority; nothing rewrites identity / canon
/ seed / soul.
