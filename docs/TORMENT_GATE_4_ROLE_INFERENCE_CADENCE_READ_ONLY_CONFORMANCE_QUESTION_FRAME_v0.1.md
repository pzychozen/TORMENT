# TORMENT Gate 4 — Role Inference / Identity-Anchor Cadence Read-Only Conformance-Question Frame v0.1

**DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — QUESTION FRAMING ONLY, NO REMEDY. NO IMPLEMENTATION,
NO TESTS, NO RUNTIME, NO VERDICT.**

This frame poses — read-only — the conformance question for exactly one Gate 4 core runtime row: **role
inference / identity-anchor cadence**. **It poses the question; it does not answer it.** It selects no
remedy, no patch, no fix order, no enforcement, no governance vehicle, no runtime authority gate, no O6
mechanic, no must-not-pin implementation, no schema/carrier, no `canon_source`, no source-sameness
mechanic, and no database/substrate decision. It makes no safe/unsafe verdict, no defect verdict, no
canon claim, and no authority claim.

**Operator-supplied docs-only state:** `f845208` (*docs(gate-4): point orientation map to warmth frame*),
treated as the current operator-supplied state; the older Gate 4 stop preconditions are superseded
**only** by the operator-named docs-only commits in the Gate 4 lane (`a92dbef` → `dd53b7e` → `f6d0fb3` →
`41c4349` → `f4ee79a` → `03af2c3` → `7090a11` → `cc6cac4` → `f1500f6` → `8953b90` → `8faabd8` →
`f54b8e7` → `f845208`). No `git` was run. **Date:** 2026-06-20. Windows-visible tracked repo state
remains authoritative.

> Memory may shape context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.

**Tags:** `[FACT]` point-in-time, carried from prior read-only artifacts · `[BOUNDARY]` already-promoted
requirement obligation · `[QUESTION]` posed only, not answered · `[POSTURE]` conformance posture ·
`[DISTINCTION]` · `[CONTEXT]` carried context, not scope · `[NON-CLAIM]` · `[NON-AUTHORIZATION]` ·
`[STOP]`.

---

## 0. Provenance, precondition, and review status

`[FACT]` **Lineage.** Gate 4 selection frame → conformance-gap map → `gravity_correction` (A) →
mood-topology (B) → derived identity anchors (C) → spirit-return warmth (D1) → **this role inference /
identity-anchor cadence frame (D2).** In the reviewed framing sequence (A → B → C → **D**), row **D** is
split into **D1 = spirit-return warmth** and **D2 = role inference / identity-anchor cadence (this
frame)**. This is a **framing sequence, not a fix order**. It is a **question frame**, not reconciliation
work and not a "row to reconcile"; it poses the Gate-4-lane conformance question and answers nothing.

`[DISTINCTION]` **D2 is distinct from both C and D1.** **D2 (role inference / cadence) ≠ C (derived
identity anchors):** the derived-anchor emitter `_maybe_emit_identity_anchor` is the row-C surface and is
**not** re-posed here; role inference is the **upstream cadence modulator**, a distinct surface. **D2 ≠
D1 (spirit-return warmth):** D1 is retrieval-side warmth shaping per deep-memory EID; D2 is durable-soft
role-profile modulation of anchor cadence. This frame is **D2 only** and must not be conflated with C or
D1.

`[FACT]` **Review status: Codex challenge completed: ACCEPT WITH CORRECTIONS; corrections applied;
pending operator review / promotion.** The file remains **docs-only and non-authorizing** and changes no
behavior; nothing downstream may treat it as ratified, or as authorizing implementation, remedy, writer
patch, enforcement, substrate, or any successor gate, until operator review / promotion.

`[FACT]` **Precondition honored.** This frame is expressible **read-only and non-substrate** (§1); no
forbidden item (§6) was required to write it. Per the Gate 4 lane `[STOP]` conditions, had posing the
question required runtime/endpoint/service/ingest-query/database/workspace-frequency evidence, a verdict,
a remedy, a writer selection, a fix order, an enforcement path, a governance vehicle, an authority gate,
an O6 / must-not-pin mechanic, or any substrate decision, drafting would have stopped instead. It did not.

### 0.1 Relationship to prior artifacts (load-bearing)

`[DISTINCTION]` This frame **adds no scope beyond the conformance question** and duplicates nothing:

- **Gate 4 conformance-gap map §2.5** already recorded this row's posture as *not-yet-conformant /
  requires reconciliation* at the O6 soft-guidance tier (an ungoverned soft writer modulating identity
  cadence). This frame states the questions that posture leaves open.
- **Role-inference cadence trace** and **pre-substrate Family G** are the carried point-in-time
  characterization (durable-soft, non-canon, indirect). They are not re-opened or re-measured.
- **Derived identity anchors frame (C)** is **downstream context only** — the `_maybe_emit_identity_anchor`
  emitter that role cadence modulates is the row-C surface; this frame **does not re-answer Q-D1–Q-D5**.
- **Spirit-return warmth frame (D1)** is a **separate split-D surface** — this frame **does not re-answer
  Q-W1–Q-W4** and shares no scope with it. **D1 and D2 are distinct runtime surfaces.**

---

## 1. Status, scope, method

`[FACT]` This is a **docs-only read-only conformance-question frame** scoped to **exactly one row**: role
inference / identity-anchor cadence. It is **no implementation, no tests, no runtime, no service, no
endpoint, no ingest/query, no instrumentation, no remedy, no verdict, no database/substrate.** Every
`[FACT]` below is **point-in-time and carried** from prior read-only artifacts (gap map §2.5, the
role-inference cadence trace, pre-substrate Family G, Stage A O6). **No new tracing, execution,
measurement, or frequency/workspace observation was performed**; specifics live in those artifacts at
their baselines and are not re-asserted as current here.

`[POSTURE]` **Required posture, carried verbatim in intent:** this is a *read-only conformance-question
frame*; role inference is a *durable-soft, non-canon, deterministic* identity-pressure signal whose
*conformance posture is not yet resolved* (*not-yet-conformant / requires reconciliation* at the O6
soft-guidance tier); this is *question framing only, no remedy*. Per Stage A O6 posture, the
not-yet-conformant flag is **not** a must-patch order; audit observes the seam without becoming authority.

`[NON-CLAIM]` **Carried boundary classification (load-bearing).** Role inference's memory/prompt-shaping
effect is **indirect, through derived identity-anchor cadence only**; it **writes no graph memory, no
canon, and no model-visible prompt text by itself**. The **dominant role is not identity authority**;
role scores are **not persona/voice writing** ("guidance signals only, never dominance, never persona
writing"). Its **diagnostic read-surface exposure** (e.g. a roles read-surface, a `role_context`
reporting field, a query-signature `dominant_role`) is a **reporting surface, not prompt authority and
not an authority crossing**. **"Durable-soft" ≠ "pinned."** This frame asserts none of "role is identity
authority", "role writes persona/voice", "role directly writes identity anchors", "role cadence is the
derived-anchor row", "role cadence is warmth", "patch role inference", "block roles", or "filter roles".

### 1.1 Required distinctions (carried)

`[DISTINCTION]`

```
D2 role-cadence ≠ C derived identity anchors
D2 role-cadence ≠ D1 warmth
role modulation of cadence ≠ direct identity-anchor writing
diagnostic read-surface exposure ≠ prompt authority
role profile ≠ identity authority
role profile ≠ persona/voice writing
durable-soft ≠ pinned
conformance characterization ≠ enforcement
audit visibility ≠ authority
```

### 1.2 Context that is not scope

`[CONTEXT]` Excluded from scope and **not** posed here: derived identity anchors (**C**, downstream
context only — the emitter role cadence modulates); spirit-return warmth (**D1**, separate split-D
surface); `gravity_correction` (**A**); mood-topology (**B**); authored `seed_text`; H3 / H5 / H6;
chamber / private cognition; database / substrate. The **affect-sensitive motif tightening** is a
separate, independent modifier inside the same anchor emitter and is **not** this surface; it is carried
as context only, not scope.

---

## 2. Carried facts (point-in-time; no new claims)

`[FACT]` Carried from the role-inference cadence trace, gap map §2.5, and pre-substrate Family G
(point-in-time, not re-measured):

- Role inference holds a **durable-soft, non-canon** role profile in **`roles.json`** (per agent), a
  **deterministic / offline / model-free keyword-scored** profile updated from **ingest-summary text via
  a slow EMA** (with a default explorer bias). A storage caveat is carried: the role save is non-atomic
  (the `IDENTITY-NON-ATOMIC-SAVE` class), and **durable-soft does not imply crash-safe, pinned, or
  canon**.
- **`dominant_role`** (argmax of the score map) maps to **`anchor_count_mult` / `anchor_gap_mult`**, which
  **scale the base identity-anchor cadence thresholds** (count / gap, with hard floors) **inside
  `_maybe_emit_identity_anchor`**.
- Role inference's memory/prompt-shaping effect is therefore **indirect, through derived identity-anchor
  cadence only**; **role modulation of cadence ≠ direct identity-anchor writing** (the derived anchor is
  emitted separately by the row-C emitter).
- It additionally has **diagnostic read-surface exposure** (a roles read-surface, a `role_context`
  reporting field, the query-signature `dominant_role`) — **reporting surfaces, not authority crossings**.

`[NON-CLAIM]` This records the modulation surface and its diagnostic exposure only. It asserts no
magnitude, no frequency, no canon, no authority, and no claim that role inference writes anchors, writes
persona/voice, or should be patched, blocked, or filtered.

---

## 3. Already-promoted boundaries this row is read against

`[BOUNDARY]` Named as the obligations the question is posed against — naming a boundary is not
implementing it; **no O6 mechanic, no must-not-pin implementation**, and no Seed-Gov or writer-authority
mechanic is selected:

- **Stage A — O6** (preserve-without-pinning: durable-soft guidance must **not** be durably pinned into
  rigid control) — role inference is recorded as an **O6 soft-tier** signal (Family G).
- **Pre-substrate Family G** (durable role inference: implemented in `roles.json`, updated from ingest
  text; modulates identity-anchor cadence → indirectly retrieval/prompt; non-canonical; durable-soft;
  "ungoverned soft writer modulating identity cadence"; later owner Stage A O6 soft tier + the matched
  writer-side lane) and **gap map §2.5** (this row's recorded posture). The Ledger Observational-Boundary
  ("audit observes authority; audit does not become authority") governs this frame's own posture; the
  cadence-trace anchor "guidance signals only, never dominance, never persona writing" is carried.

---

## 4. The conformance questions (posed only — not answered)

`[QUESTION]` Each item is **posed and left open**. None is answered; no remedy, fix order, writer-patch
selection, enforcement, governance vehicle, authority gate, canon claim, authority claim, O6 mechanic, or
must-not-pin implementation is offered or implied. "Conformance posture not yet resolved" stands for every
one.

- **Q-R1 — durable-soft persistence vs O6.** Does **durable-soft role-profile persistence** (`roles.json`)
  require a **resolved O6 soft-guidance posture** (preserve without pinning)? *Posed only; not answered
  here; no O6 mechanic or must-not-pin implementation selected.*
- **Q-R2 — indirect identity-cadence influence.** Does **role-modulated identity-anchor cadence** require
  a **resolved posture as indirect identity-cadence influence** — recognizing it as **modulation of
  cadence, not direct identity-anchor writing**? *Posed only; not answered here.*
- **Q-R3 — diagnostic read-surface exposure.** Does **diagnostic read-surface exposure** remain
  **non-authority / non-prompt-authority** (a reporting surface, not an authority crossing or prompt
  authority)? *Posed only; not answered here.*
- **Q-R4 — already-recorded routing.** Which **later-owner routings for this row are already recorded** by
  prior artifacts — **without this frame creating, selecting, or re-picking any**? *Posed only.*
  `[POSTURE]` As carried fact (not a selection): pre-substrate Family G and gap map §2.5 route this row to
  the **Stage A O6 soft tier + the matched writer-side reconciliation lane**. This frame **re-picks none,
  selects none, creates none, and resolves none** of them, and **does not open or continue any
  writer-authority work; it only characterizes the cadence-modulation surface**.

---

## 5. Surface-distinction and sequence note (framing sequence, not a fix order)

`[DISTINCTION]` This is **row D2**. **D2 (role inference / cadence), C (derived identity anchors), and D1
(spirit-return warmth) are distinct surfaces** and must not be conflated: D2 modulates the cadence of the
C emitter (it is **not** that emitter and **not** a direct anchor writer); D1 is retrieval-side warmth
shaping. With A, B, C, D1, and this D2 frame, the per-row question-framing pass over the conformance-gap
map's **five core runtime rows** would be complete once this draft is reviewed and promoted; this is a
**framing-sequence observation, not a fix order**,
and selects, schedules, and prioritizes no remedy and **no successor**. Whether any owner ever resolves
any posed question **requires separate authorization** (§8).

---

## 6. What this frame does NOT do or authorize

`[NON-AUTHORIZATION]` This frame poses a question and stops. It does **not** answer it, and authorizes
none of, and must not be read as implying:

```
answer / resolution of the question · safe/unsafe verdict · defect verdict · canon claim · authority claim
"role is identity authority" · "role writes persona/voice" · "role directly writes identity anchors"
"role cadence is the derived-anchor row" · "role cadence is warmth" · "patch role inference" · "block roles" · "filter roles"
re-answering derived-anchor Q-D1–Q-D5 · re-answering warmth Q-W1–Q-W4 · conflating D1 and D2 · conflating D2 and C
remedy · writer patch · fix order · enforcement behavior · governance vehicle · runtime authority gate
O6 mechanics · must-not-pin implementation · Seed-Governance mechanics · canon_source · source-sameness / P4 mechanics
code · tests · runtime · endpoint / service calls · ingest / query · workspace-frequency evidence
schema / store / carrier / serialization / migration · candidate store · governed admission implementation
seed rewrite · seed-revision API · chamber / private-cognition runtime · dream / incubation runtime
database / substrate · hidden chain-of-thought storage or exposure · hidden output blocker / finalizer
identity pinning · monitoring / autonomy layer · coercive mechanism · opening or continuing any writer-authority work (this frame only characterizes the cadence-modulation surface)
broadening to derived anchors (C) / warmth (D1) / gravity (A) / mood-topology (B) / seed_text / H3 / H5 / H6
```

`[NON-AUTHORIZATION]` It amends no upstream contract (Document A / Document B / Seed-Governance / P4 /
Stage A / Cluster 2 / Ledger / MCP boundary), opens no Stage B, opens no autonomy, **updates no
orientation map**, and makes **no registry amendment**.

---

## 7. Stopping evidence

`[STOP]` Stop, and do not extend this frame or draft any follow-up, if continuing would require: an
answer/resolution rather than a posed-and-open question; a safe/unsafe or defect verdict; a canon or
authority claim; any "role is identity authority" / "role writes persona/voice" / "role directly writes
identity anchors" / "role cadence is the derived-anchor row" / "role cadence is warmth" / "patch role
inference" / "block roles" / "filter roles" statement; any runtime / endpoint / service / ingest-query /
database / workspace-frequency evidence; a selected writer patch, fix order, enforcement behavior,
governance vehicle, or runtime authority gate; an O6 mechanic, must-not-pin implementation, Seed-Gov
mechanic, `canon_source`, or source-sameness mechanic; a schema / carrier / migration / database
decision; any chamber / candidate-store / seed-revision / governed-admission construction; a hidden
output blocker / finalizer; a hidden chain-of-thought store; identity pinning; a monitoring / autonomy /
coercive layer; opening or continuing any writer-authority work (this frame only characterizes the
cadence-modulation surface); re-answering Q-D1–Q-D5 or Q-W1–Q-W4; or conflating D2 with C or
D1. Also stop if the operator-supplied docs-only state is not `f845208` (or its named Gate-4-lane
predecessors), or if the question can no longer be posed read-only and non-substrate.

---

## 8. Next step after this frame

`[NON-AUTHORIZATION]` This frame is a **draft for Codex challenge (Codex as first reviewer) and operator
review**. **No automatic successor gate. No automatic implementation.** Any later step — including whether
any owner ever resolves Q-R1–Q-R4 (or the A / B / C / D1 questions) — requires **separate authorization**
and an explicitly-opened step; nothing here selects or schedules it. The orientation map is **not** updated
by this artifact; any pointer is a separate later step. Database / substrate remains last.

---

## Anti-drift footer

DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — question framing only, no remedy; no implementation, no
tests, no runtime, no service, no endpoint, no ingest / query, no workspace-frequency evidence, no
answer/resolution, no safe/unsafe verdict, no defect verdict, no canon claim, no authority claim, no "role
is identity authority" / "role writes persona/voice" / "role directly writes identity anchors" / "role
cadence is the derived-anchor row" / "role cadence is warmth" / "patch role inference" / "block roles" /
"filter roles", no writer-patch selection, no fix order, no enforcement, no governance vehicle, no runtime
authority gate, no O6 mechanics, no must-not-pin implementation, no Seed-Governance mechanics, no
`canon_source`, no source-sameness / P4 mechanics, no schema / store / carrier / serialization /
migration, no candidate store, no governed admission implementation, no seed rewrite, no seed-revision
API, no chamber / private-cognition runtime, no dream / incubation runtime, no database / substrate, no
hidden chain-of-thought storage or exposure, no hidden output blocker / finalizer, no identity pinning, no
monitoring / autonomy / coercive layer; this frame **does not open or continue any writer-authority work;
it only characterizes the cadence-modulation surface**; no orientation-map update, no
registry amendment. Scoped to one row — role inference / identity-anchor cadence — a **durable-soft,
non-canon, deterministic / offline / model-free** role profile in `roles.json` (slow EMA from
ingest-summary text), whose `dominant_role` maps to `anchor_count_mult` / `anchor_gap_mult` and
**modulates `_maybe_emit_identity_anchor` cadence**, reaching memory/prompt **indirectly through derived
identity-anchor cadence only**, plus **diagnostic read-surface exposure** (a reporting surface, **not**
prompt authority). **Role modulation of cadence ≠ direct identity-anchor writing; role profile ≠ identity
authority; role profile ≠ persona/voice writing; diagnostic read-surface exposure ≠ prompt authority;
durable-soft ≠ pinned.** Posed as **not-yet-conformant / requires reconciliation** at the O6 soft-guidance
tier whose **conformance posture is not yet resolved**; Q-R1–Q-R4 are **posed only, not answered**. **D2
(role-cadence), C (derived anchors), and D1 (warmth) are distinct runtime surfaces** and are not
conflated; this frame **does not re-answer** Q-D1–Q-D5 or Q-W1–Q-W4. The A→B→C→D order is a **framing
sequence, not a fix order**. conformance characterization ≠ enforcement; audit visibility ≠ authority;
inspection ≠ projection. Audit observes authority and does not become authority. Memory may shape context.
Memory may not seize authority. Database / substrate remains last.
