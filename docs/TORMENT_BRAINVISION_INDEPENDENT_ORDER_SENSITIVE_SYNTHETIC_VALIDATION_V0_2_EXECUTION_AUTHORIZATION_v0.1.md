# TORMENT Brainvision Independent Order-Sensitive Synthetic Validation v0.2 Execution Authorization v0.1

## Document Purpose

This is the final Stage S3B v0.2 execution-authorization artifact for one bounded future invocation of the already frozen offline experiment. It is drafted for independent review and remains uncommitted until the authoritative operator commits it in the required dedicated form.

This document contains the verified execution-authorization identity produced by dual-independent calculation. Drafting this file does not itself permit runner invocation, real-manifest contact, authority consumption, scientific evaluation, or publication.

Committing this document is a necessary precondition for technical usability, but it is not Hilmir's explicit one-run execution order. A separate explicit Hilmir order is required before any invocation.

## Machine-Readable Binding Block

BEGIN-SYNTHETIC-VALIDATION-V0-2-EXECUTION-AUTHORIZATION-BINDING-v0.1
authorization_schema=TORMENT_BRAINVISION_SYNTHETIC_VALIDATION_EXECUTION_AUTHORIZATION_BINDING
authorization_version=v0.2
later_execution_authorization_identity=715e24b1abb80ed04bbcff57ad4d0a8e33096f31af1093a7d1d3858b69f5f7af
runner_git_blob=c2dcdc25cc3e057bf63a49ccf8f4584e5ca20e5f
runner_raw_sha256=8e2fdbe5a10c0351d485cb4b650143b9aa61fb1a16c28e0de6e7caf1daf893e6
runner_test_git_blob=2f2189560f0c6a481594d1d7fe7fe46d036a0c92
runner_test_raw_sha256=d7d1e82cfa64eededbb3e989e11675f44a0b870756161440291ab4524c9e5462
schema_contract_git_blob=a37b818609ba7be24105776e2d83d82773909727
schema_contract_raw_sha256=ce57ae583a631da8255b6d87ddcea64346c378a70c54aeb3d2827247e2584986
v0_2_configuration_identity=fff90bf53f1c5a45a6c6fe5532208db479e4e153469aed3707accce2a8653be9
expected_manifest_external_sha256=05ce02af1c1a4b508e9a6566c9ff638849039df5caa45479858e634e2a117404
expected_manifest_payload_sha256=56a141bd13937caa6ac800ab8c9c12229f6bf75ee97ab490a26844664a65b4b9
END-SYNTHETIC-VALIDATION-V0-2-EXECUTION-AUTHORIZATION-BINDING-v0.1

## Activation Preconditions

The authorization can become technically usable only while this document is the latest commit touching its fixed repository path and that same commit is repository HEAD. The branch must be main, HEAD must match origin main, the working tree must be clean, and the latest commit touching this fixed path must be HEAD.

This document must be committed as the sole file in a dedicated docs-only commit. It deliberately does not embed that future commit hash, this document's Git blob, or repository HEAD.

Any later unrelated commit closes the authorization automatically because the latest path-touching commit would no longer be HEAD.

The runner may not be invoked merely because this document exists, is drafted, is reviewed, or is later committed. A separate explicit Hilmir one-run execution order is still required.

## Pre-Contact Requirements

Repository, implementation, byte-form, authorization identity, semantic identity, and parser checks must all pass before manifest contact. Any failed pre-contact check leaves authority unconsumed and causes zero real-manifest contact.

The runner-owned path, parser markers, ordered binding fields, implementation Git blobs, raw working-tree SHA values, schema-contract identity, configuration identity, and expected manifest identities must match the committed runner and accepted identity records exactly.

The runner rejects carriage returns anywhere in this document. This document is intended to be UTF-8 text with LF-only line endings and exactly one terminal LF. Because system Git has autocrlf behavior enabled, any later working-tree byte conversion that changes bound raw implementation bytes must fail closed before contact.

## Single-Use Authority

The final authority is intended for one authoritative invocation only after the dedicated commit and explicit Hilmir order. It does not permit retry after authority has been consumed.

The v0.2 lane preserves atomic authority consumption, retry prohibition, manifest-contact accounting, two-pass evaluation, byte-identical pass-bundle requirements, the existing exit-code model, the existing evidence machinery, and the existing publication paths.

No automatic rerun is authorized following scientific failure, controlled invalid outcome, infrastructure failure, evidence failure, publication failure, or any other consumed outcome.

## State Separation

The final authorization document being drafted does not mean the final authorization document has been committed.

The final authorization document being committed does not mean Hilmir has issued the explicit one-run execution order.

The runner being technically able to pass pre-contact validation does not mean the runner may be invoked without the explicit order.

The authority is conditional, single-use, and review-bound. It is not a standing operational permission.

## Windows Diagnostic Condition

Earlier bounded pytest replay reproducibly emitted a Windows access-violation diagnostic during Git subprocess interaction while pytest still returned exit code zero and all functional tests passed. Independent review found no source-level subprocess lifecycle defect and classified the behavior as an environment or toolchain anomaly.

Before any eventual invocation, a bounded pre-execution recheck must be completed. That recheck is limited to repository-state observation, Git pre-contact behavior, fail-closed refusal behavior, zero real-manifest contact, zero authority consumption, and zero authoritative artifact creation.

This document does not perform, schedule, or authorize that diagnostic during drafting.

## Permanent Boundaries

FORMAL_HOLD remains active. Mode 0 remains active. STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY remains the frozen conclusion.

This document does not authorize production-kernel modification, live Brainvision integration, memory-system integration, live capture or ingestion, service or runtime integration, PsiTRS contact, historical F3 reinterpretation, fixture changes, descriptor changes, tolerance, threshold tuning, majority rule, scientific rescue, or automatic publication.

The scientific experiment, evidence machinery, and publication model remain unchanged.

## Current Drafting Status

The retrofit is implemented, committed, synchronized, and identity-bound. The final execution-authorization specification has been committed. The authorization identity has been calculated twice independently and matched. This document contains the verified identity.

During drafting and independent review, this document remains uncommitted. Authoritative execution is not yet permitted. Real-manifest contact is not yet permitted. Authority consumption is not yet permitted. Scientific evaluation is not yet permitted. Publication is not permitted.

A later dedicated commit and a separate Hilmir order remain necessary before any invocation.

## Non-Authorization Closure

This document contains no runner invocation command. It does not instruct the operator to run now or proceed with execution. It does not claim that authority is active. It does not claim that manifest contact, authority consumption, scientific evaluation, or publication is currently permitted.

This document creates no launcher, wrapper, arming directory, execution journal, staging directory, terminal evidence file, publication bundle, manifest copy, or result artifact.
