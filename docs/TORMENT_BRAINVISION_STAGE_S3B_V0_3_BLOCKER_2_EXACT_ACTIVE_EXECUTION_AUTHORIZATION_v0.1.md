# TORMENT Brainvision Stage S3B v0.3 BLOCKER-2 Exact ACTIVE Execution Authorization v0.1

## 1. Purpose

This document is the human-readable ACTIVE governance authorization record for
TORMENT Brainvision Stage S3B v0.3 BLOCKER-2.

It authorizes only a later, separately controlled task to prepare one external
ACTIVE canonical JSON after this Markdown has been independently reviewed,
committed, pushed, and represented by committed document identity at the future
frozen repository baseline.

This document does not create executable authority by itself.

## 2. Baseline

The expected repository baseline for creating this document is:

```text
repository:
C:\TORMENT\TORMENT_repo\TORMENT-fabric_v2\torment_fabric

branch:
main

HEAD:
b39066d1974a2f0124e0d127be86d54f67268fe8

origin/main:
b39066d1974a2f0124e0d127be86d54f67268fe8

HEAD == origin/main:
true

working tree:
clean before this file is created

.git\index.lock:
absent
```

This document does not bind its own future containing commit. The future
committed repository state produced after this document is reviewed, committed,
and pushed is not known at document-writing time.

## 3. Scope

authorization_status = ACTIVE

The ACTIVE status is a governance and authorization declaration for later
external JSON preparation only.

This Markdown alone cannot invoke the wrapper.

This Markdown alone cannot consume authority.

This Markdown alone cannot create GLOBAL_AUTHORITY_ENTRY.

This Markdown alone cannot create LOCAL_GATE_ENTRY.

This Markdown alone cannot create RUN_RESULT.

This Markdown alone cannot create RETAINED_COMPLETION.

Operational activation requires a separately generated, exact-byte canonical,
independently reviewed external ACTIVE JSON that binds this Markdown after it is
committed and after the future synchronized repository commit is frozen as
H_ACTIVE.

This document does not authorize PREFLIGHT_ONLY.

This document does not authorize EXECUTE_EXACT_SINGLE_RUN.

This document does not authorize its own commit.

This document does not authorize external ACTIVE JSON generation in the current
task.

## 4. Permanent Boundaries

The following permanent project boundaries remain active:

```text
FORMAL_HOLD = active
Mode_0 = active
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

Brainvision remains:

```text
offline
quarantined
synthetic-only
non-production
non-service
non-kernel
non-memory-integrated
non-cognitive
non-autonomous
```

This document does not modify or authorize modification of:

```text
torment_service/kernel/
production TORMENT memory functionality
live service behaviour
prompt surfaces
action surfaces
autonomy
identity
truth selection
memory cognition
```

This document does not claim:

```text
rename atomicity
rename durability
power-loss persistence
general Windows support
production readiness
BLOCKER-2 closure
real-world Brainvision readiness
strong order sensitivity
```

## 5. BLOCKER State

The BLOCKER state remains:

```text
BLOCKER-1:
closed within its exact bounded local-fixed-NTFS profile

BLOCKER-2:
OPEN and active

BLOCKER-3:
closed

BLOCKER-4:
open but inactive
```

This document does not begin BLOCKER-4.

This document does not close BLOCKER-2.

## 6. Controlling Documents

This document is governed by the committed implementation semantics and by the
following controlling documents:

```text
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_AUTHORITATIVE_RETAINED_SINGLE_RUN_ASSESSMENT_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_EXACT_AUTHORITATIVE_RETAINED_SINGLE_RUN_AUTHORIZATION_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_OPERATOR_WRAPPER_AND_PATH_PREPARATION_AUTHORIZATION_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_WRAPPER_POST_COMMIT_IDENTITY_AND_PATH_PREPARATION_FINDINGS_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_IDENTITY_DERIVATION_CYCLE_CORRECTION_AUTHORIZATION_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_EXACT_INACTIVE_EXECUTION_AUTHORIZATION_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_POST_COMMIT_INACTIVE_ARTIFACT_IDENTITY_AND_ACTIVE_PREPARATION_ASSESSMENT_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_ACTIVE_AUTHORIZATION_IDENTITY_DESIGN_CORRECTION_AUTHORIZATION_v0.1.md

docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_POST_COMMIT_ACTIVE_SEQUENCING_IDENTITY_BINDING_AND_ACTIVE_MARKDOWN_PREPARATION_AUTHORIZATION_v0.1.md
```

The committed implementation and tests inspected for the current authorization
surface are:

```text
research/brainvision/run_blocker2_authoritative_retained_single_run_v0_1.py

research/brainvision/blocker2_retained_absolute_path_control_v0_1.py

research/brainvision/test_run_blocker2_authoritative_retained_single_run_v0_1.py

research/brainvision/test_run_blocker2_authoritative_retained_single_run_integration_v0_1.py

research/brainvision/test_blocker2_retained_absolute_path_control_v0_1.py

research/brainvision/test_blocker2_retained_absolute_path_control_integration_v0_1.py
```

Committed implementation semantics are the source of truth.

## 7. Locked Execution Profile

The future external ACTIVE JSON may bind only this retained execution profile:

```text
case set:
A1
A2
A3
A5

case order:
A1
A2
A3
A5

A6:
false

real executor selector:
REAL_EXISTING_ABSOLUTE_PATH_A1_A2_A3_A5_V0_1

fault injection:
disabled

single process:
required

single attempt:
required

retry:
prohibited

resume:
prohibited

overwrite:
prohibited

repair after authority consumption:
prohibited

executor substitution:
prohibited

case-set substitution:
prohibited

case-order substitution:
prohibited
```

Operator identity:

```text
Hilmir
```

The one-shot authority semantics prohibit retry, resume, overwrite, repair after
authority consumption, executor substitution, case-set substitution, and
case-order substitution. A future execution, if separately authorized, must be a
single process and a single attempt.

## 8. Prepared Roots And Preparation Evidence

The prepared fixed roots are:

```text
authority registry root:
C:\TORMENT\brainvision_authority\blocker2_s3b_v0_3

fixture root:
C:\TORMENT\brainvision_authoritative_fixture\blocker2_s3b_v0_3

result parent:
C:\TORMENT\brainvision_retained_results\blocker2_s3b_v0_3
```

Prepared root identities:

```text
authority root:
8cf4a6200aa912977fc7f63df057f467f8d2238a38f7f9b7cee76b253210afba

fixture root:
f9c5f5b62524f345cbb4f5b46cad89553a6054d87bb1048fe4ae61a478be0934

result parent:
edecbab40b38808b992a9ccde3ebcd233266eef03e8cff02fd46f9aa7e2c83d6
```

Filesystem profile:

```text
ordinary directories
non-reparse
outside repository
DRIVE_FIXED
NTFS
volume serial 2698958771
```

Preparation input:

```text
C:\TORMENT\brainvision_retained_results\blocker2_s3b_v0_3_preparation\PREPARE_PATHS_INPUT.canonical.json
```

Preparation-input SHA-256:

```text
fa0bf1b02acea1a4c52d3724f1d9dbdcbeaf748ca21a32a07faf7d42398322aa
```

Preparation result:

```text
C:\TORMENT\brainvision_retained_results\blocker2_s3b_v0_3_preparation\PREPARE_PATHS_RESULT.canonical.json
```

Preparation result facts:

```text
terminal_label = PREPARATION_COMPLETE
authoritative = false
retained_execution = false
authority_consumed = false
byte length = 3726
SHA-256 = 51600a85ee8e49187e3c443e337ea8a23688ecdd19e47aa843b64ed17ce33a16
```

This document does not authorize rerunning PREPARE_PATHS.

## 9. Policy And Schema Identities

The retained orchestration policy identity is:

```text
3d9b66a180fabf00c8bb6695c74fc9d69d21cd3ac9335cc5d2dc3a1169417531
```

The native helper policy identity is:

```text
e1094b40c5f312e32c48e6ac125c2e961996f52656b951646cecbf7432419928
```

These identities remain distinct. Native helper evidence must not be relabelled
as retained-policy evidence.

Other unchanged identities to be reverified at H_ACTIVE are:

```text
default case set:
b24057bb7ec233414d670a3e1e2aabb90f8a2728ff590b0ed4e811faf8e5f1b1

fixture profile:
3c2f65092fc6efcf58726390e4b4b9ff9ba00a73dbad68eb5b612d49a788a5a1

authority registry profile:
aa3368028954f86d294fce0dbcf61117be5750dd87202971ae4a2a8d456c2734

evidence chain:
185e4dea85abf436ac93a01632f0b1ab4895086177e2de073608a0c08b2d174b

retained schema:
81a6a21e06b397b1accd228fb37308945ebc926cb409f816789a22df39e94b3c
```

Every supposedly unchanged value must later be reverified at H_ACTIVE.

## 10. Execution Surface Authorization

The later external ACTIVE JSON must cryptographically bind the exact nine-file
execution surface:

```text
research/brainvision/run_blocker2_authoritative_retained_single_run_v0_1.py

research/brainvision/test_run_blocker2_authoritative_retained_single_run_v0_1.py

research/brainvision/test_run_blocker2_authoritative_retained_single_run_integration_v0_1.py

research/brainvision/blocker2_retained_absolute_path_control_v0_1.py

research/brainvision/test_blocker2_retained_absolute_path_control_v0_1.py

research/brainvision/test_blocker2_retained_absolute_path_control_integration_v0_1.py

research/brainvision/validate_windows_same_volume_no_replace_promotion_v0_1.py

research/brainvision/test_validate_windows_same_volume_no_replace_promotion_v0_1.py

research/brainvision/test_validate_windows_same_volume_no_replace_promotion_integration_v0_1.py
```

This Markdown authorizes this execution surface in governance terms.

The later external ACTIVE JSON performs the exact cryptographic source and
identity binding.

The Markdown itself is then bound into the external JSON by committed document
identity.

This Markdown does not hardcode current source Git blobs as final ACTIVE blobs.
The future external JSON must reverify the exact committed execution-surface
identities at H_ACTIVE.

## 11. ACTIVE Markdown Document-Identity Role

The future committed Markdown will later be represented in:

```text
execution_authorization_document_identity
```

and:

```text
document_identity_inventory
```

execution_authorization_document_identity binds:

```text
repository-relative path
Git blob ID
exact-byte SHA-256
authorization_status = ACTIVE
canonical authorization declaration identity
```

document_identity_inventory binds:

```text
repository-relative path
Git blob ID
exact-byte SHA-256
exact byte length
any descriptive/hash-bound role field included by the later canonical JSON
```

The role field is descriptive and hash-bound if included.

The current wrapper does not semantically interpret or enforce the role value.

The byte length is validated through document_identity_inventory.

Byte length is not inherently a field of execution_authorization_document_identity.

No new schema field is invented by this Markdown.

## 12. Self-Reference Prohibitions

This ACTIVE Markdown must not bind:

```text
its own future containing commit identity

H_ACTIVE before H_ACTIVE exists

the future external ACTIVE JSON Git blob

the future external ACTIVE JSON exact-byte SHA-256

the future external ACTIVE JSON byte length

the future external ACTIVE JSON authorization_input_identity

future result-directory identity

future run identity

future preflight results

future authority-consumption state
```

This document may describe how those values will later be derived.

This document does not include placeholder values for them.

This document does not claim they already exist.

## 13. Future H_ACTIVE Sequence

This document authorizes only this later sequence:

```text
1. Independently review this ACTIVE Markdown.
2. Commit and push this ACTIVE Markdown.
3. Verify HEAD == origin/main.
4. Define the resulting synchronized full commit identity as H_ACTIVE.
5. Freeze repository state.
6. Collect this Markdown's committed Git blob, SHA-256, and byte length.
7. Generate the external ACTIVE canonical JSON outside the repository.
8. Bind H_ACTIVE and this committed Markdown into that JSON.
9. Independently review the external JSON.
10. Separately authorize PREFLIGHT_ONLY.
11. Review preflight evidence.
12. Separately authorize EXECUTE_EXACT_SINGLE_RUN exactly once.
```

This task authorizes none of those operational steps.

The resulting Markdown must not authorize its own commit.

The resulting Markdown must not authorize external JSON generation in the
current task.

This Markdown may authorize a later separately controlled external-JSON
preparation task.

## 14. Frozen H_ACTIVE Requirements

After this ACTIVE Markdown is committed in the future:

```text
HEAD == origin/main == H_ACTIVE
working tree clean
.git\index.lock absent
```

No repository movement is allowed before preflight or execution:

```text
no commit
no amend
no rebase
no merge
no reset
no branch-changing checkout
no branch-changing push
no source modification
no documentation modification
no test modification
no untracked artifact inside repository
```

If repository state changes:

```text
pause the lane
do not run preflight
do not execute
derive a new baseline and new identities under separate authorization
```

## 15. Future External ACTIVE JSON

This Markdown authorizes only a later preparation task for one external
canonical JSON.

Expected schema:

```text
torment.brainvision.blocker2.operator_wrapper.authorization_input.v0.2
```

Required status:

```text
authorization_status = ACTIVE
```

Required wrapper mode:

```text
EXECUTE_EXACT_SINGLE_RUN
```

Required authoritative field:

```text
authoritative = true
```

The future JSON will be:

```text
external to the Git repository
uncommitted
supplied by operator-selected absolute CLI path
exact-byte canonical
plain SHA-256 hashed
authorization_input_identity bound
committed ACTIVE Markdown bound
H_ACTIVE bound
```

The external JSON path is a procedural operator/review location.

The path is not included in authorization_input_identity.

The path is not cryptographically bound by the wrapper.

Execution authority does not derive from file presence or path identity.

Classification:

```text
ACTIVE JSON Git blob identity:
MUST NOT BE BOUND AT THIS LAYER
```

No external ACTIVE JSON was created by this task.

## 16. Historical Inactive Identities

The following identities are preserved as historical evidence only:

```text
execution_authorization_identity:
66028ecbdaceb94b1789225c7752fa9ac9796c5fea41e11017264d17c0752785

result_directory:
C:\TORMENT\brainvision_retained_results\blocker2_s3b_v0_3\66028ecbdaceb94b1789225c7752fa9ac9796c5fea41e11017264d17c0752785

result_directory_identity:
31072e5528843492aa4666174f171460f3ad65602ace60fa56ec4eceeb1c2721

run_identity:
889090b759549449daa6b06f2e72dbf97564ce55fc18448271915e3d3c1354ad
```

They must not be reused as ACTIVE execution identities.

No projection from:

```text
4673b7d...
f1a08d34...
b39066d...
```

is final ACTIVE authority.

Final ACTIVE identities can only be derived after this ACTIVE Markdown is
committed in the future and H_ACTIVE is frozen.

## 17. Future Recomputation And Reverification Set

The later external-JSON preparation task must derive only after the ACTIVE
Markdown is committed, its committed identity is known, HEAD == origin/main ==
H_ACTIVE, and repository state is frozen:

```text
repository-state identity

execution_authorization_document_identity

execution_authorization_identity

result_directory

result_directory_identity

run_identity

authorization_input_identity

external ACTIVE JSON exact-byte SHA-256

external ACTIVE JSON byte length
```

The following may remain unchanged only if reverified at H_ACTIVE:

```text
retained orchestration policy identity
native-helper policy identity
retained schema identity
case-set identity
case order
A6=false
executor selector
fault-injection setting
prepared-root identities
host identity
volume identity
operator identity
single-attempt declaration
execution-surface identities
```

## 18. Evidence-Chain Truth Model

The evidence chain remains:

```text
GLOBAL_AUTHORITY_ENTRY
->
LOCAL_GATE_ENTRY
->
RUN_RESULT
->
RETAINED_COMPLETION
```

Truth model:

```text
RUN_RESULT:
retained_execution = false

RETAINED_COMPLETION:
sole record permitted to declare retained_execution = true
```

Authoritative completion exists only as:

```text
durable verified RUN_RESULT
+
durable verified RETAINED_COMPLETION
```

ACTIVE authorization alone does not prove:

```text
execution occurred

authority was consumed

retained completion exists

BLOCKER-2 is closed
```

## 19. Real Evidence State

At this document-writing task boundary:

```text
derived result directory:
absent

GLOBAL_AUTHORITY_ENTRY:
absent

LOCAL_GATE_ENTRY:
absent

RUN_RESULT:
absent

RETAINED_COMPLETION:
absent

authority_consumed:
false
```

No evidence object was created or mutated by this task.

PREPARE_PATHS was not rerun.

PREFLIGHT_ONLY was not executed.

EXECUTE_EXACT_SINGLE_RUN was not executed.

No authority was consumed.

## 20. Prohibited Actions

This document does not authorize:

```text
stage
commit
push
derive H_ACTIVE
freeze H_ACTIVE
collect a committed blob for this new Markdown before it is committed
generate external ACTIVE JSON in the current task
prepare preflight commands
run PREPARE_PATHS
run PREFLIGHT_ONLY
run EXECUTE_EXACT_SINGLE_RUN
consume authority
mutate evidence
modify runtime code
modify wrapper code
modify schemas
modify tests
begin BLOCKER-4
claim BLOCKER-2 closure
```

## 21. Limitations

This document is a governance record. It is not a canonical JSON authorization
input and is not accepted by the wrapper as operational authorization.

This document records the exact execution profile and authorizes later
preparation of an external ACTIVE canonical JSON, but the future external JSON
must perform the cryptographic source binding, repository-state binding,
committed Markdown binding, authorization-input identity binding, result
directory derivation, result-directory identity derivation, and run-identity
derivation after H_ACTIVE exists.

This document does not claim that any ACTIVE execution identity, ACTIVE
result-directory identity, ACTIVE run identity, ACTIVE external JSON exact-byte
SHA-256, ACTIVE external JSON byte length, or ACTIVE authorization_input_identity
already exists.

## 22. Exact Stop Boundary

This task ends after:

```text
the one requested ACTIVE Markdown is created
its exact bytes are hashed
its contents are validated
the working tree contains exactly that one untracked document
```

The final state for this task must remain:

```text
no external ACTIVE JSON was created
no H_ACTIVE was derived
no PREFLIGHT_ONLY was executed
no EXECUTE_EXACT_SINGLE_RUN was executed
no authority was consumed
BLOCKER-2 remains OPEN
BLOCKER-4 remains inactive
```

## 23. Next Separately Authorized Task

The next task, if Hilmir separately authorizes it after review, is to commit and
push this ACTIVE Markdown, verify HEAD == origin/main, define H_ACTIVE, freeze
repository state, and only then prepare one external ACTIVE canonical JSON
outside the repository for independent review.

That later task must not run PREFLIGHT_ONLY or EXECUTE_EXACT_SINGLE_RUN unless
those steps receive their own separate authorizations.
