# TORMENT — Document A Admission-Crossing Target-Selection Frame v0.1

**Status:** DOCS-ONLY target-selection frame. **Non-authorizing. Does NOT self-select or self-authorize.**
It presents — for operator decision — the target-selection question created by the prior convergence frame
(`docs/TORMENT_DOCUMENT_A_ADMISSION_CROSSING_ROOT_BLOCKER_CONVERGENCE_FRAME_v0.1.md`, verdict *NO-OPEN /
target-selection deferred to operator*). It selects no mechanic, opens no gate, authorizes no code/test,
names no durable field / schema / carrier / store, and amends no contract. Navigation / decision-surface
aid only.

**Authority note:** Document A (containment wall / admission edge), the P4 Reader/Projection-Safety
Contract, the Gate B writer-authority doctrine, Document B, the Ledger Observational-Boundary, the
Decision Registry, and `PROJECT_ORIENTATION_MAP.md` §0 remain source of truth. This frame reads them; it
does not amend, weaken, or reinterpret them.

**Doctrine (carried, exact):**

> Memory may guide context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.
> Automatic remains allowed only where separately ratified. Autonomous remains unopened.

---

## 1. Status / non-authorizing banner

Decision-surface only. This frame records the options and their bounds so the operator can decide; it
**does not choose**. Presenting "SELECT" as an option neither selects it nor blesses any downstream work.
No admission crossing / carrier / mechanism / workflow / actor / API / UI / runtime / persistence is
created or implied. The full non-authorization list is §7.

## 2. Inherited decisions and bounds (carried, NOT re-decided here)

- **Admission authority floor = `operator-only`** — the requirement-level FLOOR for any future governed
  admission (`..._GATE_A_GOVERNED_ADMISSION_AUTHORITY_OPTION_SELECTION_FRAME_v0.1.md`). Unchanged by this
  frame and unchanged by any option below.
- **Stage B = framing-only** — the open lane is a *framing* lane (cognition-roadmap
  ratification-to-implementation sequencing); it opens no database / schema / storage / carrier /
  migration mechanics (`..._L2_STAGE_B_OPENING_DECISION_RECORD_v0.1.md`).
- **Substrate = §K-eligible, NOT authorized** — the unmet-transactional-guarantees trigger makes
  substrate-design *eligible*; eligibility ≠ authorization
  (`..._SUBSTRATE_READINESS_PHASE_CONSOLIDATION_MEMO_v0.1.md`).
- **Candidate representation / pre-carrier constraints** — the inert candidate representation is selected
  and its pre-carrier constraints are framed; the durable carrier itself remains unselected.
- **Convergence established** — the Document A governed admission crossing **+ candidate carrier** is the
  shared root blocker beneath Gate A containment, P4 O1/O2 reader-trace / source-sameness, Gate B durable
  writer resolution, and Dream/Regime-B staging→crossing; the **carrier** is the true shared hard
  dependency (convergence frame).
- **Current HOLD posture** — Gate A producer-independent wall = HOLD; P4 mechanics = unopened; Gate B
  writer-authority micro-work = HOLD; Dream / Regime-B = HOLD / blocker-dependent.

## 3. The target-selection question

> Should the **Document A governed admission crossing + candidate carrier** now become the **selected next
> design-framing target** — under the existing **operator-only** authority floor, **Stage-B framing-only**
> posture, and **§K substrate eligibility-not-authorization** — or should it **remain HOLD**?

This is a **sequencing / agenda** decision (what to *frame* next), not a mechanism decision. It is the
operator's to make; this frame does not answer it.

## 4. Options

**Option S — SELECT.** Name the Document A governed admission crossing + candidate carrier as the next
design-framing target. The item moves from *deferred / root-blocker convergence* to *named next framing
target*. What this authorizes and does not authorize is spelled out in §5–§6. A future design-framing pass
would itself be a **separate, separately-authorized, still-docs-only** slice.

**Option H — HOLD.** Retain the convergence frame's *NO-OPEN* posture; select no target now; leave the
item deferred. Fully consistent with the current HOLD posture across Gate A / P4 / Gate B / Dream, and with
the operator-controlled §K eligibility gate.

**Option R — REVIEW-FIRST (optional / background).** Commission a further Codex adversarial review of the
convergence frame's root-blocker claim before any selection. **Note:** Codex has **already PASSed** the
convergence frame, so R is optional and only warranted if the operator wants additional review; it is not
a prerequisite for S or H.

*This frame presents S and H as the live choices (R optional) and recommends neither.*

## 5. What SELECT (Option S) WOULD authorize

- **Only** that a **future, separately-authorized, docs-only design-framing pass** may take the Document A
  governed admission crossing + candidate carrier as its **subject**; and
- that the item **moves from deferred / root-blocker convergence to named next framing target.**

That is the entire scope of a SELECT outcome: it sets the agenda. It grants **no build authority**, and any
subsequent design-framing pass is a separate approved slice.

## 6. What SELECT (Option S) would NOT authorize

- carrier / store / schema / field / enum / ID design or selection;
- admission mechanism / crossing / workflow / decision-procedure / actor / API / UI;
- database / substrate construction or Stage-B mechanics;
- any change to the **operator-only** authority floor;
- writer crossing or Gate B fix;
- P4 `ReaderPolicy` / source-sameness / `diagnostic_only` mechanics;
- Dream / Regime-B runtime;
- scheduler / trigger / budget / autonomy;
- Gate D / Envelope-Audit / Document B chamber runtime;
- AgentRunner / app / spine / MCP wiring;
- model / provider / API / prompt path;
- memory writes / persistence / logging / transcripts;
- output-control / finalizer / refusal / identity / canon behavior;
- dynamic-kernel or `conversation_shock`;
- any contract amendment.

**Selection sets the framing agenda; it opens no mechanics.**

## 7. Explicit non-authorizations

This frame (regardless of which option the operator later picks) opens none of the surfaces listed in §6.
It is docs-only; it selects nothing; it authorizes no code, test, mechanism, carrier, gate, runtime, or
lane; it amends no Document A / P4 / Gate A / Gate B / Document B / Cluster 2 / Ledger / Stage-A / Stage-B
contract; and it preserves the doctrine verbatim. Choosing HOLD opens nothing; choosing SELECT opens only
the §5 agenda item and still opens no mechanics.

## 8. Verdict

**PRESENTED FOR OPERATOR DECISION / NO MECHANICS OPENED.** The target-selection question (§3) is recorded
with Options S and H as the live choices (R optional; Codex has already PASSed the convergence frame). This
frame **does not choose** and **opens no mechanics, carrier, gate, runtime, or lane**. Whichever option the
operator selects, the inherited bounds hold: `operator-only` floor unchanged, Stage-B framing-only,
substrate §K-eligible-not-authorized, and every mechanism deferred to a future, separately-authorized
decision.

*End — Document A Admission-Crossing Target-Selection Frame v0.1. Docs-only, non-authorizing, non-selecting.
Verdict: PRESENTED FOR OPERATOR DECISION / NO MECHANICS OPENED.*
