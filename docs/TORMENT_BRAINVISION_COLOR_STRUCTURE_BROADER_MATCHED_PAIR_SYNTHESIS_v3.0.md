# TORMENT Brainvision Color Structure Broader Matched-Pair Synthesis v3.0

## 1. Status / quarantine and non-claims

**DOCS-ONLY synthesis. Non-authorizing, non-implementing. Opens no runtime, integration, or implementation
lane.** It synthesizes the accepted v2.9 broader matched-pair diagnostic and recommends the next direction
**without opening any implementation**. It **authorizes no code and no tests**, invents no threshold, defines
no replacement acceptance criteria, changes no formula / §7 anti-proxy logic / §8 verdict logic, deletes or
weakens no control, and implements no fixture. Everything discussed stays offline under `research/brainvision/`
+ `tests/research/`, HELD per v0.6.

This note makes **no** vision claim, **no** "Brainvision sees" claim, and **no** temporal-order claim.
`first_pass_structure_validity_claim_allowed` remains **False** and `temporal_claim_allowed` remains **False**.
It touches no `torment_service/`, runtime, camera / sensor / live-capture / screen-capture / streaming, or
prompt / context / memory / action / render-body / autonomy paths, and makes **no real-clip / local-clip move**
and **no memory-system integration**. Brainvision Path B is **not proven vision** and is **not a functioning
vision layer for TORMENT memory**. **No `§0` pointer; no tags.**

## 2. Accepted v2.9 findings

Carried forward by identity from the accepted, committed v2.9 slice:

```text
v2.9 committed edge:                          d6c0c89  (research(brainvision): broaden matched-pair residual diagnostic)
tests:                                        passed per operator / Codex (v2.9 + v2.4 regression + full suite)
classification:                               mixed_or_unresolved
directional axis:                             directional_B_strengthened
per-channel-spectral axis:                    per_channel_C_strengthened
A_descriptor_limitation_supported:            False
verdict:                                      HOLD
first_pass_structure_validity_claim_allowed:  False
temporal_claim_allowed:                        False
```

The classification is **reporting-only** and cannot re-grade the frozen gate.

## 3. What v2.9 changed

- **Directional (B):** v2.5 recorded the directional validity-surface mismatch as a *candidate*; v2.9
  **strengthened** it with broader directional matched-pair evidence (multiple angular speeds / a radius
  variant / a phase offset, all separating at matched directional blockers, plus smoothness-without-winding
  cases that stay low `S` / `PSC`).
- **Per-channel (C):** v2.6 recorded the per-channel bank-composition artifact as a *candidate*; v2.9
  **strengthened** it with **non-collinear** per-channel matching (a non-collinear canceller matched centroid
  and spread within tolerance and still separated).
- **Collinear caveat weakened:** the v2.6 caution that the per-channel matched pairs were **collinear-only** is
  weakened, because v2.9 found a **non-collinear** match that behaves the same way.
- **A stays unsupported:** a direct descriptor limitation remains **not supported** by the current matched-pair
  evidence (on both axes `S` / `PSC` still separates at matched blockers).
- **Both axes still unresolved:** *strengthened is not resolved* — each candidate reading is better supported,
  but neither is established, and the frozen §7 gate still HOLDs.

## 4. Current Path B interpretation

```text
directional_validity_surface_mismatch_candidate_strengthened = True
directional_proxy_failure_resolved                           = False
per_channel_bank_composition_artifact_candidate_strengthened = True
per_channel_proxy_failure_resolved                           = False
A_descriptor_limitation_supported                            = False
descriptor_validity_claim_allowed                            = False
verdict                                                      = HOLD
```

Both candidate readings are strengthened and mutually distinct; neither resolves, and neither licenses any
gate, control, or validity move.

## 5. Remaining risks

- The evidence is **synthetic-only** (constructed chroma-plane fixtures).
- It is **not real clips** and authorizes no real-clip / local-clip move.
- It is **not temporal order**; `temporal_claim_allowed = False`.
- It is **not memory integration**; no memory-system contact.
- The matched-pair families are **broader but not exhaustive** — more angular / per-channel regimes remain
  untested.
- The **smoothness / per-channel controls may still hide untested cases** (e.g. constructions not in the
  predeclared pools).
- The frozen pooled **§7 still HOLDs** on both axes.
- **No descriptor-validity claim is allowed.**

## 6. Candidate next branches

### A. HOLD / freeze Path B interpretation for now

**Pros.** Prevents endless synthetic tuning; the current residual map is now strong enough to preserve; avoids
gate-gaming.

**Risks.** Leaves possible improvements unexplored.

### B. Doctrine-level validity-surface review

**Pros.** Addresses whether §7 is too strict for target-adjacent directional geometry (the deepest question
from v2.5).

**Risks.** Could drift into **relaxing §7 or defining away confounds**.

### C. Null / control-bank interpretation review

**Pros.** Addresses the strengthened per-channel C reading directly.

**Risks.** Could drift into **control deletion / re-weighting**.

### D. More diagnostics

**Pros.** More evidence.

**Risks.** **Highest fixture-chase risk.**

## 7. Recommended next direction

**Recommended: A — HOLD / freeze the Path B color-structure interpretation for now, with a docs-only closure
note if needed.**

**Reason.** v2.9 strengthened both candidate readings and kept A unsupported. More immediate code carries
**diminishing returns and fixture-chase risk** (branch D), while the doctrine (B) and control-bank (C) branches
are **too risky until the interpretation is frozen** — each risks touching the gate or the controls before the
evidence justifies it. Freezing preserves a now-strong residual map without overfitting.

Suggested outcome (reporting-only; none of these move the verdict or any gate):

```text
path_b_color_structure_interpretation_frozen = True
implementation_opened                        = False
next_code_slice_recommended                  = False
gate_change_allowed                          = False
control_deletion_allowed                     = False
descriptor_validity_claim_allowed            = False
verdict                                      = HOLD
```

Branches B, C, and D remain **recorded-but-unopened**; the real-clip / local-clip move and any memory-system
integration stay disallowed.

## 8. Recommended follow-up

Recommend **either**:

- **stop this Brainvision Path B arc here for now** (freeze in place, no further note), **or**
- **create one final docs-only closure / handoff note for a fresh chat** that carries the frozen residual map
  (directional B strengthened / per-channel C strengthened / A unsupported / verdict HOLD) forward.

No `§0` pointer is added here, and none should be added unless the operator explicitly requests it.

- **Codex review** of this synthesis and of the recommendation to **freeze** the Path B color-structure
  interpretation (branch A), keeping branches B, C, and D recorded-but-unopened, the verdict at HOLD, and all
  disallowed moves disallowed.
- **If the operator explicitly chooses a follow-up, it should be either stopping here or a single docs-only
  closure / handoff note; otherwise HOLD.**

Brainvision remains **offline / quarantined**, HELD per v0.6.
`first_pass_structure_validity_claim_allowed = False` and `temporal_claim_allowed = False` are unchanged.
**No `§0` pointer; no tags.**

*End — TORMENT Brainvision Color Structure Broader Matched-Pair Synthesis v3.0. Docs-only, non-authorizing.
Opens no implementation lane; changes no frozen formula, gate, or verdict; deletes or weakens no control;
invents no threshold; makes no descriptor-validity claim; no `§0` pointer added; no tags.*
