# TORMENT Memory Substrate — I4G-R2 Empty-Scope Source-Posture Ratification

## Status

~~~text
FROZEN_SUCCESSOR_SOURCE_POSTURE_CONTRACT

I4G_R1_TOPOLOGY_LAW_PRESERVED
I4G_R1_ZERO_SHARED_REMEDY_SUPERSEDED

IMPLEMENTATION_NOT_YET_QUALIFIED
REAL_ROOT_WRITE_NOT_AUTHORIZED
WRITER_FREEZE_NOT_AUTHORIZED
~~~

## Purpose, predecessors, and narrow supersession

This documentation-only successor records a narrowly superseding decision
following the adversarial evidence named
TORMENT_I4G_R1_PRE_FREEZE_MATERIALIZATION_SOURCE_CLASSIFICATION_ADVERSARIAL_REVIEW.md.

It preserves the frozen I4G-R1 public topology law in
[TORMENT_MEMORY_SUBSTRATE_I4G_R1_ROOT_TOPOLOGY_SUCCESSOR_QUALIFICATION_v0.1.md](TORMENT_MEMORY_SUBSTRATE_I4G_R1_ROOT_TOPOLOGY_SUCCESSOR_QUALIFICATION_v0.1.md):

~~~text
PUBLIC_PRIVATE_CARDINALITY = 0_TO_N
PUBLIC_SHARED_CARDINALITY = 1_TO_N

PRIVATE_ONLY_PUBLIC_WORKSPACE = NOT_LAWFUL
MULTI_PRIVATE_LAWFUL = YES
MULTI_SHARED_LAWFUL = YES
ZERO_PRIVATE_WITH_SHARED_LAWFUL = YES
~~~

It does not rewrite historical I4G, I4G-R1, or real-root observation evidence.
It supersedes only I4G-R1's selected remedy:

~~~text
I4G_R1_ZERO_SHARED_REMEDY =
    PRE_FREEZE_LEGACY_MATERIALIZATION

I4G_R2_ZERO_SHARED_REMEDY =
    DECLARED_EMPTY_SHARED_NATIVE_ADMISSION
~~~

## Why ordinary legacy opening is refused

I4G-R1 had selected ordinary pre-freeze legacy workspace opening as the
zero-shared remedy. New static archaeology establishes that
TormentFabric.get_workspace(workspace_id) is not evidence-preserving for all
six observed zero-shared workspaces.

When workspace_meta.json is absent, that legacy operation writes a new
embedding lock using the currently configured embed_provider, embed_model, and
embed_dim. For ws3, ws4, and ws5, this would manufacture source
representation identity where historical vector geometry remains unknown.

~~~text
ORDINARY_LEGACY_OPEN_AS_ZERO_SHARED_REMEDY =
    REJECTED_FOR_INITIAL_PROFILE

PRE_FREEZE_LEGACY_MATERIALIZATION_SELECTED = NO
PRE_FREEZE_REAL_ROOT_WRITE_REQUIRED_FOR_ZERO_SHARED = NO
PROPOSED_30_LANE_LEGACY_MATERIALIZATION = WITHDRAWN
~~~

This is a source-evidence decision. It neither changes the requirement that a
public workspace have a shared/domain lane nor authorizes any write to the
real root.

## Ratified zero-shared resolution

The selected initial-profile resolution is:

~~~text
ZERO_SHARED_RESOLUTION =
    DECLARED_EMPTY_SHARED_NATIVE_ADMISSION
~~~

The difference from I4G-R1 is only the lawful path from an already-declared,
unmaterialized legacy domain to a native empty shared lane. It is not a new
TORMENT memory behavior.

## DECLARED_EMPTY_SHARED source posture

DECLARED_EMPTY_SHARED is a frozen source/admission posture for a domain that
is lawfully declared by legacy workspace semantics but has no materialized
shared MemoryGraph.

~~~text
DECLARED_EMPTY_SHARED:

domain declaration = PRESENT
shared/ materialized MemoryGraph = ABSENT
shared/nodes.jsonl = ABSENT
motifs.json = PRESENT | ABSENT
canonical memory content count = 0
source representation vectors = NONE
canonical source memories = NONE
~~~

The posture must be proven from immutable source facts bound to:

1. workspace identity;
2. declared domain identity;
3. domains.json evidence, or the exact frozen legacy default-domain rule where
   domains.json is absent;
4. observed absence of the materialized shared MemoryGraph under the applicable
   writer freeze;
5. motif evidence recorded as present or absent;
6. no source representation vectors; and
7. no canonical source memories.

No provider or model identity may be manufactured for an empty source lane.

~~~text
DECLARED_EMPTY_SHARED_POSTURE = FROZEN
MOTIF_REQUIRED_FOR_DECLARED_EMPTY_SHARED = NO
~~~

## Target semantics for an empty declared domain

During a future qualified root-normalization epoch, a
DECLARED_EMPTY_SHARED source obligation may produce exactly one native shared
RootScopeKey with:

~~~text
target lane =
    st / BAAI/bge-small-en-v1.5 / 384

memory objects = 0
B3 representation requests = 0
~~~

When source motif evidence exists, the already-qualified zero-member / motif
normalization law applies as appropriate. When source motif evidence is absent,
normalization creates no invented motif state. No fake source history is
created.

The posture preserves existing EMPTY_SHARED_WITH_MOTIF evidence. It broadens
that qualified state only by allowing the legitimate fresh or unvisited legacy
domain that has neither nodes nor a motif registry.

## Root discovery and census correction

The root-completeness model must distinguish:

~~~text
MATERIALIZED_MEMORY_SCOPE

DECLARED_UNMATERIALIZED_DOMAIN_OBLIGATION

IDENTITY_ONLY_OR_EMPTY_PRIVATE_SCOPE

SOURCE_CAUSED_NONPUBLIC_ENTRY
~~~

A declared domain whose shared directory is absent must not disappear from
root accounting.

~~~text
EVERY_DECLARED_PUBLIC_DOMAIN =
    EXPLICITLY_ACCOUNTED_FOR
~~~

Each declared public domain must have one explicit posture/disposition such as:

~~~text
MATERIALIZED_SHARED_SOURCE
DECLARED_EMPTY_SHARED
RETAINED_NONPUBLIC_WHOLE_WORKSPACE
~~~

No undeclared empty domain may be invented.

The original closure law remains intact:

~~~text
DISCOVERED_CENSUS == DECLARED_CENSUS
~~~

Its successor census has separate typed facts for materialized scopes,
declared-unmaterialized domains, qualified identity-only or empty private
scopes, and source-caused non-public entries. The root must still close
completely.

## EMPTY_PRIVATE source posture

EMPTY_PRIVATE is a frozen source/admission posture for an already-existing
lawful agent identity with:

~~~text
identity evidence = PRESENT
private directory = PRESENT
nodes.jsonl = ABSENT
canonical memory count = 0
~~~

Its meaning is an existing admitted agent with zero stored memories. It never
means new agent creation.

~~~text
EMPTY_PRIVATE_POSTURE = FROZEN

EMPTY_PRIVATE_WITH_LAWFUL_IDENTITY =
    PUBLIC_NATIVE

NATIVE_CAPABILITY_DEFICIT_AS_NONPUBLIC_CAUSE =
    FORBIDDEN
~~~

If lawful identity evidence is absent, EMPTY_PRIVATE does not apply and the
entry remains source-classification work. I4G-R2 does not infer the identity
facts of any real-root private directory; that remains a later frozen-epoch
observation and administration concern.

An empty private scope can contain embedding-store residue without canonical
nodes. The frozen authority distinction is:

~~~text
canonical memory authority =
    nodes / canonical source objects

orphan vector rows without canonical node identity =
    historical unreachable residue
~~~

Such bytes must never be promoted into memories merely because they exist.
Future qualification may retain them as provenance or evidence, but may not
use them to infer canonical memory content.

## Unknown representation ratification

The frozen unknown-representation disposition is:

~~~text
UNKNOWN_REPRESENTATION_DISPOSITION =
    REEMBED_FROM_CANONICAL_SOURCE

SOURCE_REPRESENTATION_IDENTITY_REQUIRED_FOR_REEMBED =
    NO
~~~

This applies when the existing Phase 9B metadata-less source eligibility
contract passes and canonical source evidence is complete. The source vector
identity remains unknown; retained historical vector bytes remain unknown
evidence where required; canonical source content is verified; and the target
representation is:

~~~text
st / BAAI/bge-small-en-v1.5 / 384
~~~

~~~text
UNKNOWN_LEGACY_VECTOR_CAN_BECOME_TARGET_BY_RELABEL =
    NO
~~~

Unknown legacy vectors must never be relabeled as hash or ST. The preserved
Phase 9B status is:

~~~text
PHASE_9B_METADATA_LESS_NORMALIZATION_ARCHITECTURE =
    ALREADY_QUALIFIED

REAL_ROOT_UNKNOWN_SCOPE_ELIGIBILITY =
    REQUIRES_FUTURE_FROZEN_EPOCH_ADMINISTRATION
~~~

The three currently observed unknown scopes therefore do not require a new
representation architecture.

## Hash scopes

The existing hash path is unchanged:

~~~text
LEGACY_HASH_SCOPES = REEMBED_REQUIRED
HASH_NORMALIZATION_REQUIRES_NEW_ARCHITECTURE = NO
~~~

## Real-root and writer-freeze status

The six zero-shared real workspaces remain unchanged until a later
writer-frozen admission process. No pre-freeze real-root mutation is required
or authorized.

~~~text
REAL_ROOT_CONTACT = NONE
REAL_ROOT_WRITE_CONTACT = NONE

WRITER_FREEZE_ARCHITECTURALLY_READY = NO
~~~

Writer freeze remains architecturally unready because implementation and
qualification are still required for:

1. DECLARED_EMPTY_SHARED;
2. EMPTY_PRIVATE;
3. typed root-census closure including declared-unmaterialized domains; and
4. the I4G-R1 multi-scope public workspace view.

The real empty-private identity facts are also observationally unresolved.

## Next implementation qualification

The next phase is synthetic and offline. It must qualify:

1. DECLARED_EMPTY_SHARED source posture;
2. EMPTY_PRIVATE source posture;
3. typed root-census closure with declared-unmaterialized domains;
4. zero-request normalization of empty scopes;
5. I4G-R1 workspace view for private 0..n and shared 1..n;
6. multi-private public ingest and query routing;
7. zero-private shared workspace view and refusal behavior;
8. no partial-root startup; and
9. v1 and I4G predecessor non-regression.

No implementation, test, service, provider, model, embedding, writer freeze,
admission, normalization, cutover, or activation is authorized by I4G-R2.

## Frozen result

~~~text
I4G_R2_EMPTY_SCOPE_SOURCE_POSTURE_CONTRACT = FROZEN

I4G_R1_FROZEN_TOPOLOGY_LAW = PRESERVED
I4G_R1_ZERO_SHARED_REMEDY = SUPERSEDED

ZERO_SHARED_RESOLUTION =
    DECLARED_EMPTY_SHARED_NATIVE_ADMISSION

PRE_FREEZE_LEGACY_MATERIALIZATION_SELECTED = NO
REAL_ROOT_WRITE_REQUIRED_FOR_ZERO_SHARED = NO

DECLARED_EMPTY_SHARED_POSTURE = FROZEN
MOTIF_REQUIRED_FOR_DECLARED_EMPTY_SHARED = NO

EMPTY_PRIVATE_POSTURE = FROZEN
EMPTY_PRIVATE_WITH_LAWFUL_IDENTITY = PUBLIC_NATIVE

UNKNOWN_REPRESENTATION_DISPOSITION =
    REEMBED_FROM_CANONICAL_SOURCE

SOURCE_REPRESENTATION_IDENTITY_REQUIRED_FOR_REEMBED = NO
PHASE_9B_UNKNOWN_SOURCE_PATH = PRESERVED
HASH_NORMALIZATION_ARCHITECTURE_GAP = NO

PROPOSED_30_LANE_REAL_ROOT_WRITE = WITHDRAWN

WRITER_FREEZE_ARCHITECTURALLY_READY = NO

NEXT_PHASE =
    SYNTHETIC_EMPTY_SCOPE_AND_MULTI_TOPOLOGY_IMPLEMENTATION_QUALIFICATION

PRODUCTION_CODE_CHANGES = 0
TEST_CODE_CHANGES = 0
TESTS_RUN = 0

REAL_ROOT_CONTACT = NONE
BRAINVISION_OPENED = NO
SECOND_COGNITIVE_FUNCTION_INSPECTED = NO
TORMENT_MATHEMATICS_CHANGED = NO
~~~
