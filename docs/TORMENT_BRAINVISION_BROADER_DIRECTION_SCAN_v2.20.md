# TORMENT Brainvision Broader Direction Scan v2.20

## 1. Status / Scope

**DOCS-ONLY broader direction SCAN.** This is a comparison note only. It opens **no** code, **no** tests, **no**
runtime, and **no** integration lane; it authorizes **no** implementation, **no** validation, **no** readiness claim,
and **no** capability claim, and it is not corrective. It sits over the accepted v2.19 edge (`dc51ff7`) and changes
none of the accepted files. It executes the Path C recommended by v2.19: a broader, docs-only comparison of Brainvision
research directions.

**v2.20 is a broader direction scan only.** It steps back from the paused flat opponent-field symbolic branch and
compares broader Brainvision research layers, to help the operator choose what is safest and most useful next. It
compares directions and makes a recommendation; the recommendation is **operator-gated and non-self-authorizing** — it
selects no work and starts none.

**v2.20 authorizes no implementation, validation, readiness, or capability claim.** It introduces and authorizes
**no** descriptor, coordinate system, numeric geometry, metric, equation, threshold, control metric, pass/fail gate,
validation, screen analysis, real clip, camera / live / sensor / streaming path, runtime path, memory path, prompt /
context / action / render-body / autonomy contact, classifier (form B), or neural encoder (form C). It makes **no**
production vision claim, **no** "Brainvision sees" claim, **no** temporal-order claim, and **no** descriptor-validity /
geometry-validity / screen-readiness / memory-readiness / runtime-readiness / integration-readiness claim. The flat
opponent-field symbolic branch stays **HELD**. Everything stays offline under `research/brainvision/` +
`tests/research/`, HELD per v0.6. **HOLD / HELD means held for analysis and claim control — not abandoned.**

```text
flat_field_validated                        = False
first_pass_structure_validity_claim_allowed = False
temporal_claim_allowed                      = False
descriptor_validity_claim_allowed           = False
geometry_validity_claim_allowed             = False
screen_readiness_claim_allowed              = False
runtime_readiness_claim_allowed             = False
memory_readiness_claim_allowed              = False
integration_readiness_claim_allowed         = False
vision_claim_allowed                        = False
verdict                                      = HOLD
```

**No `§0` pointer; no tags.**

## 2. Current Brainvision Position

```text
PRIOR BY / COLOR / CHROMA EVIDENCE (frozen localization evidence, NOT closure):
  The SAG / recurrence line established a central NEGATIVE result -- the candidate is NOT temporal-order-specific. The
  color/chroma-structure line then LOCALIZED where the difficulty sits: the descriptor's response is spectrum-explained,
  the directional / per-channel-spectral axis is entangled, the by_std residual was a pool-composition artifact, and a
  best-effort matched search could not close all cheap-baseline shortcuts at once -- the "proxy wall" stands, with no
  unique color-structure advantage shown. This is USEFUL localization evidence of WHERE problems are; it is NOT proof
  of closure, and forms B (classifier) and C (neural) stay CLOSED. Treat all of it as FROZEN EVIDENCE.

FLAT OPPONENT-FIELD SYMBOLIC BRANCH (paused scaffold evidence, NOT validation):
  v2.6-v2.18 built a generated/reporting fixture layer + a minimal guarded static symbolic representation artifact +
  eight boundary-hardening reviews, then paused HELD (v2.19 kept it paused). It is a useful, bounded, non-authorizing
  scaffold with a fully documented non-claim wall. It validates NOTHING.

OPEN CLAIMS: none. No validation or vision claim is open. flat_field_validated = False; all claim locks False; verdict
HOLD. The programme sits at a clean pause with two bodies of frozen/paused evidence and no pending claim.
```

## 3. Area Comparison

Each area is evaluated across the required dimensions: what it would examine; what it would not authorize; primary
benefit; primary risk; boundary risk; required guardrails; recommended now?; operator choice needed before follow-up.

```text
====================================================================================================================
AREA A -- PRIOR BY / COLOR / CHROMA RESIDUAL EVIDENCE (review as FROZEN EVIDENCE only)
  examine  : BY-axis residual issues; color/chroma structure questions; residual/proxy failures; old fixture-metric
             lessons; why prior BY/color paths LOCALIZED problems but did not open closure.
  not auth : does NOT re-open the color-structure descriptor, re-run fixtures, adopt a metric, or treat prior evidence
             as solved; no implementation / real-clip / screen / runtime / memory / vision.
  benefit  : consolidates hard-won localization lessons so a future question is informed, not naive.
  risk     : reviving a failed path as if solvable-with-one-more-tweak.
  boundary : MEDIUM -- "revisit BY/color" invites a metric/descriptor re-attempt.
  guard    : FROZEN EVIDENCE only; no re-run, no new metric; results stay non-authorizing; per-v2.16/v2.17 wording.
  now?     : NO (as a standalone move) -- valuable as INPUT to Area C/D, not as its own next slice.
  choice   : if pursued, a docs-only evidence-consolidation note, separately approved.

====================================================================================================================
AREA B -- FLAT OPPONENT-FIELD SCAFFOLD (review as PAUSED scaffold only)
  examine  : the A-F symbolic fixture scaffold; vocabulary and family-identity guardrails; the protocol / non-claim
             boundary; WHY this branch is useful but non-validating.
  not auth : does NOT continue or expand the branch; no new families / vocabulary; no descriptors / coordinates /
             metrics / validation; no screen / runtime / memory / vision.
  benefit  : keeps the scaffold's lessons legible as input to question framing.
  risk     : auto-continuing or expanding the paused branch.
  boundary : MEDIUM -- reviewing the scaffold invites resuming it.
  now?     : NO -- v2.10-v2.19 already exhausted scaffold review; further scaffold work is low-yield.
  choice   : none recommended; the branch stays PAUSED HELD.

====================================================================================================================
AREA C -- SYNTHETIC FALSIFICATION DESIGN (docs-first plan for a NEW offline synthetic falsification question)  [PREFERRED]
  examine  : whether the next useful work is a DOCS-FIRST plan for a new offline synthetic falsification question --
             what failure/question is still worth falsifying; what would count ONLY as reporting, not validation; how
             to avoid descriptor / metric / screen / runtime drift; how to keep results non-authorizing.
  not auth : does NOT implement; no real clips; no descriptor / coordinate / metric / pass-fail; no screen / runtime /
             memory / vision; a DESIGN plan only.
  benefit  : HIGHEST-VALUE -- synthetic falsification is the safest research mode, and a sharper offline question is
             what the programme most needs next (a better QUESTION before more math or code); builds on the frozen
             BY/color localization (Area A) without reviving it as solved.
  risk     : over-planning without a crisp empirical target; designing a question that quietly needs a metric.
  boundary : LOW-MEDIUM -- it is a design plan, not a step into implementation; drift risk is controllable by keeping
             the target reporting-only and metric-free at the design level.
  now?     : YES (PREFERRED) -- the clean pause makes a docs-first synthetic-falsification design the best next move.
  choice   : the operator approves opening a docs-only v2.21 synthetic-falsification DESIGN scan; no code.

====================================================================================================================
AREA D -- DIRECTIONAL / STRUCTURAL OPPONENT-FIELD QUESTION (new question framing, no coordinates/metrics/descriptors)
  examine  : whether a new question should focus on directional / structural RELATIONS without adopting coordinates,
             metrics, or descriptors -- whether the project needs a better QUESTION before better code; how to keep
             "structure" from becoming geometry validity; whether the symbolic scaffold can inform question framing
             without authorizing representation expansion.
  not auth : does NOT implement; adopts no coordinate / metric / descriptor; opens no screen / runtime / memory /
             vision; frames a question only.
  benefit  : sharpens the STRUCTURAL question the programme keeps circling (directional/per-channel entanglement) at
             the framing level, feeding a later synthetic design.
  risk     : "structure" sliding into geometry validity; framing that presupposes a descriptor/coordinate.
  boundary : MEDIUM -- "directional / structural" is close to geometry; needs firm wording guards (v2.17).
  now?     : SECONDARY -- recommend IF the scan concludes the project needs a sharper STRUCTURAL question before any
             synthetic design; otherwise it feeds into Area C.
  choice   : the operator approves opening a docs-only v2.21 question-framing scan; no code.

====================================================================================================================
AREA E -- PAUSE BRAINVISION AND RETURN TO ANOTHER TORMENT LAYER (e.g. cognition / thinking / dream / SRG governance)
  examine  : whether Brainvision should pause and the project return to another TORMENT layer, WITHOUT treating
             Brainvision as failed.
  not auth : does NOT close or abandon Brainvision; does NOT treat the frozen/paused evidence as failed; no vision claim.
  benefit  : reallocates effort if Brainvision is currently over-constrained or low-yield.
  risk     : leaving Brainvision prematurely and losing momentum on a nearly-framable next question.
  boundary : LOW -- moving to another layer carries little Brainvision claim risk; the risk is opportunity cost.
  now?     : NO -- the scan does NOT find Brainvision over-constrained or low-yield; a sharper question (Area C/D) is
             within reach, so pausing the whole programme now is premature. (Legitimate operator option regardless.)
  choice   : operator's call; if pursued, Brainvision pauses HELD and another layer's gating applies.
====================================================================================================================
```

## 4. Cross-Area Risks

Risks that span the areas, each to be refused regardless of which area is chosen:

```text
- REVIVING OLD FAILURES AS IF SOLVED: the BY/color/chroma and fixture-metric results are FROZEN localization evidence,
  not solvable-with-one-tweak; no area may resume them as if closure is near (Area A/D guardrail).
- TREATING SCAFFOLD AS VALIDATION: the flat opponent-field symbolic branch validates nothing; no area may cite it as
  evidence the field "works" (v2.18 non-claim wall).
- OVER-PLANNING WITHOUT AN EMPIRICAL TARGET: a design or framing scan (C/D) must aim at a crisp, reporting-only,
  metric-free target; endless planning with no target is its own failure mode.
- JUMPING TOO SOON TO IMPLEMENTATION: no area authorizes code; any empirical step is docs-first and separately
  approved.
- INVENTING MATH BEFORE SHARPENING THE QUESTION: the programme likely needs a sharper FALSIFICATION QUESTION before it
  needs more math or more code; do not add machinery ahead of the question.
- DRIFTING TOWARD SCREEN / RUNTIME / MEMORY / VISION: no area may open a screen / real-clip / camera / live / runtime /
  memory / classifier / neural / vision path; Brainvision stays offline / quarantined.
```

## 5. Recommendation

```text
RECOMMEND: AREA C -- docs-first synthetic falsification design scan.

Reason: the flat opponent-field symbolic branch is safely paused, and the prior BY/color/chroma work has already
LOCALIZED where the difficulty sits (spectrum-explained descriptor, entangled directional/per-channel axis, proxy wall
standing) as frozen evidence. The next useful Brainvision move is NOT richer representation and NOT more scaffold
review -- it is a SHARPER OFFLINE FALSIFICATION QUESTION that can be PLANNED (docs-first) without claiming validation.
Synthetic falsification remains the safest research mode, and a crisp reporting-only question is what the programme
most needs before any more math or code. Area C builds on the frozen localization (Area A) without reviving it as
solved.

SECONDARY (docs-first) option: AREA D -- directional / structural opponent-field QUESTION FRAMING -- IF the operator
concludes the project needs a sharper STRUCTURAL question before any synthetic design. D feeds C; if the structural
question is the blocker, frame it first.

NOT recommended now: Area A (valuable as INPUT to C/D, not a standalone slice); Area B (scaffold review is exhausted);
Area E (the scan does not find Brainvision over-constrained or low-yield, though it remains a legitimate operator
option). No descriptor / coordinate / metric / validation / screen / real-clip / runtime / memory / classifier (B) /
neural (C) / vision work is recommended or authorized by this scan.
```

## 6. Operator Decision Needed

```text
The next ACTUAL slice must be chosen by the OPERATOR. This scan makes a clear recommendation (Area C, with Area D as a
docs-first secondary and Area E as a legitimate operator option) but is NOT self-authorizing: it starts no work and
commits to no area. Whichever area the operator picks becomes a SEPARATE, docs-first slice with its own boundary, Codex
review, and (for anything beyond docs) explicit operator approval. Until then, the flat opponent-field symbolic branch
stays PAUSED HELD, the prior BY/color/chroma work stays FROZEN EVIDENCE, and Brainvision stays offline / quarantined.
```

## 7. Verdict

```text
verdict                                      = HOLD
flat_field_validated                         = False
first_pass_structure_validity_claim_allowed  = False
temporal_claim_allowed                       = False
descriptor_validity_claim_allowed            = False
geometry_validity_claim_allowed              = False
screen_readiness_claim_allowed               = False
runtime_readiness_claim_allowed              = False
memory_readiness_claim_allowed               = False
integration_readiness_claim_allowed          = False
vision_claim_allowed                         = False

OUTCOME_LABEL: BRAINVISION_BROADER_DIRECTION_SCAN_ONLY
```

v2.20 is a docs-only broader Brainvision direction scan. It summarizes the current position (prior BY/color/chroma work
as frozen localization evidence; the flat opponent-field symbolic branch as paused scaffold evidence; no open claim),
compares five broader areas (A BY/color/chroma frozen-evidence review, B flat-field scaffold review, C synthetic
falsification design, D directional/structural question framing, E pause Brainvision for another TORMENT layer) across
what each would examine and not authorize, its benefit, primary and boundary risks, required guardrails, whether to
recommend now, and the operator choice needed, addresses the cross-area risks, and recommends **Area C** (with Area D
as a docs-first secondary). It selects no work, starts none, and is not self-authorizing. All claim locks and the
frozen verdict **HOLD** are preserved and unmoved.

## 8. Codex review prompt

```text
Please review docs/TORMENT_BRAINVISION_BROADER_DIRECTION_SCAN_v2.20.md
(new, docs-only, untracked; over the accepted v2.19 edge dc51ff7).

Verify that this scan:
- is docs-only and authorizes NO implementation, NO test expansion, NO validation, NO readiness claim, and NO
  capability claim (no code / tests / schema, no torment_service/, no runtime, no memory, no camera / live / sensor /
  screen-capture / streaming, no real clips, no pixels / images); keeps form B (classifier) and form C (neural) CLOSED;
  opens no screen-analysis / numeric-geometry; changes no field / value / family / lock; keeps the flat opponent-field
  symbolic branch HELD;
- frames the central question as WHICH broader Brainvision research direction should be considered next after the
  symbolic-scaffold pause;
- summarizes the current position: prior BY/color/chroma work as FROZEN localization evidence (not closure; forms B/C
  closed), the flat opponent-field symbolic branch as PAUSED scaffold evidence (not validation), and no open claim;
- compares the five required areas (A prior BY/color/chroma residual evidence as frozen evidence; B flat opponent-field
  scaffold as paused scaffold; C synthetic falsification design; D directional/structural opponent-field question; E
  pause Brainvision and return to another TORMENT layer) across ALL required dimensions (what it would examine; what it
  would not authorize; primary benefit; primary risk; boundary risk; required guardrails; whether recommended now; what
  operator choice would be needed before follow-up), with each area's allowed/forbidden scope respected;
- preserves the required lessons: prior BY/color/chroma failures are useful localization evidence not proof of closure;
  the flat opponent-field symbolic branch is useful scaffold evidence not validation; synthetic falsification is the
  safest research mode if implementation resumes later; any next empirical step must be docs-first and separately
  approved; no path jumps to screen / real clips / runtime / memory / classifier-neural / vision; the project likely
  needs a sharper QUESTION before more math or code;
- addresses the cross-area risks (reviving old failures as if solved; treating scaffold as validation; over-planning
  without an empirical target; jumping too soon to implementation; inventing math before sharpening the falsification
  question; drifting toward screen/runtime/memory/vision);
- RECOMMENDS Area C unless a stronger reason is found, explains why, and mentions Area D as a docs-first secondary if
  question-framing is more urgent than synthetic design; recommends NO descriptor / coordinate / metric / validation /
  screen / real-clip / runtime / memory / neural / classifier / vision work;
- states that the next actual slice must be chosen by the OPERATOR and that the scan is not self-authorizing;
- preserves the locks and verdict (Section 7): flat_field_validated = False; first_pass_structure_validity_claim_allowed
  = False; temporal_claim_allowed = False; descriptor_validity_claim_allowed = False; geometry_validity_claim_allowed =
  False; screen_readiness_claim_allowed = False; runtime_readiness_claim_allowed = False; memory_readiness_claim_allowed
  = False; integration_readiness_claim_allowed = False; vision_claim_allowed = False; verdict = HOLD; outcome label
  BRAINVISION_BROADER_DIRECTION_SCAN_ONLY; interprets HOLD/HELD as held for analysis, not abandoned;
- adds NO §0 pointer and NO tags, and makes no vision / "Brainvision sees" / descriptor-validity / geometry-validity /
  temporal-order / readiness claim.

Flag any area recommended in a way that authorizes work without separate operator approval, any prior failed path
treated as solved / auto-resumable, any scaffold cited as validation, any adopted descriptor / coordinate / metric /
validation / pass-fail rule, any screen / real-clip / camera / live / runtime / memory authorization, any classifier
(B) / neural (C) opening, any "Brainvision sees" / vision / descriptor-validity / geometry-validity / temporal-order /
readiness / capability claim, or any claim-lock / verdict movement.
Return ACCEPT AS-IS or MODIFY with exact required changes.
```

Brainvision remains **offline / quarantined**, HELD per v0.6.
`flat_field_validated = False`, all claim locks False, and the frozen verdict **HOLD** are unchanged.
**No `§0` pointer; no tags.**

*End — TORMENT Brainvision Broader Direction Scan v2.20. Docs-only direction scan. Opens no implementation lane, no
test expansion, and no representation expansion; opens no classifier / neural / screen / real-clip / runtime / memory
work; adopts no descriptor / coordinate system / numeric geometry / metric / equation / threshold / control metric /
pass-fail rule; changes no field / value / family / lock; compares five broader Brainvision directions (A BY/color/
chroma frozen evidence, B flat-field scaffold, C synthetic falsification design, D directional/structural question, E
pause for another TORMENT layer) across what each would examine and not authorize, its benefit, primary and boundary
risks, guardrails, recommend-now, and operator-choice-needed; preserves the frozen-evidence and scaffold-not-validation
lessons; recommends Area C (Area D a docs-first secondary); keeps the flat opponent-field symbolic branch PAUSED HELD;
is not self-authorizing and leaves the next slice to the operator; preserves all claim locks and the frozen verdict
HOLD; makes no vision / "Brainvision sees" / descriptor-validity / geometry-validity / temporal-order / readiness
claim; outcome label BRAINVISION_BROADER_DIRECTION_SCAN_ONLY; no `§0` pointer added; no tags.*
