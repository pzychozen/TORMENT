# TORMENT Memory Substrate 7G5D1F — Core-only concrete fixture freeze

Status: experiment-only concrete-input freeze. No native formal administration
has been authorized, started, or recorded.

## Selected scope

The checked-in
`experiments/memory_substrate_d1_trace_replay_v1/fixtures/concrete_core_legacy_only_fixture_set_20260831.json`
is the only 7G5D1F core fixture lock. Its explicit profile is `CORE_ONLY` and
its immutable L0 fingerprint is
`f74fc5f104a71788f21f0f60ed753d35c029b1427f04e04d850e9c65f78fde63`.

`CORE_ONLY` contains exactly M1 create, M2 create/reinforce, M3
create/distinct, M4 create/contradiction, M5 no-write, and a sequential
create/reinforce/distinct/contradiction arm. Every fixture has a unique
legacy clone and a unique native clone; every fixture ID is assigned once.
It has no Character arm and accepts no Character fixture.

The core L0 is read only with `character_seed_required=False`. It has no
Character seed or state artifact and exactly one ordinary baseline memory.
The normal-service capture used fresh independent core-L0 clones and distinct
core-only request bytes; it did not replay or inspect a native outcome.

## Held Character boundary

The older
`concrete_legacy_only_fixture_set_20260831.json` is preserved byte-for-byte as
Character-bearing scientific evidence. Its current disposition is
`CHARACTER_BASELINE_STATUS = DEFERRED_PENDING_PROVENANCE_VOCABULARY`.
Neither its fixture hash nor its administration inputs can verify or
authorize the core-only lock.

`CHARACTER_EXTENDED` remains an explicit profile and requires its Character
arm. It is not an alternative for this core-only freeze and remains held on
the provenance-vocabulary boundary.

## Pinned B5 and N0 evidence

The core fixture binds only the pre-qualified retained-side-store witness:
`ed86e486a94080671c7eb672bf21cab96ec383a944c13492acc4a9616d081a4c`.
It is reverified against the immutable core L0 with the pinned legacy source
namespace; a formal path may not regenerate or substitute another witness.

A fresh, separate N0 construction from that L0 established B2 and B3A,
B4A = 1, B4B = 0, memory/motif/member-reference/side-store closure, and the
existing core-STAGING binding, routing, and post-write constructibility gates.
No formal fixture event was routed during that construction.

## Non-authority declaration

The fixture reports `native_formal_event_count = 0`,
`character_arm_administered = false`, and
`character_subarm_status = DEFERRED_PENDING_PROVENANCE_VOCABULARY`.
There is no `authorized=True` administration authorization, no administration
marker, no formal result root, and no formal legacy/native trace contact.

This phase adds no production selector, native activation, dual read/write,
cutover, migration behavior, or Character provenance vocabulary.
