# TORMENT — Document A Admission-Crossing Target-Selection Decision Record v0.1

**Status:** DOCS-ONLY operator decision record. **Non-mechanizing, non-authorizing of any mechanism.** It
records the operator's answer to the target-selection frame's presented question and **nothing more**. It
selects no mechanic, opens no gate, authorizes no code/test/runtime, names no durable field / schema /
carrier / store, and amends no contract. Records a decision; builds nothing.

**Answers:** `docs/TORMENT_DOCUMENT_A_ADMISSION_CROSSING_TARGET_SELECTION_FRAME_v0.1.md` (which presented
Options S / H / R and explicitly did not choose). **Builds on:**
`docs/TORMENT_DOCUMENT_A_ADMISSION_CROSSING_ROOT_BLOCKER_CONVERGENCE_FRAME_v0.1.md`. Those two frames are
left **unedited**; this record carries the operator attribution the neutral frame should not.

**Authority note:** Document A (containment wall / admission edge), the P4 Reader/Projection-Safety
Contract, the Gate B writer-authority doctrine, Document B, the Ledger Observational-Boundary, the
Decision Registry, and `PROJECT_ORIENTATION_MAP.md` §0 remain source of truth. This record amends none of
them.

**Doctrine (carried, exact):**

> Memory may guide context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.
> Automatic remains allowed only where separately ratified. Autonomous remains unopened.

---

## 1. Status / non-authorizing banner

Decision record only. It moves the target-selection question from *PRESENTED FOR OPERATOR DECISION* to a
recorded operator choice. Recording the choice neither designs nor opens anything downstream: no admission
crossing / carrier / mechanism / workflow / actor / API / UI / runtime / persistence is created or
implied. The full non-authorization list is §6.

## 2. Decision

**Operator (Hilmir) selects Option S** from the target-selection frame.

**Meaning:** the **Document A governed admission crossing + candidate carrier** is **selected as the next
design-framing target only.** This is an **agenda / sequencing** decision — it names what may next be
*framed*; it designs, opens, and builds nothing. Option H (HOLD) and Option R (REVIEW-FIRST) are not
selected; Codex had already PASSed the convergence frame, so no further review is required by this record.

## 3. Inherited and unchanged bounds

Carried forward exactly, and **unchanged** by this decision:

- **Admission authority floor = `operator-only`** — the requirement-level FLOOR for any future governed
  admission; unchanged.
- **Stage B = framing-only** — the open lane is a *framing* lane; it opens no database / schema / storage /
  carrier / migration mechanics.
- **Substrate = §K-eligible, NOT authorized** — eligibility ≠ authorization.
- **Candidate representation / pre-carrier constraints** — the inert representation is selected and its
  pre-carrier constraints framed; the durable carrier itself remains unselected.
- **HOLD posture preserved** — Gate A producer-independent wall = HOLD; P4 mechanics = unopened; Gate B
  writer-authority micro-work = HOLD; Dream / Regime-B = HOLD / blocker-dependent.

## 4. What this authorizes

**Only:**

- a **future, separately-authorized, docs-only design-framing pass** may take the **Document A governed
  admission crossing + candidate carrier** as its **subject**; and
- the item **moves from deferred / root-blocker convergence to named next design-framing target.**

That is the entire grant. It sets the framing agenda; it confers **no build authority**. Any such
design-framing pass is itself a separate, separately-authorized, still-docs-only slice, and any actual
mechanism or carrier remains gated behind further separately-authorized decisions.

## 5. What this does not authorize

- carrier / store / schema / field / enum / ID design or selection;
- admission mechanism / crossing / workflow / decision-procedure / actor / API / UI;
- database / substrate construction or Stage-B mechanics;
- any change to the **operator-only** authority floor;
- writer crossing or Gate B fix;
- P4 `ReaderPolicy` / source-sameness / `diagnostic_only` mechanics;
- Dream / Regime-B runtime;
- scheduler / trigger / budget / autonomy;
- Gate D / Envelope-Audit / Document B chamber runtime;
- AgentRunner / app / spine / MCP wiring;
- model / provider / API / prompt path;
- memory writes / persistence / logging / transcripts;
- output-control / finalizer / refusal / identity / canon behavior;
- dynamic-kernel or `conversation_shock`;
- any contract amendment.

**Selection sets the framing agenda; it opens no mechanics.**

## 6. Explicit non-authorizations

This record is docs-only; it authorizes no code, test, mechanism, carrier, gate, runtime, or lane; it
amends no Document A / P4 / Gate A / Gate B / Document B / Cluster 2 / Ledger / Stage-A / Stage-B contract;
it edits neither the target-selection frame nor the convergence frame nor `PROJECT_ORIENTATION_MAP.md`; and
it preserves the doctrine verbatim. The only state change it makes is documentary: the target-selection
question is now recorded as **answered — Option S**, and the crossing+carrier is the **named next
design-framing target**, still with every mechanism deferred to a future, separately-authorized decision.

## 7. Verdict

**SELECTED — NEXT DESIGN-FRAMING TARGET NAMED / NO MECHANICS OPENED.** The operator has selected Option S:
the Document A governed admission crossing + candidate carrier is the next **design-framing** target only.
The `operator-only` floor, Stage-B framing-only posture, §K substrate eligibility-not-authorization, and
all Gate A / P4 / Gate B / Dream HOLD postures are unchanged, and **no mechanic, carrier, gate, runtime, or
lane is opened** by this record.

*End — Document A Admission-Crossing Target-Selection Decision Record v0.1. Docs-only, non-mechanizing.
Verdict: SELECTED — NEXT DESIGN-FRAMING TARGET NAMED / NO MECHANICS OPENED.*
