# TORMENT — Stage 4R1 disposable native environment qualification

2026-09-10 UTC. **POST_P7_NATIVE_PRODUCTION_VALIDATION_STAGE_4R1 = PASS.**

One fresh, isolated native deployment was created through existing constructors,
admitted by the ordinary service, exercised with a baseline read/write/read, and
shut down normally. Production stayed byte-identical throughout the run.
This qualifies the disposable environment only. No Stage-4 failure scenario ran.

## Frozen state and execution boundary

Starting HEAD = origin/main = `c15febe11a9c29d7459fe82863c0ffa03c0f4586`;
tracked worktree clean. P1–P7 and Stages 1–3 remain closed. The original Stage-4
result remains `BLOCKED_DISPOSABLE_ROOT_ADMISSION`; its document was not edited.
Its SHA-256 remains
`e8c2ce64509171151288eceff0834919a8dc737201bce1e30c347b9fb074b91b`.

This result is the only repository change. Helpers, receipts and the disposable
root are outside the repository. The final evidence-only commit, remote equality,
clean tracked worktree and unchanged untracked listing are recorded in external
`publication.json` and the final return. No production application code changed.
All project Python ran in Windows CMD with `conda activate torment`, using
`C:\Users\Notandi\miniconda3\envs\torment\python.exe` and SQLite 3.53.4.

## Architecture decision

| Candidate | Classification | Basis |
| --- | --- | --- |
| A: qualified admitted-state relocation API | **NO** | No supported clone/re-root entry point was found in current deployment, cutover, native-owner and trajectory-coordinator APIs. The absolute-root identity check that blocked Stage 4 remains mandatory. |
| B: fresh authority around an already-active copied core | **NOT_QUALIFIED** | Current bootstrap qualifies inert STAGING / LEGACY_ACTIVE, never-active cores. No established operation requalifies an ACTIVE copied core and its immutable admission/profile/membership history under fresh selector and trajectory authority. This is a qualification finding, not a claim that a future design is impossible. |
| C: existing synthetic builder using current production mechanisms | **YES** | Fresh schema, scope membership and normalization inputs cross the existing positive cutover and production disposition constructors. Ordinary native startup, public read/write/read, actual representation publication, receipts and trajectory writes passed. |

Selected method:
`EXISTING_SYNTHETIC_ROOT_BUILDER_WITH_PRODUCTION_CUTOVER_AND_HANDOFF`.

The narrow review covered `real_root_staging_bootstrap.py` (`bootstrap`,
`_is_inert`, around lines 237 and 517), `offline_cutover_controller.py`
(`prepare_root`, `activate_root_core`, `execute_root_disposition_plan`,
`activate_root_external_selector`), `deployment_selector.py`,
`deployment_core_maintenance.py`, `production_root_disposition.py`,
`production_native_owner.py` (`_recover_root_v2_workspace_runtime` and record agreement),
and `trajectory_writer_handoff.py` (`create`, `open_existing`, native admission,
session replacement and token validation). These modules are under
`torment_service/substrate/`. Their exact source hashes are preserved in
`architecture_review.json`.

`TrajectoryScopeIdentity.payload/exclusivity_key` binds the artifact root;
coordinator `_validate_database` binds the data root. Ordinary session replacement
increments generation and `_validate_current_token` checks it. Neither identity
binding nor refusal law was changed. A newly constructed deployment lawfully has
its own core, scope, profile/envelope, session and path identities.

`DISPOSABLE_AUTHORITY_IDENTITY_MAY_DIFFER = YES`.
Stage-4 mechanism tests require the production authority semantics under a valid
identity; they do not require impersonating the exact production identity.
`PRODUCTION_TRAJECTORY_HISTORY_REQUIRED_FOR_STAGE4 = NO` for the specified
transaction, restart, writer-replacement and stale-refusal mechanisms. Fresh
history generated by the normal writer supplies the necessary starting evidence.
Exact historical migration, production-image repair, and production Character
continuity would need separate source-specific evidence.

## Construction and identity

External evidence directory:
`C:\TORMENT\TORMENT_administration\post-p7-stage-4r1-20260910`.

Disposable root:
`C:\TORMENT\TORMENT_administration\post-p7-stage-4r1-20260910\root`.

Production root:
`C:\TORMENT\TORMENT_repo\TORMENT-fabric_v2\torment_fabric\data`.

The administration composition `build_root.py` invokes
`tests/test_post_i4_full_root_disposable_rehearsal_r1.py:_build_disposable_root`
with two workspaces, north/south, each with same-agent and common-domain, plus
north/second-agent: five admitted scopes in all. EID 7 in each scope is synthetic
historical input. No production memory, core, selector or trajectory chunk was
copied into this root.

One bounded request adaptation sets private `motif_domain_id=None`, preserving
Model D. This occurs before creating an envelope, selector or coordinator; the
existing scope-plan digest function qualifies the new request. The private
readiness configurations are rebuilt with the existing helper. No copied
authority digest, path, generation or database row is rewritten.

The sequence uses existing `prepare_root`, compatibility pending,
normalization, completion verification, core pending, P6 activation, the real
`ProductionRootDispositionAdapter`, persisted disposition receipt, then P7
selector activation. The adapter creates and initializes fresh trajectory
authority, quiesces/fences legacy authority, admits native authority, and
completes each handoff. The test activation helper containing deliberate refusal
probes was not used. No negative case was injected.

| Durable fact | Disposable value |
| --- | --- |
| Core | `b05b207c-9781-4c1d-9c46-b0ea8143e903` |
| Core file | `root\substrate\cores\root-r1.db` |
| Root identity | `post-i4-r1:root` |
| Selector | generation 2, NATIVE_ACTIVE |
| Profile digest | `38d4d9432f4bbdade5444c372c05f2fd7b947810473d639bded1db34cd0aaab1` |
| Admission envelope | `0a0766116aa33ad1dcb5640ca33a2da3f09ce3f11ffbcd675e10327fca10e9a7` |
| P6 receipt | `3ab81c72-cb16-4667-8017-af1af4c587fa` |
| Production disposition receipt | `ca6b5e5e2057e8bbcf8b80f4738ed3ec2b1f5b8e3b9edf7c6cfc2ae978d8c029` |

The admitted profile retains compression/deep memory disabled and the st /
BAAI/bge-small-en-v1.5 / 384 representation lane. The scope-plan and external-owner
digests describe this new synthetic deployment and differ from production.
Schema and current runtime semantics are unchanged; topology and historical data
are fixtures, not a production-state-identical copy.

The existing builder supplies synthetic Character seed/state and receipt-only
external-owner evidence. Preparation uses readiness-only dependency sentinels.
These qualify fixture setup; no live SQLite, native owner, public executor,
representation store, receipt store or trajectory writer is mocked. Historical
vectors are deterministic test inputs, not a BGE semantic-quality sample.
No preparation re-embedding occurred. These limitations exclude claims about
production Character history, real external-owner migration, and ranking quality.

## Baseline admission and behavior

`python -m torment_service` started normally as PID 29032. Native-owner recovery
for both workspaces, selector/core agreement and coordinator admission passed.
The prelaunch check recovered the persisted profile and checked the host
configuration. The service used only the ordinary data-root/profile settings,
`TORMENT_EMBED_PROVIDER=st`, `TORMENT_EMBED_STRICT=1`, and `HF_HUB_OFFLINE=1`.
The test service-environment helper, feature/threshold overrides, supplied
embeddings and diagnostic query timing were not used.

The first health attempt preceded the listener and received connection refused;
it is retained in `health.json`. After startup completed, `/health` returned
HTTP 200, NATIVE, st/BGE-small-en-v1.5, dimension 384, not degraded.
No generative provider or external AI API was configured or invoked. BGE was
used offline only by the ordinary baseline service's embedding path.

The initial read expected private fixture EID 7 and returned HTTP 200 with no
hits. Its failed helper expectation is preserved in `baseline_read.json`.
Read-only diagnosis established that the fixture payload lacks `user_id`:
`NativeMemoryVectorRuntime._search_vector` applies the normal private user filter
(around line 390). The initial query also selected zero relational candidates
under the ordinary ThinkingController plan. No production refusal, missing
committed row or corruption was observed. No fixture or runtime was patched.

A normal recall-form query enabled the ordinary relational lane and returned the
exact north/common-domain shared EID 7 fixture. This is the successful baseline
read, recorded separately in `baseline_read_recall.json`; it does not erase the
initial expectation error. Positive private retrieval is then established using
the ordinary new write, whose payload contains the correct user identity.

| Operation | Result | Observed duration |
| --- | --- | --- |
| Baseline recall | HTTP 200; exact north/common-domain shared EID 7 summary | 0.594 s |
| One ordinary private ingest | HTTP 200; stored=true, reinforced=false; EID 8 | 3.047 s |
| Read-after-write | HTTP 200; exact EID 8 summary, north/same-agent/private | 0.593 s |
| Other-agent control | HTTP 200; EID 8 absent from north/second-agent | 0.593 s |
| Other-workspace control | HTTP 200; EID 8 absent from south/same-agent | 0.578 s |

The write text was: “Remember that the disposable observatory keeps the amber
calibration card inside the striped tin.” Step 1000601; unique public key
`stage4r1-baab3e6d-8cb6-4831-a290-18b8a92c228e`. Exactly one ingest request was sent;
no mutation retry, replay, interruption or lock was introduced. Recorded timings
are observations on this tiny synthetic root, not production performance claims.

Read-only snapshots show five to six memories, 40 to 50 operations and 10 to 11
representations. Every original fixture's current revision/payload stayed intact.
The new object is `40aebaac-62b1-4bd0-84dd-580e79456e37`, current revision
`c34ba9b9-9d9c-4b09-a109-52de8f30e351`, ordinal 2, in private semantic scope
`85b0f337-1fc5-4d01-a076-300f032558a9`. Its summary SHA-256 is
`6227e24ecc4af0ed07c7414541644601ed494fa6da616326531df95b337980f8`.

Its current embedding is READY/USABLE, 384 dimensions, 1,536 bytes, with selected
integrity result MATCH. The ledger contains the actual native canonical commit,
representation publication and RESERVED, COGNITION_STARTED, PREPARED, COMPLETE
public receipts in the admitted private namespace. COMPLETE agrees with EID 8.
No interrupted receipt recovery or replay was attempted.

## Trajectory and clean shutdown

All five coordinator scopes are HANDOFF_COMPLETE and bound inside the disposable
root. Fresh handoff generation was 3, legacy fence 2. The baseline private writer
lawfully claimed generation 4 with session
`pid:29032:04d555e6-6769-4324-98d9-1e9d3434e083`; legacy session remains absent.
Initialization, genesis, step and close effects are all SETTLED under native
generation 4. No stale token was exercised and no generation was reset.

Normal Ctrl+C shutdown completed, logged application shutdown and process exit,
and returned service exit code 0. PID 29032 and the port-8787 listener are absent.
The writer sealed epoch 1, chunk 1: one frame at step 1000601, two records, 213
bytes. Its SHA-256 is
`8018cfb259765ca9493bdcaf7d7afd996ca1d98997ca7c9ddcad9d4bebc56a7b`.
`TrajectoryV2Verifier.verify(mode='sealed')` reports valid, one checked chunk,
one step, two records, zero open tails, no issues. No partial chunk remains.
Postshutdown selector/core agreement and existing-coordinator admission pass.
Read-only integrity/foreign-key checks pass for core, selector and coordinator.

## Stage-4 mechanism coverage

“Available” means the current production implementation can be exercised in this
root. It does not claim that its failure behavior passed R1.

| Stage-4 property | Disposable mechanism exercises real production code? | Qualification |
| --- | --- | --- |
| SQLite transaction atomicity | Yes: `objects.execute_semantic`, BEGIN IMMEDIATE, invariant validation, COMMIT/ROLLBACK; current schema/connection path | Ordinary successful transactions observed; interruption, BUSY and rollback cases available, not run |
| Native memory commit | Yes: public runtime uses `NativePublicIngestExecutor` and native canonical commit | One ordinary create and matching read passed; partial/interrupted publication cases not run |
| Restart recovery | Yes: persisted root-v2 admission, native owner recovery, public receipt recovery and trajectory writer reconstruction | Ordinary startup/recovery of admitted authority passed; service restart and unclean-restart cases not run in R1 |
| Selector/core agreement | Yes: `resolve_deployment_agreement` and native-owner envelope/profile/membership validation | Prelaunch, service native admission and postshutdown agreement passed; post-failure reconstruction deferred |
| Scope isolation | Yes: qualified native read model, admitted namespaces and Model D private routing | Positive own-private read, two negative controls, unchanged original memory revisions and exact new scope passed; after-failure isolation deferred |
| Representation publication | Yes: `NativeRepresentationService.publish_representation_ready` and current vector witness checks | New READY/USABLE embedding with MATCH observed and retrieved; interrupted/orphan READY cases not run |
| Receipt recovery | Yes: `NativePublicMutationReceiptStore` immutable operation ledger and executor recovery dispatch | All four ordinary receipt stages observed; uncertain/incomplete/replay recovery cases available, not run |
| Trajectory fencing | Yes: `TrajectoryWriterHandoffCoordinator`, native session replacement and per-effect validation | Fresh handoff, lawful generation 3→4, native effects and sealed history passed; failure/replacement scenarios deferred |
| Stale writer refusal | Yes: unchanged `_validate_current_token` and generation/session authority checks | Mechanism available with independent disposable identity; stale native/legacy writer cases not run |

Native writer recovery uses the same persisted coordinator and trajectory code.
The retained EID 8 and sealed chunk provide real committed disposable evidence
for later scenarios A/E. SQLite contention can target its own core; existing
executor interruption/storage hooks remain available for an authorized bounded
scenario B; ordinary tokens and replacement can exercise scenario D. R1 invoked
none of those hooks or failure conditions. No material mechanism in this table
was replaced by a mock.

## Production isolation

The service's selected core, recovered scope configuration, coordinator metadata
and every trajectory artifact root resolve under the disposable directory.
The root and ancestors contain no reparse point or symlink; root contents were
also checked. Production and disposable core/path identities differ. The
unchanged coordinator requires a registered artifact-root identity, so these
disposable authority bindings do not authorize a production trajectory path.
No copied trajectory history or alias was used. This is configured-path and
authority-binding isolation evidence, not a claim of a separate OS sandbox.

Complete production inventories before construction, while PID 29032 was serving
after the write, and after clean shutdown compare equal: **2,093 files,
1,163,889,969 bytes**, canonical path/size/SHA-256 tree digest
`345d29441220273acc1371641be08e4e0dfd0fc381018b16a601b16dfb648776`.
The comparison covers databases, WAL/SHM files, trajectory artifacts, authority,
policy and configuration under the production root. Its read-only authority
snapshots also match exactly.

Production remains selector generation 8, NATIVE_ACTIVE, core
`f21c730f-5222-4aa8-9a5f-1c1188456df3`, NATIVE_AGREEMENT. The observed lifecycle
trajectory remains generation 10, legacy fence 2, HANDOFF_COMPLETE.
`REAL_ROOT_PATH_COLLISION = NO`; `DISPOSABLE_TO_PRODUCTION_WRITE_LEAK = NO`;
`REAL_ROOT_UNCHANGED_BY_STAGE4R1 = YES`.

## Evidence and final boundary

`assessment.json` records 28 passing checks, including ordinary positive
publication and trajectory evidence, scope controls, clean shutdown and complete
production equality. The initial health timing and fixture expectation errors
remain retained. Read-only helper development also corrected a schema-column
name and selector-file locator before successful assessment; these were
observation-script errors and caused no application mutation or injected failure.

`disposable_root_inventory.json` freezes the stopped root's complete file hashes.
`architecture_review.json` binds reviewed source files. `evidence_manifest.json`
binds the helper scripts, launch logs, requests, receipts, inventories and result
assessment; raw root files are bound through its root inventory. Cache files,
the manifest itself and later `publication.json` are excluded from that manifest.
Evidence manifest SHA-256: `24bcf030346333a13fe23b4233fb2c6dd786e5ddd53c4a5b4d6f1472b5b80e6e`.

No full test suite was run: application code is unchanged and R1 authorizes a
positive baseline only. No unclean termination, transaction interruption,
lock contention, stale-writer attempt, corrupted storage, tuning or repair ran.
Synthetic shared retrieval is bounded fixture evidence; production nonempty
shared-domain ranking and production Character-history validation remain deferred.

```text
QUALIFIED_NATIVE_RELOCATION_API = NO
FRESH_DISPOSABLE_AUTHORITY_OVER_COPIED_CORE = NOT_QUALIFIED
SYNTHETIC_CURRENT_SEMANTIC_ROOT_SUFFICIENT_FOR_STAGE4 = YES
DISPOSABLE_AUTHORITY_IDENTITY_MAY_DIFFER = YES
PRODUCTION_TRAJECTORY_HISTORY_REQUIRED_FOR_STAGE4 = NO
STAGE4_DISPOSABLE_METHOD = EXISTING_SYNTHETIC_ROOT_BUILDER_WITH_PRODUCTION_CUTOVER_AND_HANDOFF
DISPOSABLE_ROOT_CREATED = YES
DISPOSABLE_ROOT_PRODUCTION_SEMANTICALLY_FAITHFUL = YES
DISPOSABLE_AUTHORITY_CREATED_THROUGH_QUALIFIED_API = YES
DISPOSABLE_BASELINE_STARTUP = PASS
NATIVE_OWNER_RECOGNITION = PASS
SELECTOR_CORE_AGREEMENT = PASS
LEGACY_PUBLIC_FALLBACK = NO
TRAJECTORY_COORDINATOR_ADMISSION = PASS
BASELINE_READ = PASS
BASELINE_WRITE = PASS
BASELINE_READ_AFTER_WRITE = PASS
DISPOSABLE_TO_PRODUCTION_WRITE_LEAK = NO
DISPOSABLE_BASELINE_SHUTDOWN = PASS
REAL_ROOT_UNCHANGED_BY_STAGE4R1 = YES
REAL_SELECTOR_CORE_AGREEMENT = PASS
REAL_NATIVE_ACTIVE = YES
GENERATIVE_MODEL_INVOLVED = NO
GENERATIVE_MODEL_DETAILS = NONE; OFFLINE BGE EMBEDDINGS ONLY
POST_P7_NATIVE_PRODUCTION_VALIDATION_STAGE_4R1 = PASS
POST_P7_NATIVE_PRODUCTION_VALIDATION_STAGE_4 = BLOCKED_PENDING_FAILURE_SCENARIOS
SQLITE_MIGRATION_FULLY_VALIDATED = NO
NEXT_AUTHORIZATION_BOUNDARY = POST_P7_NATIVE_PRODUCTION_VALIDATION_STAGE_4R1_REVIEW
```

The frozen original Stage-4 admission blocker remains valid for a byte-identical
relocated production copy. This newly qualified method changes the pending work
status to failure scenarios awaiting review; it does not retroactively pass
Stage 4 or authorize those scenarios. Stop at the R1 review boundary.
