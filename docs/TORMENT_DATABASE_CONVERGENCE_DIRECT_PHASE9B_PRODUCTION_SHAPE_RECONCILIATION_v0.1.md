# TORMENT Database Convergence — Direct Phase-9B Production-Shape Reconciliation v0.1

## Verdict

`DIRECT_PHASE9B_PRODUCTION_SHAPE = QUALIFIED`

The direct-only bridge supplies frozen caller-owned P1 namespace bindings to
the existing `qualify_metadata_less_per_eid_legacy_source(...)` authority for
the three qualified UNKNOWN_IDENTITY private scopes.  It projects the returned
evidence into the existing direct carrier without creating a source identity,
namespace, canonical-text source, or representation identity.

`nodes.jsonl` remains the canonical text-bearing evidence.  The bridge binds
the selected `emb_<eid>.npy` and existing optional-edge evidence, while the
Phase-9B qualifier selects the canonical payload input and preserves UNKNOWN
identity plus `REEMBED_REQUIRED`.

## Qualification ledger

```text
DIRECT_PHASE9B_PRODUCTION_SHAPE = QUALIFIED
DIRECT_SYNTHETIC_METADATA_FIELD_REQUIRED = NO
DIRECT_CANONICAL_TEXT_FILE_REQUIRED = NO
PHASE9B_QUALIFIER_REUSED = YES
UNKNOWN_PROVIDER_REMAINS_UNKNOWN = YES
UNKNOWN_MODEL_REMAINS_UNKNOWN = YES
REEMBED_REQUIRED_PRESERVED = YES
DIMENSION_IDENTITY_INFERENCE = NONE
LEGACY_SOURCE_MUTATION = NONE
HISTORICAL_CORRECTIVE_SEMANTICS_CHANGED = NO
P1_SEMANTICS_CHANGED = NO
P2_SEMANTICS_CHANGED = NO
REAL_ROOT_CONTACT = NONE
PRODUCTION_SQLITE_WRITE = NONE
P2_EXECUTED = NO
```

## Evidence

Disposable production-shaped fixtures cover successful qualification and exact
projection for `ws3|PRIVATE|a1`, `ws4|PRIVATE|a1`, and `ws5|PRIVATE|a1`,
including the canonical `summary` input.  They also refuse missing, malformed,
or mismatched vectors; duplicate EIDs; and unavailable canonical input.  The
existing historical corrective capture fixtures retain their frozen synthetic
compatibility grammar.

The required focused suite result was `111 passed, 5 skipped`.  No production
root, provider, model, service, or P2 path was contacted or executed.
