# TORMENT Brainvision BY-Aware Closure Failure Anatomy Plan v1.6a

## 1. Status / non-claims

**DOCS-ONLY plan. Non-authorizing, non-implementing. Opens no code, no tests, no runtime, no integration lane.**
It turns the accepted v1.6 failure-anatomy proposal into a **finite plan** for a possible later reporting-only
diagnostic: exactly which inspections a final BY fixture-metric anatomy could run over the frozen records to
decide whether a concrete **bounded closure lever** exists — or whether the branch should **pivot** off the
fixture-metric route. It **plans inspections only — it adopts no metric, no equation, no threshold, no pass/fail
validity rule, no offset-vs-`TOL` gate, no binding gate**, and implements nothing. It **authorizes no code and
no tests**, **redefines no `TOL`**, changes no formula / §7 anti-proxy logic / §8 verdict logic, deletes or
weakens no control, redesigns no descriptor, reopens no spectral group as a closure group, expands no generator
family, and opens **no classifier (form B) and no neural encoder (form C)**. It does **not** pivot to flat /
screen geometry itself and opens **no flat-geometry / screen-analysis implementation**. Everything stays offline
under `research/brainvision/` + `tests/research/`, HELD per v0.6.

This note makes **no** vision claim, **no** "Brainvision sees" claim, **no** temporal-order claim, **no**
descriptor-validity claim, **no** memory-readiness claim, **no** runtime-readiness claim, and **no**
integration-readiness claim. It touches no `torment_service/`, runtime, camera / sensor / live-capture /
screen-capture / streaming, or prompt / context / memory / action / render-body / autonomy paths, and makes
**no real-clip / local-clip move** and **no memory-system integration**. A plan alone moves nothing: **no claim
lock and no verdict changes here.**

```text
first_pass_structure_validity_claim_allowed = False
temporal_claim_allowed                      = False
descriptor_validity_claim_allowed           = False
verdict                                      = HOLD
```

**No `§0` pointer; no tags.**

## 2. Relation to v1.6 proposal

```text
v1.5f (559964a)  FINDINGS: BY_aware_closure_gap_still_visible; closure_achieved False; HOLD.
v1.6  (e992936)  failure-anatomy PROPOSAL: candidate mechanisms A-E; decision point (bounded lever OR pivot);
                 the LAST BY fixture-metric anatomy step unless it produces a clear bounded lever.
v1.6a (this doc) finite anatomy PLAN: exactly which reporting-only inspections a later diagnostic could run over
                 the frozen records to decide lever-vs-pivot; adopting none; authorizing no code.
```

v1.6 framed the decision and the candidate mechanisms; v1.6a fixes the finite inspection plan and the concrete
bounded-lever test, so a later, separately-gated diagnostic (or the pivot) is a narrowed choice. It proves
nothing, validates nothing, adopts nothing, and changes nothing v1.4 / v1.4a / v1.5 / v1.6 froze.

## 3. Finite anatomy objective

```text
CORE QUESTION:
  Can ONE final bounded anatomy step identify a concrete CLOSURE LEVER for the persistent BY signed-ordering gap,
  or should the branch PIVOT away from the current fixture-metric route?

OBJECTIVE:
  Pre-commit, in finite detail, the reporting-only inspections (mapped to mechanisms A-E) that a later diagnostic
  could run over the frozen records, and the exact test that decides LEVER vs PIVOT -- WITHOUT adopting any metric
  / equation / threshold / gate and WITHOUT authorizing any code.
```

## 4. Frozen inputs

All inputs are reused **by identity**; none are recomputed, replaced, reseeded, or expanded by this plan or by a
later diagnostic.

```text
- TOL = 0.0634; PSC_FLOOR = AIC_FLOOR = 0.30; CHANCE_BAND = 0.60 (referenced frozen; not re-thresholded).
- the frozen evaluator; descriptor / _stats / GROUPS / best-threshold BA / robustness lens;
  proxy_match_residual (L-inf over the ten matched stats, spectral excluded); PSC < PSC_FLOOR feasibility.
- the sealed v0.7b matching: 19 matched / 5 unmatched pairs; single matching family segment_paired_canceller;
  closed F1-F5 family set; spectral audit-note-only (NOT a closure group).
- the reused BY-aware quantities (v0.8a / v0.9b / v1.0b / v1.2 / v1.5 panels A-G, and the natural reference chance
  level 0.5) by identity.
- claim locks and verdict HOLD.
```

## 5. Allowed anatomy questions

A later diagnostic MAY ask (reporting-only, reuse by identity, adopting nothing), mapped to the v1.6 mechanisms:

```text
A (residual aggregation hides ordering):
  - Is the signed BY ordering SYSTEMATIC ACROSS PAIRS beyond per-pair sign consistency (e.g. described against the
    chance level 0.5), on the pairs where residual <= TOL?
C (BY/RG balance mismatched):
  - On the matched pairs, how does the BY-axis discrepancy compare to the RG / directional discrepancy (reported
    as the existing |smd|/TOL magnitudes), i.e. is the passing residual balancing away a BY-concentrated gap?
D (by_std binds strongest):
  - Conditioned on which stat binds the L-inf residual, how does the BY signed ordering look when by_std binds vs
    when centroid / spread binds (annotation only; residual unchanged)?
B (offset structural to the family):
  - Is the signed offset INVARIANT across the single family's members (i.e. a family property) vs separable from
    them, over the frozen pairs?
E (wrong abstraction):
  - Do the A / C / D contrasts point at something REPRESENTABLE over the fixtures, or do they only re-express the
    winder / non-winder trajectory-canceller abstraction (i.e. abstraction-level, not fixture-metric)?
All questions are DESCRIPTIVE re-presentations of frozen quantities; each answer is reported, not decided.
```

## 6. Forbidden anatomy questions

```text
- NO question that requires a new metric / equation / threshold / ratio / sign-consistency cutoff / pass-fail rule.
- NO question that redefines TOL / floors / CHANCE_BAND, adds an offset-vs-TOL gate, or a binding gate.
- NO question that redesigns the descriptor / _stats / GROUPS, expands the family, or reopens spectral as closure.
- NO question requiring new data / samples / seeds / families / candidate generation, a classifier (form B), or a
  neural encoder (form C).
- NO question requiring flat-geometry / screen-analysis IMPLEMENTATION, camera / live / sensor / screen / streaming,
  runtime / memory / integration, or real clips.
- NO question that promotes a report-only view (A / C / D) into a decision input or gate.
A forbidden question is out of scope regardless of how promising it looks; the anatomy INSPECTS, it does not build.
```

## 7. Failure mechanism inspection plan

Finite, bounded, reporting-only; each item re-presents frozen quantities and is tagged by whether a positive
finding would point to a fixture-metric LEVER or a PIVOT:

```text
item  mechanism  inspection (reporting-only, reuse by identity)                         positive finding points to
----  ---------  -------------------------------------------------------------------    --------------------------
1     A          across-pair sign-structure of the BY offset on residual<=TOL pairs      LEVER candidate (A/C/D)
                 (systematic beyond per-pair sign consistency, vs chance 0.5)
2     C          BY-axis vs RG/directional |smd|/TOL magnitudes on matched pairs         LEVER candidate (A/C/D)
                 (is the passing residual balancing away a BY-concentrated gap?)
3     D          BY signed ordering conditioned on the binding stat (by_std vs others)    LEVER candidate (A/C/D)
                 (annotation only; residual definition unchanged)
4     B          invariance of the signed offset across the single family's members       PIVOT signal (B)
5     E          whether items 1-4 are representable over fixtures or only re-express      PIVOT signal (E)
                 the winder/non-winder abstraction
Items 1-3 test for a bounded LEVER; items 4-5 test for a PIVOT signal. The plan runs the SAME frozen records; it
adds no statistic that is not a re-presentation, and it adopts nothing.
```

## 8. Stop/pivot rule

```text
This is the FINAL BY fixture-metric anatomy step UNLESS it identifies a concrete BOUNDED lever.
  - IF items 1-3 surface a concrete bounded lever (per §9) -> open a later, separately-gated diagnostic of THAT
    lever (docs-first first), still adopting no metric / threshold / gate.
  - IF items 1-3 surface NO bounded lever, OR items 4-5 indicate the offset is structural to the family (B) or the
    abstraction is wrong (E) -> the fixture-metric route is exhausted -> PIVOT (§14 Branch B), docs-only.
No further BY fixture-metric anatomy is planned beyond this step absent a bounded lever. The rule exists to end
metric grind: one final bounded inspection, then either a lever or a pivot.
```

## 9. What would count as a bounded lever

A finding qualifies as a **bounded lever** only if it is ALL of:

```text
- SPECIFIC   : names one concrete inspection / represented quantity (not a vague "look closer").
- FINITE     : runs over the existing frozen records (19 matched / 5 unmatched pairs, ten stats, three BY stats),
               no new data / seeds / families / candidate generation.
- inspectable WITHOUT redefining TOL (TOL and the residual definition stay frozen).
- inspectable WITHOUT inventing thresholds (uses natural references, e.g. chance 0.5, or reports raw magnitudes).
- inspectable WITHOUT descriptor redesign (descriptor / _stats / GROUPS unchanged).
- inspectable WITHOUT generator-family expansion (single matching family; caveat preserved).
- COMPATIBLE with all claim locks remaining False and verdict HOLD (non-authorizing).
A lever is a candidate DIRECTION for a later docs-first plan; identifying it adopts nothing and closes nothing.
```

## 10. What would not count as a bounded lever

```text
- Anything requiring a new metric / equation / threshold / ratio / sign-consistency cutoff / pass-fail validity rule.
- Anything requiring a TOL change, an offset-vs-TOL gate, or a binding gate.
- Anything requiring descriptor redesign, family expansion, spectral reopening, or new data / seeds / candidates.
- Anything requiring a classifier / neural encoder, flat-geometry / screen-analysis implementation, runtime / memory
  / integration, or real clips.
- A vague or open-ended "inspect more" with no specific, finite, frozen-records inspection.
- Anything that would move a claim lock or the verdict.
If the only way to "close" the gap is one of the above, it is NOT a bounded lever -> PIVOT.
```

## 11. What later implementation may inspect

If a later, separately-gated reporting-only diagnostic is authorized (docs-first first), it **may inspect / re-
present** over the frozen records:

```text
- the across-pair sign structure of the BY offset on residual<=TOL pairs (item 1, mechanism A);
- the BY-vs-RG / directional |smd|/TOL magnitudes on matched pairs (item 2, mechanism C);
- the BY signed ordering conditioned on the binding stat (item 3, mechanism D; annotation only, residual frozen);
- the invariance of the signed offset across the single family's members (item 4, mechanism B);
- whether items 1-4 are fixture-representable or abstraction-level (item 5, mechanism E);
- protocol_ok / a reporting label / a claim-lock + verdict summary (locks False; HOLD).
All inspection re-presents frozen quantities; it computes no new statistic that is not such a re-presentation and
decides nothing.
```

## 12. What later implementation may not claim

```text
not vision                     not "Brainvision sees"
not descriptor validity        not temporal order
not closure                    not a pass / a met threshold / an adopted metric
not real-video understanding   not a unique real-world color-structure advantage
not generator-family generalization (single-family caveat stands)
not memory readiness           not runtime readiness           not integration readiness
not live / screen / camera use
```

Inspecting the failure — and even choosing to pivot — is an in-vitro, single-matching-family, design-layer step;
it says nothing about real clips or screens and does not validate the descriptor or adopt a metric. The proof
route remains **HELD / HOLD** because the offset is visible but not closed. The claim locks and `verdict = HOLD`
remain in force.

## 13. Review checklist

A later diagnostic (or the pivot decision) is **consistent with this plan** iff a reviewer can check all of:

```text
[ ] docs/records only at THIS stage; any diagnostic only after a separate docs-first gate.
[ ] reuses v0.7b / v0.8a / v0.9b / v1.0b / v1.2 / v1.5 by identity; no new data / seeds / families / candidates.
[ ] inspections limited to items 1-5 (§7); each a re-presentation of frozen quantities; residual definition frozen (item 3).
[ ] NO new metric / equation / threshold / ratio / cutoff / pass-fail rule; TOL / floors / CHANCE_BAND frozen.
[ ] NO offset-vs-TOL gate; NO binding gate; NO descriptor redesign; NO family expansion; NO spectral reopening.
[ ] a "bounded lever" claim meets ALL of §9; anything in §10 is NOT a lever -> pivot.
[ ] the stop/pivot rule (§8) is applied: lever -> docs-first lever plan; no lever / mechanism B or E -> pivot to Branch B.
[ ] claim locks False; verdict HOLD; no §0 pointer; no tags.
```

## 14. Candidate next branches

Docs-first candidates only; **none opened or authorized here**:

```text
A. Later reporting-only FAILURE-ANATOMY diagnostic (docs-first, then SEPARATE authorization)  -- IF a bounded lever
   (items 1-3, per §9) is surfaced: inspect that lever over the frozen records, adopting no metric / threshold / gate.
B. FLAT OPPONENT-PLANE / SPATIAL-FIELD proposal (docs-only)  -- IF no bounded lever (items 1-3 empty) OR mechanism
   B / E (offset structural to the family / wrong abstraction): pivot off the fixture-metric route. Conceptual only;
   NO flat-geometry / screen-analysis implementation.
C. Operator / new-math NOTE (docs-only).
D. Pause Brainvision and return to TORMENT memory / kernel work.
```

## 15. Recommended next step

**Recommend using this plan as the final fixture-metric decision gate.** If a later diagnostic (or the operator's
reading of items 1-5) surfaces a concrete bounded lever meeting ALL of §9, open Branch A as a docs-first lever
plan (a diagnostic is then a SEPARATE authorization). Otherwise — no bounded lever, or mechanism B / E — **pivot
to Branch B (docs-only flat opponent-plane / spatial-field geometry proposal)**. This is explicitly the **final BY
fixture-metric anatomy step unless it identifies a concrete bounded lever** (§8). C (operator new-math) and D
(pause) remain legitimate operator calls.

```text
1. Codex review THIS plan (docs-only; over committed edge e992936).
2. If accepted, the operator commits this doc. No §0 pointer; no tags.
3. If the operator chooses to proceed:
   - if items 1-3 surface a bounded lever (all of §9) -> open Branch A as a docs-first lever plan; any diagnostic is
     a SEPARATE authorization (reporting-only; no metric / threshold / gate / family / spectral / flat / screen /
     runtime / memory adopted);
   - else (no lever, or mechanism B / E) -> pivot to Branch B (docs-only flat opponent-plane / spatial-field proposal;
     no flat-geometry / screen-analysis implementation).
4. Otherwise the Brainvision prototype line stays HELD. No code, classifier (B), neural (C), runtime, memory,
   real-clip, screen, flat-geometry, §0, or tag work is recommended or authorized here.
```

Claim locks and verdict are unchanged: `first_pass_structure_validity_claim_allowed = False`,
`temporal_claim_allowed = False`, `descriptor_validity_claim_allowed = False`, `verdict = HOLD`.

## 16. Codex review prompt

```text
Please review docs/TORMENT_BRAINVISION_BY_AWARE_CLOSURE_FAILURE_ANATOMY_PLAN_v1.6a.md
(new, docs-only, untracked; over committed edge e992936, turning the accepted v1.6 proposal into a finite
failure-anatomy plan and the concrete lever-vs-pivot test, adopting none, authorizing no code).

Verify that this plan:
- is docs-only and authorizes no implementation (no code/tests, no torment_service/, no runtime, no memory, no
  camera/live/sensor/screen/streaming, no real clips); keeps form B (classifier) and form C (neural) CLOSED; and
  authorizes NO flat-geometry and NO screen-analysis implementation;
- plans INSPECTIONS ONLY and ADOPTS NONE -- it defines no metric, no equation, no threshold, no pass/fail validity
  rule, no offset-vs-TOL gate, no binding gate; redefines no TOL; redesigns no descriptor; expands no family;
  reopens no spectral group;
- states the core question (can one final bounded anatomy identify a concrete closure lever, or pivot?) and maps a
  FINITE, reporting-only inspection plan to mechanisms A-E over the frozen records (items 1-5), each a
  re-presentation of frozen quantities with the residual definition frozen (item 3);
- lists ALLOWED (§5) vs FORBIDDEN (§6) anatomy questions, and defines a BOUNDED LEVER (§9) that must be ALL of:
  specific, finite, inspectable without redefining TOL / inventing thresholds / descriptor redesign /
  generator-family expansion, and compatible with all claim locks False; with §10 listing what does NOT count;
- gives the stop/pivot rule (§8 / §15): the FINAL BY fixture-metric anatomy step unless it identifies a concrete
  bounded lever; else pivot to a docs-only flat opponent-plane / spatial-field proposal (Branch B);
- keeps everything in §4 frozen and §12 unproven (vision / descriptor validity / temporal order / closure remain
  unproven under lever or pivot);
- provides a review checklist (§13), recommends Branch A (docs-first lever plan) only if a bounded lever is
  surfaced else Branch B (pivot), lists C/D, and opens none;
- preserves all claim locks (first_pass_structure_validity_claim_allowed = False; temporal_claim_allowed = False;
  descriptor_validity_claim_allowed = False) and verdict = HOLD; adds no §0 pointer and no tags.

Flag any adopted metric / equation / threshold / pass-fail rule, any TOL redefinition, any offset-vs-TOL / binding
gate, any descriptor redesign, any family expansion, any spectral reopening, any new data / seeds / candidates, any
flat-geometry / screen-analysis / runtime / memory / real-clip authorization, any code authorization, any "bounded
lever" that fails §9, any claim that closure is ACHIEVED, any descriptor-validity / vision / temporal-order claim,
or any claim-lock/verdict movement.
Return ACCEPT AS-IS or MODIFY with exact required changes.
```

Brainvision remains **offline / quarantined**, HELD per v0.6.
`first_pass_structure_validity_claim_allowed = False`, `temporal_claim_allowed = False`,
`descriptor_validity_claim_allowed = False`, and the frozen verdict **HOLD** are unchanged.
**No `§0` pointer; no tags.**

*End — TORMENT Brainvision BY-Aware Closure Failure Anatomy Plan v1.6a. Docs-only plan, non-authorizing. Opens no
implementation lane; opens no classifier / neural / screen / flat-geometry work; changes no frozen formula, gate,
evaluator, or verdict; deletes or weakens no control; redesigns no descriptor; invents no threshold; redefines no
TOL; adopts no closure metric or equation; creates no offset-vs-TOL or binding gate; plans finite reporting-only
inspections around mechanisms A-E only, adopting none; is the FINAL BY fixture-metric anatomy step unless it
identifies a concrete bounded lever, else pivot to flat opponent-plane / spatial-field; keeps the gap visible not
closed; makes no vision / descriptor-validity / temporal-order / memory / runtime / integration claim; no `§0`
pointer added; no tags.*
