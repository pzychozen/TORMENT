# TORMENT Gate 4 — Derived Identity Anchors Read-Only Conformance-Question Frame v0.1

**DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — QUESTION FRAMING ONLY, NO REMEDY. NO IMPLEMENTATION,
NO TESTS, NO RUNTIME, NO VERDICT.**

This frame poses — read-only — the conformance question for exactly one Gate 4 core runtime row: the
live automatic writer `torment_service/fabric.py::_maybe_emit_identity_anchor` (derived identity-anchor
emission). **It poses the question; it does not answer it.** It selects no remedy, no patch, no fix order,
no enforcement, no governance vehicle, no runtime authority gate, no schema/carrier, no `canon_source`, no
source-sameness mechanic, and no database/substrate decision. It makes no safe/unsafe verdict, no defect
verdict, no canon claim, and no automatic-promotion claim, and selects no `diagnostic_only`
implementation.

**Operator-supplied docs-only state:** `f1500f6` (*docs(gate-4): point orientation map to mood topology
frame*), treated as the current operator-supplied state; the older Gate 4 stop preconditions are
superseded **only** by the operator-named docs-only commits in the Gate 4 lane (`a92dbef` → `dd53b7e` →
`f6d0fb3` → `41c4349` → `f4ee79a` → `03af2c3` → `7090a11` → `cc6cac4` → `f1500f6`). No `git` was run.
**Date:** 2026-06-20. Windows-visible tracked repo state remains authoritative.

> Memory may shape context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.

**Tags:** `[FACT]` point-in-time, carried from prior read-only artifacts · `[BOUNDARY]` already-promoted
requirement obligation · `[QUESTION]` posed only, not answered · `[POSTURE]` conformance posture ·
`[DISTINCTION]` · `[CONTEXT]` carried context, not scope · `[NON-CLAIM]` · `[NON-AUTHORIZATION]` ·
`[STOP]`.

---

## 0. Provenance, precondition, and review status

`[FACT]` **Lineage.** Gate 4 selection frame → Gate 4 read-only conformance-gap map → Gate 4
`gravity_correction` row question frame → Gate 4 mood-topology row question frame → **this derived
identity-anchors row question frame.** This is row **C** in the reviewed framing sequence (A
`gravity_correction` → B mood-topology → **C derived identity anchors** → D warmth / role-cadence) — a
**framing sequence, not a fix order**. It is eligible now because the sequence records C as derived
anchors and because this row **does not depend on warmth or role-cadence being framed first**. It is a
**question frame**, not reconciliation work and not a "row to reconcile"; it poses the Gate-4-lane
conformance question and answers nothing.

`[FACT]` **Review status: Codex challenge completed: ACCEPT WITH CORRECTIONS; corrections applied;
pending operator review / promotion.** The file remains **docs-only and non-authorizing** and changes no
behavior; nothing downstream may treat it as ratified, or as authorizing implementation, remedy, writer
patch, enforcement, substrate, or any successor gate, until operator review / promotion.

`[FACT]` **Precondition honored.** This frame is expressible **read-only and non-substrate** (§1); no
forbidden item (§6) was required to write it. Per the Gate 4 lane `[STOP]` conditions, had posing the
question required runtime/endpoint/service/ingest-query/database/workspace-frequency evidence, a verdict,
a remedy, a writer selection, a fix order, an enforcement path, a governance vehicle, an authority gate, a
`canon_source` / source-sameness / `diagnostic_only` mechanic, or any substrate decision, drafting would
have stopped instead. It did not.

### 0.1 Relationship to prior artifacts (load-bearing)

`[DISTINCTION]` This row already has paired write-side / read-side doctrine and routing in prior promoted
artifacts; this frame **adds no scope beyond the conformance question** and does not duplicate them:

- **Matched P2.5/P4 Writer/Reader reconciliation** (§5.2; registry §N13) and **registry §N4** already
  **pair** the derived-anchor write-side seam (P2.5 §6 family C — member-eid resolution by presence) with
  the read-side requirement (**P4 O2** source-membership sameness under the applicable family-bound
  adequacy standard). This frame **does not duplicate, re-open, re-scope, amend, or answer** that pairing;
  it poses the Gate-4-lane conformance question only.
- **Gate 4 conformance-gap map §2.2** already recorded this row's posture as *not-yet-conformant /
  requires reconciliation* on two named axes (write-side A-O1, reader-side P4 requirement). This frame
  states the questions that posture leaves open.
- **Gate B hazard inventory H2** and **pre-substrate Family D** are the carried point-in-time
  characterization. The **gravity_correction frame (Q-C1–Q-C4)** and the **mood-topology frame
  (Q-M1–Q-M4)** are separate rows; this frame **does not re-answer** them.

---

## 1. Status, scope, method

`[FACT]` This is a **docs-only read-only conformance-question frame** scoped to **exactly one row**: the
live writer `torment_service/fabric.py::_maybe_emit_identity_anchor`. It is **no implementation, no tests,
no runtime, no service, no endpoint, no ingest/query, no instrumentation, no remedy, no verdict, no
database/substrate.** Every `[FACT]` below is **point-in-time and carried** from prior read-only
artifacts (Gate B H2, gap map §2.2, the matched P2.5/P4 reconciliation §5.2 / registry §N4, pre-substrate
Family D, the role-inference cadence trace). **No new tracing, execution, measurement, or
frequency/workspace observation was performed**; line-anchored specifics live in those artifacts at their
baselines and are not re-asserted as current here.

`[POSTURE]` **Required posture, carried verbatim in intent:** this is a *read-only conformance-question
frame*; the writer is a *not-yet-conformant / requires reconciliation* automatic derived identity-family
writer; its *conformance posture is not yet resolved*; this is *question framing only, no remedy*. Per
Document A A-O5 / Seed-Gov SG-O5, the not-yet-conformant flag is **not** a must-patch order; audit
observes the seam without becoming authority.

`[POSTURE]` **No verdict.** "Not-yet-conformant / requires reconciliation" and "conformance posture not
yet resolved" are governance-conformance postures, **not** a claim that the writer is wrong, right, safe,
unsafe, or defective.

### 1.1 Carried write-surface facts (point-in-time; no new claims)

`[FACT]` Carried from Gate B H2, gap map §2.2, and the role-inference cadence trace (point-in-time, not
re-measured): `_maybe_emit_identity_anchor` is an **automatic derived identity-family writer**. It emits
`mtype="identity_anchor"` with **`canon=False`** and provenance **`anchor_origin="derived"`** /
**`anchor_source="motif_cluster"`**, reached **only under motif-recurrence count/gap gates** (it is
emitted when an agent repeatedly contributes to the same motif and the count/gap conditions are met). It
**may retire a prior anchor for the same motif** (write-surface behavior; see Q-D4). Derived anchors are
**cognition-reenterable today through ordinary tiering**.

`[NON-CLAIM]` **Reentry bound (carried, load-bearing).** Cognition-reenterability here is **ordinary
tiering / ordinary reentry only**. This frame does **not** imply privileged canon-anchor treatment or a
full continuity boost for derived anchors. **Derived anchors as scoped here are `canon=False` and
ordinary-tier / ordinary-reentry only; any separate promotion question is outside this frame, unopened,
and unevaluated** (no automatic promotion, no canon claim is made or implied here).

### 1.2 Context that is not scope

`[CONTEXT]` **Role inference / cadence is context only, not the in-scope row.** Role modulation scales the
base anchor count/gap cadence thresholds *as input to this emitter*; the **role-cadence row is the
separate row D**, not this one, and is not posed here.

`[CONTEXT]` **Affect-sensitivity tightening is gating context only, not a separate scope.** The
affect-sensitive motif tightening is a later, independent modifier **inside the same identity-anchor
emitter** that can further raise the thresholds; it is carried as gating context, **not** a separate row
or scope.

`[OUT-OF-SCOPE]` This frame is **one row only**. The `gravity_correction` row, the mood-topology row,
the warmth row, the role-cadence row (D), authored `seed_text` (boundary context), and H3 / H5 / H6
appear **only** as out-of-scope notes, never as scope.

---

## 2. Already-promoted boundaries this row is read against

`[BOUNDARY]` Named as the obligations the question is posed against — naming a boundary is not
implementing it; no P4 / source-sameness / Seed-Gov mechanic is selected:

- **Document A — A-O1** (class-bound writer authority; authorization **must not** be inferred solely from
  payload flags (`canon`/`mtype`/`tier`/half-life) or from source presence; today these are trusted
  without a writer-authority check). **Carried context: A-O4** (Document A's write-side obligations pair
  with P4's read-side; runtime conformance is P2.5 / separately-authorized) and **A-O5** (existing
  automatic writers are named **unreconciled seams, not exemptions**; Document A patches none).
- **P4 — O2 (named as a requirement only).** **P4 O2 requires a persisted motif member reference to
  contribute to derived identity-anchor emission only when source-membership sameness is proven under the
  applicable family-bound adequacy standard. P4 §9 names `diagnostic_only` as an eligibility posture for
  unprovable references. This frame names the requirement / posture only; it selects no proof design,
  source-membership proof design, source-sameness mechanics, carrier, schema, P4/Q lineage mechanics, or
  `diagnostic_only` implementation.**
- **Pre-substrate Family D** (derived identity anchors: automatic, non-canonical, **cognition-reenterable
  today**; "reenterable-today → P4 proof-or-`diagnostic_only`"; later owner P4 reader-side + P2.5 writer)
  and **gap map §2.2** (this row's recorded posture). The Ledger Observational-Boundary ("audit observes
  authority; audit does not become authority") governs this frame's own posture.

---

## 3. The conformance questions (posed only — not answered)

`[QUESTION]` Each item is **posed and left open**. None is answered; no remedy, fix order, writer-patch
selection, enforcement, governance vehicle, authority gate, canon claim, automatic promotion, or
`diagnostic_only` implementation is offered or implied. "Conformance posture not yet resolved" stands for
every one.

- **Q-D1 — write-side authority.** Does the **automatic creation of a derived identity-family anchor**
  require a **resolved writer-authority posture under A-O1** (class-bound authority, not inferred from
  payload flags or source presence)? *Posed only; not answered here.*
- **Q-D2 — reader-side reenterability.** Does this writer's product being **cognition-reenterable**
  require a **resolved reader-side posture under P4 O2** (motif-member source-membership sameness, or the
  `diagnostic_only` eligibility posture) — **without this frame selecting any source-sameness mechanic**?
  *Posed only; not answered here.*
- **Q-D3 — does `canon=False` settle it?** Does **`canon=False`** by itself remove the authority
  question, **or** does **identity-family reenterability** (a derived identity-typed row re-entering
  ordinary cognition through ordinary tiering) **still require reconciliation**? This is **posed as an
  open question**, consistent with the carried framing that *not canon does not mean behaviorally inert*;
  it asserts **no verdict** either way. *Posed only; not answered here.*
- **Q-D4 — retirement / replacement.** Is the **retirement / replacement of a prior anchor for the same
  motif** a **separate conformance question**, or is it carried **only as write-surface behavior**? *Posed
  only.* `[NON-AUTHORIZATION]` This poses the question **at the write-surface level only**; it opens **no**
  `update_payload` repair, **no** stored-edge repair, and **no** P4/Q-* lineage mechanics.
- **Q-D5 — already-recorded routing.** Which **later-owner routings for this row are already recorded** by
  prior artifacts — **without this frame creating, selecting, or re-picking any**? *Posed only.*
  `[POSTURE]` As carried fact (not a selection): prior artifacts already record routings — pre-substrate
  Family D and gap map §2.2 route this row to **P4 reader-side + P2.5 writer**; Document A A-O4 routes
  write-side runtime conformance to the **P2.5 / separately-authorized track**; the matched P2.5/P4
  reconciliation §5.2 / registry §N4 record the **derived-writer ↔ P4-O2 membership-sameness pairing**.
  This frame **re-picks none, selects none, creates none, and resolves none** of them.

---

## 4. Dependency / sequence note (framing sequence, not a fix order)

`[DISTINCTION]` This row (C, derived anchors) **does not depend** on the warmth or role-cadence rows being
framed first; role inference and affect-sensitivity appear here only as **gating context** (§1.2). The
reviewed order — **A.** `gravity_correction` → **B.** mood-topology → **C.** derived anchors (this frame)
→ **D.** warmth / role-cadence soft O6 paths — is a **framing sequence, not a fix order**, and selects,
schedules, and prioritizes no remedy. Whether the warmth / role-cadence (D) frame is ever drafted **would
be eligible only as a further separately authorized step**.

---

## 5. (reserved — no additional scope)

`[NON-AUTHORIZATION]` This frame intentionally adds no section beyond the question above. It does not open
the role-cadence row, the affect-sensitivity modifier as a row, the warmth row, or any chamber /
private-cognition surface.

---

## 6. What this frame does NOT do or authorize

`[NON-AUTHORIZATION]` This frame poses a question and stops. It does **not** answer it, and authorizes
none of, and must not be read as implying:

```
answer / resolution of the question · safe/unsafe verdict · defect verdict · canon claim · automatic promotion
remedy · writer patch · fix order · enforcement behavior · governance vehicle · runtime authority gate
source-membership proof design · source-sameness / P4 mechanics · diagnostic_only implementation · canon_source · update_payload / stored-edge repair · P4/Q-* lineage mechanics
re-answering the gravity frame's Q-C1–Q-C4 or the mood-topology frame's Q-M1–Q-M4
treating role inference as the in-scope row · treating affect-sensitivity as a separate row
code · tests · runtime · endpoint / service calls · ingest / query · workspace-frequency evidence
schema / store / carrier / serialization / migration · Seed-Governance mechanics · candidate store
governed admission implementation · seed rewrite · seed-revision API · chamber / private-cognition runtime
dream / incubation runtime · database / substrate · hidden chain-of-thought storage or exposure
hidden output blocker / finalizer · identity pinning · monitoring / autonomy layer · coercive mechanism
broadening to gravity / mood-topology / warmth / role-cadence / seed_text / H3 / H5 / H6
```

`[NON-AUTHORIZATION]` It amends no upstream contract (Document A / Document B / Seed-Governance / P4 /
Stage A / Cluster 2 / Ledger / MCP boundary), opens no Stage B, opens no autonomy, **updates no
orientation map**, and makes **no registry amendment**.

---

## 7. Stopping evidence

`[STOP]` Stop, and do not extend this frame or draft any follow-up, if continuing would require: an
answer/resolution rather than a posed-and-open question; a safe/unsafe or defect verdict; a canon claim or
automatic promotion; any runtime / endpoint / service / ingest-query / database / workspace-frequency
evidence; a selected writer patch, fix order, enforcement behavior, governance vehicle, or runtime
authority gate; a source-membership proof design, source-sameness / P4 mechanic, `diagnostic_only`
implementation, `canon_source`, or any `update_payload` / stored-edge / P4-Q lineage repair; a schema /
carrier / migration / database decision;
any chamber / candidate-store / seed-revision / governed-admission construction; a hidden output blocker /
finalizer; a hidden chain-of-thought store; identity pinning; a monitoring / autonomy / coercive layer;
re-answering Q-C1–Q-C4 or Q-M1–Q-M4; or treating role inference or affect-sensitivity as in-scope rows.
Also stop if the operator-supplied docs-only state is not `f1500f6` (or its named Gate-4-lane
predecessors), or if the question can no longer be posed read-only and non-substrate.

---

## 8. Next step after this frame

`[NON-AUTHORIZATION]` This frame is a **draft for Codex challenge (Codex as first reviewer) and operator
review**. **No automatic successor gate. No automatic implementation.** Any later step — including whether
the warmth / role-cadence (D) frame is ever drafted, or whether any owner ever resolves Q-D1–Q-D5 —
requires **separate authorization** and an explicitly-opened step; nothing here selects or schedules it.
The orientation map is **not** updated by this artifact; any pointer is a separate later step. Database /
substrate remains last.

---

## Anti-drift footer

DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — question framing only, no remedy; no implementation, no
tests, no runtime, no service, no endpoint, no ingest / query, no workspace-frequency evidence, no
answer/resolution, no safe/unsafe verdict, no defect verdict, no canon claim, no automatic promotion, no
writer-patch selection, no fix order, no enforcement, no governance vehicle, no runtime authority gate, no
source-membership proof design, no source-sameness / P4 mechanics, no `diagnostic_only` implementation, no
`canon_source`, no `update_payload` / stored-edge / P4-Q lineage repair, no schema / store / carrier /
serialization / migration, no
Seed-Governance mechanics, no candidate store, no governed admission implementation, no seed rewrite, no
seed-revision API, no chamber / private-cognition runtime, no dream / incubation runtime, no database /
substrate, no hidden chain-of-thought storage or exposure, no hidden output blocker / finalizer, no
identity pinning, no monitoring / autonomy / coercive layer, no orientation-map update, no registry
amendment. Scoped to one row — `torment_service/fabric.py::_maybe_emit_identity_anchor` — an **automatic
derived identity-family writer**, `canon=False`, provenance `anchor_origin="derived"` /
`anchor_source="motif_cluster"`, reached under motif-recurrence count/gap gates, which **may retire a
prior anchor for the same motif (write-surface behavior only)** and is **cognition-reenterable through
ordinary tiering only** — **not** privileged canon-anchor or full-continuity-boost treatment; **as scoped
here derived anchors are `canon=False` and ordinary-tier / ordinary-reentry only, and any separate
promotion question is outside this frame, unopened, and unevaluated**. Posed as a **not-yet-conformant /
requires reconciliation** writer whose
**conformance posture is not yet resolved**; Q-D1–Q-D5 are **posed only, not answered**. Role inference /
cadence and affect-sensitivity tightening are **carried context, not scope** (role-cadence is the separate
row D). **P4 is named only as a reader-side requirement** (source-membership sameness or `diagnostic_only`
posture). This frame **does not re-answer** the gravity Q-C1–Q-C4 or mood-topology Q-M1–Q-M4, and **does
not duplicate, re-open, or re-pick** the matched P2.5/P4 reconciliation / registry §N4 routing. The
A→B→C→D order is a **framing sequence, not a fix order**. conformance characterization ≠ enforcement;
audit visibility ≠ authority; inspection ≠ projection; not canon does not mean behaviorally inert. Audit
observes authority and does not become authority. Memory may shape context. Memory may not seize
authority. Database / substrate remains last.
