# TORMENT — Document A Admission-Crossing Design-Framing Scope Frame v0.1

**Status:** DOCS-ONLY design-framing scope frame. **Non-authorizing, non-answering.** This is the **first
design-framing slice** after the operator selected Option S (`docs/TORMENT_DOCUMENT_A_ADMISSION_CROSSING_TARGET_SELECTION_DECISION_RECORD_v0.1.md`).
It **scopes** the design-framing of the selected target — the Document A governed admission crossing +
candidate carrier — by inventorying settled inputs, enumerating the open design questions **as questions**,
proposing a decomposition, and naming a **recommended first sub-question**. It **answers no design
question, selects no carrier, and names no mechanism.** Navigation / agenda aid only.

**Authority note:** Document A (containment wall / admission edge), the Gate A governed-admission
authority-option selection (`operator-only` floor), the candidate-representation selection + pre-carrier
constraints, the P4 Reader/Projection-Safety Contract, the Gate B writer-authority doctrine, Document B,
the Ledger Observational-Boundary, the substrate-readiness memo / Stage-B opening decision record, and
`PROJECT_ORIENTATION_MAP.md` §0 remain source of truth. This frame reads them; it amends none.

**Doctrine (carried, exact):**

> Memory may guide context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.
> Automatic remains allowed only where separately ratified. Autonomous remains unopened.

---

## 1. Status / non-authorizing banner

Scope + question-decomposition only. Enumerating an open design question here neither answers it nor opens
it; naming a *recommended first* sub-question neither selects it nor authorizes work on it — that remains a
later, separately-authorized decision. No admission crossing / carrier / store / schema / field / enum / ID
/ mechanism / workflow / actor / API / UI / runtime / persistence is created, selected, or implied. The
full non-authorization list is §6.

## 2. Inherited and settled inputs (carried, NOT re-decided or re-opened)

- **Selection:** Option S is selected — the crossing + carrier is the **next design-framing target only**
  (decision record). This frame is that framing's *scope*, not its content.
- **Admission authority floor = `operator-only`** — the requirement-level FLOOR for any future governed
  admission; **unchanged and out of scope** for this framing.
- **Candidate representation is selected; pre-carrier constraints are framed** — the inert candidate
  representation and its pre-carrier constraints exist; the durable **carrier itself remains unselected**.
- **Substrate = §K-eligible, NOT authorized; Stage B = framing-only** — no database / schema / storage /
  carrier / migration mechanics are open.
- **P4 O1/O2 have no selected carrier or comparison mechanism**; the live joins are presence-only, and
  `MemoryGraph.update_payload` is canonical-last reappend (a moving target behind an `eid`) — the
  characterized reason a carrier is the shared hard dependency.
- **HOLD posture preserved** — Gate A producer-independent wall = HOLD; P4 mechanics = unopened; Gate B
  writer-authority micro-work = HOLD; Dream / Regime-B = HOLD / blocker-dependent.

## 3. Open design questions (enumerated — UNANSWERED)

The crossing+carrier design-framing will eventually have to address the following. **None is answered
here; each is stated as a question only.**

- **Q1 — Carrier properties (requirement level).** *What requirement-level properties must any future
  candidate carrier satisfy* so the governed admission crossing can later support P4 O1/O2
  reader-trace / source-sameness, Gate B durable writer resolution, and Dream staging→crossing — **without**
  naming or selecting a carrier / store / schema / field / enum / ID? (Recommended first — §5.)
- **Q2 — Crossing record (requirement level).** *What must a governed admission crossing record or produce*,
  at requirement level, to be operator-auditable, inspectable, and contestable — without designing a
  workflow, decision-procedure, actor, API, or UI?
- **Q3 — Floor binding.** *How does the crossing bind to the already-decided `operator-only` floor* as a
  necessary condition, without re-opening or weakening that floor?
- **Q4 — P4 relation.** *What must the carrier + crossing expose (requirement level) so a later P4
  source-sameness reader-trace can bind to a source across `update_payload` canonical-last reappend and
  `eid` reuse* — without opening P4 `ReaderPolicy` / source-sameness / `diagnostic_only` mechanics?
- **Q5 — Gate B relation.** *How does the crossing relate to Gate B durable writer resolution* (the write
  side) — without opening a writer crossing or Gate B fix?
- **Q6 — Dream relation.** *How does the crossing receive a Document-B staged candidate* (B stages, A
  crosses) — without opening Dream / Regime-B runtime or Document B chamber runtime?
- **Q7 — Substrate boundary.** *What of the above is carrier/substrate-dependent and must therefore route
  to Stage-B-framing / §K* rather than being framed here?

Each question is a *design-framing target for a future, separately-authorized slice*, not a task opened
now.

## 4. Proposed decomposition

A conservative, dependency-ordered decomposition (proposed; not authorized beyond the first item):

1. **Q1 carrier properties (requirement level)** — the shared hard dependency of Q4 (P4) and Q5 (Gate B);
   framing its *required properties* stays purely requirement-level and unblocks the most downstream
   reasoning. **Recommended first (§5).**
2. **Q2 crossing record** + **Q3 floor binding** — the crossing's own requirement shape, once carrier
   properties are framed.
3. **Q4 / Q5 / Q6 relations** — how the framed carrier+crossing meet P4, Gate B, and Dream.
4. **Q7 substrate boundary** — continually routes carrier/substrate mechanics out to Stage-B-framing / §K.

This ordering is a **proposal**; each step is a separate, separately-authorized docs-only slice, and the
operator selects each in turn. Naming the order opens none of them.

## 5. First recommended sub-question — carrier-properties question

**Recommended first sub-question (subject to later operator approval — not opened here):**

> **What requirement-level properties must any future candidate carrier satisfy so the Document A governed
> admission crossing can later support P4 O1/O2 reader-trace / source-sameness, Gate B durable writer
> resolution, and Dream staging→crossing — without opening carrier / store / schema / field / enum / ID
> design or selection?**

Why first: the carrier is the *shared hard dependency* the convergence frame identified (P4 O1/O2 and Gate
B durable resolution both block on it), and framing its **required properties** is expressible entirely at
requirement level — a "what must be true of any carrier" statement — with **no** carrier, field, schema, or
mechanism named or selected. It is the smallest requirement-level unit that unblocks the most downstream
questions. **This frame recommends it as the first target; it does not answer it and does not open it.**

## 6. Explicit non-authorizations

This frame answers none of §3's questions and opens none of the following: carrier / store / schema / field
/ enum / ID design or selection; admission mechanism / crossing / workflow / decision-procedure / actor /
API / UI; database / substrate construction or Stage-B mechanics; any change to the `operator-only`
authority floor; writer crossing or Gate B fix; P4 `ReaderPolicy` / source-sameness / `diagnostic_only`
mechanics; Dream / Regime-B runtime; scheduler / trigger / budget / autonomy; Gate D / Envelope-Audit /
Document B chamber runtime; AgentRunner / app / spine / MCP wiring; model / provider / API / prompt path;
memory writes / persistence / logging / transcripts; output-control / finalizer / refusal / identity /
canon behavior; dynamic-kernel / `conversation_shock`; any contract amendment. It edits no other doc and no
§0. Doctrine preserved verbatim.

## 7. Verdict

**SCOPE FRAMED / FIRST QUESTION NAMED — NO ANSWERS, NO MECHANICS OPENED.** The design-framing of the
selected target is scoped: settled inputs inventoried, the open design questions (Q1–Q7) enumerated as
questions, a dependency-ordered decomposition proposed, and the **carrier-properties question named as the
recommended first sub-question** (subject to later operator approval). No design question is answered, no
carrier or mechanism is selected, and every mechanism remains deferred to a future, separately-authorized
decision.

*End — Document A Admission-Crossing Design-Framing Scope Frame v0.1. Docs-only, non-authorizing,
non-answering. Verdict: SCOPE FRAMED / FIRST QUESTION NAMED — NO ANSWERS, NO MECHANICS OPENED.*
