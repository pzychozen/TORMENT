# TORMENT Brainvision BY Opponent-Axis Closure Audit Plan v0.9a

## 1. Status / non-claims

**DOCS-ONLY plan. Non-authorizing, non-implementing. Opens no code, no tests, no runtime, no integration
lane.** It turns the accepted v0.9 BY opponent-axis visibility proposal into a concrete *future*
reporting-only audit over the existing v0.7b / v0.8a records, so a systematic blue-yellow opponent-axis offset
(`BY_axis_asymmetry`) is made **explicitly visible** rather than compressed inside the group-level residual /
`TOL` match. It **authorizes no code and no tests**, invents no threshold, **redefines no `TOL`**, adopts **no
new closure metric**, proposes no pass/fail rule change, changes no formula / §7 anti-proxy logic / §8 verdict
logic, deletes or weakens no control, redesigns no descriptor, reopens no spectral group, expands no generator
family, and opens **no classifier (form B) and no neural encoder (form C)**. It authorizes **no flat-geometry
implementation and no screen-analysis implementation**. Everything stays offline under `research/brainvision/`
+ `tests/research/`, HELD per v0.6.

This note makes **no** vision claim, **no** "Brainvision sees" claim, **no** temporal-order claim, **no**
descriptor-validity claim, **no** memory-readiness claim, **no** runtime-readiness claim, and **no**
integration-readiness claim. It touches no `torment_service/`, runtime, camera / sensor / live-capture /
screen-capture / streaming, or prompt / context / memory / action / render-body / autonomy paths, and makes
**no real-clip / local-clip move** and **no memory-system integration**. A plan alone moves nothing: **no claim
lock and no verdict changes here.**

```text
first_pass_structure_validity_claim_allowed = False
temporal_claim_allowed                      = False
descriptor_validity_claim_allowed           = False
verdict                                      = HOLD
```

**No `§0` pointer; no tags.**

## 2. Relation to v0.8a / v0.8b / v0.9

```text
v0.8a (8977248)  BY-channel metric anatomy -> BY_axis_asymmetry (systematic, consistently-signed opponent-axis
                 offset; BY dominates RG/directional; BY often binds the L-inf; coupling/amplitude ruled down).
v0.8b (ea20804)  synthesis: sharpened the wall to a systematic opponent-axis offset; flat geometry only a future
                 framing; recommended the closure proposal.
v0.9  (6485e55)  BY opponent-axis closure PROPOSAL: treat BY_axis_asymmetry as a closure-layer VISIBILITY /
                 aggregation insufficiency; proposed candidate requirements A-F, adopting none.
v0.9a (this doc) turns those requirements into a concrete future reporting-only audit PLAN over existing records.
```

This plan is reporting-only and requirements-driven. It does not prove Brainvision, validate the descriptor,
invalidate v0.7b / v0.8a, adopt a metric, or select flat / screen geometry. It changes nothing v0.4b/v0.4c/v0.7a
froze or v0.7b/v0.8a produced.

## 3. Audit question

```text
How should a future audit make systematic BY_axis_asymmetry EXPLICITLY VISIBLE, without adopting a new metric,
changing TOL, changing pass/fail rules, or claiming descriptor validity?
```

## 4. Records to inspect

```text
- the existing v0.7b larger-N replication records (the 19 matched / 5 unmatched winder->canceller pairs).
- the existing v0.8a BY-channel anatomy records (signed offsets, sign consistency, BY-vs-RG effects, binding-stat
  distribution, region concentration, coupling / amplitude comparisons).
- NO rerun or replacement of the v0.7b samples; NO new seeds; NO new families; NO new candidate generation.
```

The future v0.9b audit reproduces the v0.7b matching **by identity** (same sealed enumeration, deterministic, as
v0.8a already does) and/or reuses the v0.8a outputs; it introduces no new data.

## 5. Required BY visibility panels

Each panel is a **reporting requirement only** — not a new closure metric, not a pass/fail gate; it does not
redefine `TOL` and does not move claim locks:

```text
A. Signed-offset panel
   Report by_centroid / by_spread / by_std SIGNED offsets and sign consistency across the matched pairs
   (sign and across-pair consistency, not just |residual|).
B. BY-vs-RG dominance panel
   Compare BY effect magnitude against the RG and directional comparison features (so BY dominance is explicit).
C. Binding-stat panel
   Report how often BY stats are the L-inf binding feature, broken out separately for by_std / by_spread / by_centroid.
D. Region / family panel
   Report whether the BY asymmetry concentrates in target regions (speed / phase / radius) or candidate families,
   while ACKNOWLEDGING that the v0.8a matched candidates were all segment_paired_canceller (single matching family).
E. Coupling / leakage separation panel
   Distinguish BY_axis_asymmetry from centroid/spread coupling and from amplitude / channel-energy leakage.
F. Residual-aggregation warning panel
   Flag cases where group-level residual / TOL closure COEXISTS with a systematic BY signed ordering.
```

## 6. Audit outputs

```text
- signed-offset summaries (per BY feature: signed offset + sign consistency)
- sign-consistency summaries
- BY / RG / directional effect comparison
- binding-stat distribution (with by_std / by_spread / by_centroid separated)
- region / family concentration summary (with the single-matching-family caveat surfaced)
- coupling / leakage comparison
- residual-aggregation warning summary
- protocol_ok summary
- claim-lock summary (locks False; verdict HOLD)
```

All outputs are reporting-only. No pass/fail upgrade; no decision-score / classifier-score / label-accuracy /
held-out-performance / cheap-baseline-BA optimization.

## 7. Forbidden interpretations

```text
- Do NOT say this proves Brainvision or validates the descriptor.
- Do NOT say this proves screen analysis or that flat geometry is selected.
- Do NOT say a new closure metric is adopted or that TOL is obsolete.
- Do NOT say v0.7b / v0.8a are invalid.
- Do NOT move claim locks; do NOT authorize runtime / memory / integration.
- Do NOT invent a threshold, redefine TOL, change any pass/fail rule, redesign the descriptor, expand a family,
  or reopen spectral as a closure group.
- The panels make the BY offset VISIBLE; they do not decide anything with it.
```

## 8. What would count as useful evidence

The evidence is **explicit visibility** of the systematic BY-axis offset in the audit layer — **not** a pass, a
new threshold, or a validity statement:

```text
- The panels surface, in one place, that the per-pair residual / TOL closure COEXISTS with a systematic,
  consistently-signed, BY-dominant, often-binding class-level BY offset (A + B + C + F) -- i.e. the compression
  the current closure hides is made explicit.
- The region / family panel (D) shows whether the offset is broad or localized, with the single-family caveat.
- The coupling / leakage panel (E) confirms coupling and amplitude stay weak, keeping BY_axis_asymmetry the best
  description.
```

Making the offset visible is a reporting-layer improvement; it upgrades no claim and moves no verdict (§9).

## 9. What would still not be proven

Even a future audit that makes the BY-axis offset fully visible would leave all of the following **unproven**:

```text
not vision                     not "Brainvision sees"
not descriptor validity        not temporal order
not real-video understanding   not a unique real-world color-structure advantage
not memory readiness           not runtime readiness           not integration readiness
```

Making a synthetic BY-axis offset visible in the audit layer is an in-vitro, metric-level reporting improvement
within the same family set; it says nothing about real clips or screens and does not validate the descriptor.
The proof route remains **HELD / HOLD**. The claim locks (`first_pass_structure_validity_claim_allowed`,
`temporal_claim_allowed`, `descriptor_validity_claim_allowed`) and `verdict = HOLD` remain in force under every
outcome.

## 10. Candidate outcomes

```text
BY_visibility_confirmed     panels A-F populate and make the systematic BY offset explicitly visible (signed,
                            BY-dominant, often-binding, coexisting with TOL closure).
BY_visibility_partial       some panels populate while others are limited (e.g. the region/family panel is
                            constrained by the single matching family); the offset is partially surfaced.
BY_visibility_inconclusive  the existing records cannot make the offset visible via these panels; no claim movement.
invalid_protocol_breach     rerun / sample replacement / non-finite in a required panel / non-reproduction of the
                            v0.7b matching -> no evidential weight, moves no claim.
```

For **every** outcome: claim locks stay False and `verdict = HOLD`; no vision / descriptor-validity /
temporal-order / memory / runtime / integration claim follows.

## 11. Recommended next step

```text
1. Codex review THIS plan (docs-only; over committed edge 6485e55).
2. If accepted, commit this plan doc. No §0 pointer; no tags.
3. Only THEN, and only on explicit operator instruction, may a SEPARATE future v0.9b code audit implement EXACTLY
   these reporting-only panels over the reused v0.7b / v0.8a records -- form A, non-learning, frozen surfaces
   reused by identity, inventing no threshold, redefining no TOL, adopting no metric, redesigning no descriptor,
   expanding no family, delivered UNCOMMITTED for review, Windows the source of truth. This plan authorizes no
   such code by itself.
4. Otherwise the Brainvision prototype line stays HELD. No code, classifier (B), neural (C), runtime, memory,
   real-clip, screen, flat-geometry, §0, or tag work is recommended or authorized here.
```

Claim locks and verdict are unchanged: `first_pass_structure_validity_claim_allowed = False`,
`temporal_claim_allowed = False`, `descriptor_validity_claim_allowed = False`, `verdict = HOLD`.

## 12. Codex review prompt

```text
Please review docs/TORMENT_BRAINVISION_BY_OPPONENT_AXIS_CLOSURE_AUDIT_PLAN_v0.9a.md
(new, docs-only, untracked; over committed edge 6485e55, turning the v0.9 proposal into a concrete audit plan).

Verify that this plan:
- is docs-only and authorizes no implementation (no code/tests, no torment_service/, no runtime, no memory, no
  camera/live/sensor/screen/streaming, no real clips); keeps form B (classifier) and form C (neural) CLOSED; and
  authorizes NO flat-geometry and NO screen-analysis implementation;
- plans a REPORTING-ONLY audit that makes systematic BY_axis_asymmetry explicitly visible via panels A-F
  (signed-offset, BY-vs-RG dominance, binding-stat, region/family, coupling/leakage separation, residual-
  aggregation warning) -- panels are reporting requirements, NOT a metric / pass-fail gate / TOL change;
- inspects ONLY the existing v0.7b / v0.8a records (reproduces the v0.7b matching by identity), with NO rerun /
  sample replacement / new seeds / new families / new candidate generation;
- adopts NO new closure metric, invents NO threshold, redefines NO TOL, changes NO pass/fail rule, redesigns NO
  descriptor, expands NO family, reopens NO spectral group;
- limits outputs to the reporting summaries listed and lists candidate outcomes BY_visibility_confirmed /
  BY_visibility_partial / BY_visibility_inconclusive / invalid_protocol_breach, all leaving claim locks and verdict
  unchanged;
- does NOT say this proves Brainvision / validates the descriptor / proves screen analysis / selects flat geometry
  / adopts a metric / makes TOL obsolete / invalidates v0.7b / v0.8a; does NOT move claim locks or authorize
  runtime/memory/integration;
- preserves all claim locks (first_pass_structure_validity_claim_allowed = False; temporal_claim_allowed = False;
  descriptor_validity_claim_allowed = False) and verdict = HOLD; adds no §0 pointer and no tags.

Flag any adopted metric / threshold / pass-fail change, any TOL redefinition, any descriptor redesign, any rerun
or sample replacement, any new family, any flat-geometry / screen-analysis authorization, any descriptor-validity
/ vision claim, or any claim-lock/verdict movement.
Return ACCEPT AS-IS or MODIFY with exact required changes.
```

Brainvision remains **offline / quarantined**, HELD per v0.6.
`first_pass_structure_validity_claim_allowed = False`, `temporal_claim_allowed = False`,
`descriptor_validity_claim_allowed = False`, and the frozen verdict **HOLD** are unchanged.
**No `§0` pointer; no tags.**

*End — TORMENT Brainvision BY Opponent-Axis Closure Audit Plan v0.9a. Docs-only, non-authorizing. Opens no
implementation lane; opens no classifier / neural / screen / flat-geometry work; changes no frozen formula,
gate, evaluator, or verdict; deletes or weakens no control; redesigns no descriptor; invents no threshold;
redefines no TOL; adopts no closure metric; reruns / replaces no v0.7b sample; makes no vision /
descriptor-validity / temporal-order / memory / runtime / integration claim; no `§0` pointer added; no tags.*
