# TORMENT Brainvision Algebraic N=64 PRIMARY_V0_1 Frozen-Family F3 Read-Only Asymmetry Audit Findings v0.1

## 0. Disposition

```text
A. RECORD THE COMPLETED VALID ONE-RUN READ-ONLY FROZEN-FAMILY ASYMMETRY AUDIT
```

This document records the completed, valid, one-run read-only asymmetry audit over the retained canonical algebraic N=64 `PRIMARY_V0_1` frozen-family F3 evaluation result, and binds its exact canonical artifact identities.

This document is:

```text
docs-only
post-execution
descriptive
read-only
non-gating
non-rescue
offline
quarantined
non-production
```

The authoritative F3 verdict remains permanently:

```text
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

These audit findings must not be represented as:

```text
a rescue of the frozen family
a replacement F3 verdict
proof of vision
proof of perception
proof of temporal-order perception
proof of recursive-time mechanism
scientific significance
physics emergence
production readiness
generalization beyond the exact frozen family
```

The audit is descriptive only. It does not amend, weaken, reinterpret, reconsider, replace, or rescue the authoritative F3 verdict.

## 1. Execution bindings

```text
audit execution commit =
b392e801017ff4ab99d455acc7e2af1f76cbe131

implementation commit =
8325bff376a646d1ca2c54dd6aa7f14eb84a9282

source findings commit =
f0c5fdbc6f75e3749323de9e2b13c77b3710df82

audit specification commit =
db82ec06faf398d52a002ba56f976e91045d12a4

audit implementation-authorization commit =
75ba05485ae3d1dafcbe64d8330920459d54fa7e
```

The audit execution commit is the synchronized docs-only execution-authorization commit under which the committed analyzer ran, dynamically recorded in the canonical result as `audit_execution_commit_identity`.

## 2. Frozen analyzer identities

```text
analyzer path =
research/brainvision/analyze_algebraic_n64_f3_asymmetry_v0_1.py

analyzer Git blob =
8f093f01535d2bcb04c88a87be426a78e795752a

analyzer raw SHA-256 =
59006c6b6089db68da4e75ff4aab86755870df40c3a77bf6c28402971f41e395
```

The canonical result independently records these analyzer identities as `analyzer_git_blob_sha` and `analyzer_raw_file_sha256`.

## 3. Retained F3 input bindings

```text
retained result path =
research/brainvision/results/algebraic_n64_primary_v0_1_f3_evaluation_v0_1/
algebraic_n64_primary_v0_1_f3_evaluation_result.json

retained result size =
10,784,993 bytes

retained result whole-file SHA-256 =
51e7cd8087050428c2559262764044624fcb84e19576b5f682bae3ca5b59fd7b

retained result payload SHA-256 =
f996869ffa58d2ac6e6cdcc2ffc8bbd5769f1e5568ac71c6f63faadd4a5a4125

historical F3 execution commit =
c4f489c439d4190611e8e0c5b3034ead3353c26d

accepted candidate order =
[478, 479, 480]

retained summary SHA-256 =
43a19f5ca7d1e22491bfae209603b4f595b17c886a9d4320346ef2aa4a129daf
```

Post-audit hashing showed both retained files remained unchanged:

```text
retained result whole-file SHA-256 =
51e7cd8087050428c2559262764044624fcb84e19576b5f682bae3ca5b59fd7b

retained summary whole-file SHA-256 =
43a19f5ca7d1e22491bfae209603b4f595b17c886a9d4320346ef2aa4a129daf
```

The audit opened the retained canonical JSON read-only. No retained-evidence bytes were edited, reserialized, renamed, moved, deleted, or replaced.

## 4. Published audit artifacts

```text
final directory =
research/brainvision/results/
algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1/

result filename =
algebraic_n64_primary_v0_1_f3_asymmetry_audit_result.json

result size =
377,106 bytes

result whole-file SHA-256 =
bc3163115b47f974b9ad3b947480dd8f3080783caab68445c8c3f5722fefd581

summary filename =
algebraic_n64_primary_v0_1_f3_asymmetry_audit_summary.txt

summary size =
1,604 bytes

summary whole-file SHA-256 =
0f2c25828bed167c9db77ca53e95fb346b794eef7a13d212974dab2f05f0490c

canonical asymmetry-audit payload SHA-256 =
a0242bb379313d9ef64b07f7dda2aa305fc9fa41f68f2e27b23586a5775d2814
```

Publication state:

```text
exit code = 0
exact two-file final artifact set present
staging directory absent after successful atomic publication
working tree remained clean
gate cleared after execution
no second invocation occurred
```

The canonical JSON is the authoritative audit evidence. The summary is operator convenience only. The published audit artifacts are derived evidence and are not a replacement for the retained F3 evidence.

## 5. Exact retained machine findings

```text
audit_valid =
True

ordered_failure_codes =
[]

pair_order =
[478, 479, 480]
```

```text
candidate 478:
pair_order_index = 0
blocking_class_count = 27
classification = broad
```

```text
candidate 479:
pair_order_index = 1
blocking_class_count = 12
classification = broad
```

```text
candidate 480:
pair_order_index = 2
blocking_class_count = 17
classification = broad
```

```text
family_disposition =
B. BLOCKING SELF-ORBIT ELEVATION IS BROAD
```

```text
authoritative_f3_verdict_preserved =
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

## 6. Bounded interpretation

The audit found that the blocking self-orbit elevation was not confined to one or two isolated canonical shift classes.

Preregistered broad classification threshold:

```text
broad classification threshold =
8 or more blocking classes out of 32 canonical classes
```

Observed counts:

```text
478 = 27
479 = 12
480 = 17
```

All three retained pairs exceeded the preregistered broad threshold. Therefore the exact frozen-family descriptive disposition is:

```text
B. BLOCKING SELF-ORBIT ELEVATION IS BROAD
```

This means only:

```text
under the exact retained F3 evidence
under the exact canonical q=1..32 inverse-shift class convention
under the exact committed analyzer
all three frozen pairs showed blocking elevation across a broad set of canonical shift classes
```

It does not establish a cause, mechanism, statistical significance, or generalization.

It does not determine that every shift class blocks.

It does not imply that all pairs have the same shape or degree of elevation.

It does not weaken or amend the F3 verdict.

## 7. Authority closure

```text
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZED = consumed
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_BEGUN = True
READ_ONLY_ASYMMETRY_AUDIT_COMPLETED = True
READ_ONLY_ASYMMETRY_AUDIT_VALID = True
READ_ONLY_ASYMMETRY_AUDIT_RERUN_AUTHORIZED = False

F3_EVALUATION_COMPLETE = True
F3_RERUN_AUTHORIZED = False

PSITRS_CONTACT_AUTHORIZED = False
DESCRIPTOR_RECOMPUTATION_AUTHORIZED = False
RETAINED_EVIDENCE_MODIFICATION_AUTHORIZED = False
SCIENTIFIC_INFERENCE_AUTHORIZED = False
PRODUCTION_INTEGRATION_AUTHORIZED = False
KERNEL_MODIFICATION_AUTHORIZED = False

FORMAL_HOLD = active
Mode_0 = active
```

The one-run accepted audit authority is consumed. No rerun is authorized. Any future audit of this frozen family requires a new docs-only authorization.

## 8. Production boundary

The immutable boundary is preserved around:

```text
torment_service/kernel/
torment_service/memory_kernel.py
torment_service/fabric.py
```

No production memory, prompt construction, model context, actions, tools, live models, network services, capture devices, sensors, or service integration were contacted. The production TORMENT memory kernel remains immutable. Brainvision remains offline, quarantined, non-runtime, non-production, non-live, and a non-scientific-claim lane.

## 9. Final disposition

```text
A. VALID ONE-RUN READ-ONLY ASYMMETRY AUDIT RECORDED
A. FAMILY DISPOSITION = B. BLOCKING SELF-ORBIT ELEVATION IS BROAD
A. AUTHORITATIVE F3 VERDICT PRESERVED
```

The authoritative result remains:

```text
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

No analyzer execution, gate use, retained-evidence modification, published-artifact modification, F3 rerun, PsiTRS contact, descriptor recomputation, or production/kernel contact occurred while preparing these findings.

*End — TORMENT Brainvision Algebraic N=64 PRIMARY_V0_1 Frozen-Family F3 Read-Only Asymmetry Audit Findings v0.1. Docs-only, post-execution, descriptive, read-only, non-gating, non-rescue. Records one completed valid one-run asymmetry audit with family disposition B (blocking self-orbit elevation is broad across all three frozen pairs) under the exact retained F3 evidence and committed analyzer. The audit is incapable of amending the authoritative frozen-family F3 verdict, which remains STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY.*
