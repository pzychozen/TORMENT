# TORMENT Gate 4 — Read-Only Conformance-Gap Map v0.1

**DOCS-ONLY READ-ONLY GAP CHARACTERIZATION — NO IMPLEMENTATION, NO TESTS, NO RUNTIME, NO REMEDY, NO
VERDICT.**

This artifact is the **read-only gap characterization** named as the only eligible successor in the
Gate 4 selection frame (§7). For each named old path it records, against already-promoted boundaries,
whether a **conformance posture is yet recorded** — and where it is not, it states the path as
**not-yet-conformant / requires reconciliation**. It makes **no** safe/unsafe verdict, **no** defect
verdict, **no** remedy, **no** fix order, **no** writer-patch selection, **no** enforcement demand,
**no** governance-vehicle selection, and **no** runtime-authority-gate claim. It selects nothing and
builds nothing.

**Operator-supplied docs-only state:** `f6d0fb3` (*docs(gate-4): point orientation map to selection
frame*). The Gate 4 selection frame's older `a92dbef` stop precondition is treated as superseded **only**
by the two operator-named docs-only commits `dd53b7e` (selection frame) and `f6d0fb3` (orientation-map
pointer). No `git` was run; `f6d0fb3` is taken as the current operator-supplied docs-only state on
operator authority. **Date:** 2026-06-19. Windows-visible tracked repo state remains authoritative.

> Memory may shape context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.

**Tags:** `[FACT]` point-in-time, carried from prior read-only artifacts · `[BOUNDARY]` already-promoted
requirement obligation · `[POSTURE]` conformance posture · `[GAP]` not-yet-conformant / requires
reconciliation · `[DISTINCTION]` · `[LATER OWNER]` named, no remedy · `[NON-AUTHORIZATION]` · `[STOP]`.

---

## 0. Provenance, precondition, and review status

`[FACT]` **Lineage.** Heavy-gate selection packet → Gate 4 selected first at read-only
runtime-conformance selection level (`TORMENT_GATE_4_READ_ONLY_RUNTIME_CONFORMANCE_SELECTION_FRAME_v0.1`,
Codex ACCEPT WITH CORRECTIONS) → orientation-map pointer → **this read-only conformance-gap map.** This
is the separately authorized successor the selection frame §7 names; it is the *only* eligible kind of
successor (a read-only map), and it opens no further gate.

`[FACT]` **Review status: Codex ACCEPT WITH CORRECTIONS — corrections applied; pending operator review /
commit.** The selection frame §7 records that this map must pass **Codex challenge (Codex as first
reviewer)** before it is treated as load-bearing; Codex returned **ACCEPT WITH CORRECTIONS** and those
corrections were **applied before commit**. The artifact remains **docs-only and non-authorizing**;
**operator review / commit is the promotion action**. It changes no behavior, and **nothing downstream
may treat it as authorizing implementation, remedy, writer patch, enforcement, substrate, or any
successor gate** — promotion records only that this read-only characterization is filed, not that
anything is authorized to follow.

`[FACT]` **Precondition honored.** This map is expressible **read-only and non-substrate** (§1.3); no
forbidden item (§6) was required to write it. Per the selection frame §6 / §7 `[STOP]` conditions, had
the map required runtime/endpoint/service/ingest/query/database/workspace-frequency evidence, a
verdict, a remedy, a writer selection, an enforcement path, a governance vehicle, an authority gate, or
any substrate decision, drafting would have stopped instead. It did not.

---

## 1. Status, scope, method

### 1.1 Status

`[FACT]` This is a **docs-only read-only gap characterization**. It is **no implementation, no tests, no
runtime, no service, no endpoint, no ingest/query, no instrumentation, no remedy, no enforcement, no
database/substrate.** It introduces no new runtime facts: every `[FACT]` below is point-in-time and
**carried from prior read-only artifacts** (the influence traces, the Gate B hazard inventory, Document
A's traced write-side map, Seed-Governance's survey, and the pre-substrate framing) plus read-only
source-surface naming. No new tracing, execution, or measurement was performed for this map.

### 1.2 Preserved controlled distinctions (load-bearing, carried)

`[DISTINCTION]`

```
audit visibility ≠ authority
inspection ≠ projection
conformance characterization ≠ enforcement
old identity-pressure paths ≠ chamber / private-cognition design
staging ≠ governed admission
seed revision ≠ ordinary memory admission
identity-relevant ≠ identity-authoritative
recognition ≠ authority
not canon does not mean harmless; not governance authority does not mean behaviorally inert
```

### 1.3 What this map does and does not assert

`[POSTURE]` **"Conformance posture not yet recorded"** means: no prior artifact has recorded a finished
proof that the path conforms to the named boundary obligation. It is **not** a finding of
non-conformance, **not** a defect, and **not** a safety claim.

`[GAP]` **"Not-yet-conformant / requires reconciliation"** is a **governance-conformance posture**: the
path is an automatic or durable-soft influence whose conformance to an already-promoted boundary has not
been ratified, so a later separately authorized reconciliation owns the question. It carries **no**
safe/unsafe verdict, **no** magnitude claim, and **no** ordering. Per Seed-Governance SG-O5, a
not-yet-conformant flag is **not** a must-patch order.

`[NON-AUTHORIZATION]` **Non-substrate.** Stating any posture below requires **no** schema, store,
carrier, serialization, migration, `canon_source`, source-sameness mechanic, or database decision. Where
a boundary obligation is the P4 reader-side requirement, it is **named as a requirement only**; no P4
mechanic is selected, designed, or implemented here.

### 1.4 Reading key

Each core row records, read-only: **(a)** already-characterized point-in-time behavior (carried, with
the source surface and prior artifact named); **(b)** the already-promoted boundary obligation(s) the
path is read against; **(c)** the conformance posture (recorded vs not yet recorded); **(d)** the gap
statement; **(e)** the named later owner (no remedy, no order). **Row order is not a fix order, a
priority, or a severity ranking** (§5).

---

## 2. Core rows

### 2.1 `gravity_correction`

`[FACT]` Point-in-time behavior (carried from the pre-substrate framing Family C, Gate B hazard
inventory H1, Document A §11 traced map, Seed-Governance §8/§9): `character.py::gravity_correction`
**automatically** writes a `drift_correction` memory with `canon=True` at the core-identity tier,
attached to the seed motif, reached through the periodic post-store drift check inside
`TormentFabric.ingest()` and gated by its current drift-check conditions (character enabled; a memory
stored this call; positive step; step divisible by the drift interval; seed and seed-motif resolvable;
high-away drift). Purely additive (it rewrites/deletes nothing).

`[BOUNDARY]` Read against: Document A **A-O2** (no automatic canon / identity-tier material produced
directly without a governed crossing) and **A-O5** (existing automatic writers are named unreconciled
seams, not exemptions); Seed-Governance **SG-O5 / SG-O6** (automatic identity/seed/canon writers require
later governed reconciliation; soft/derived signals must not silently become canon authority);
pre-substrate Family C (automatic-canon vs "canon never automatic").

`[POSTURE]` Conformance posture **not yet recorded** — it is an automatic `canon=True` writer and no
prior artifact records a ratified conformance proof against A-O2 / SG-O5.

`[GAP]` **Not-yet-conformant / requires reconciliation** as an automatic canon identity-reinforcement
writer. No magnitude, frequency, or safety claim is made; the randomized-concept, not-rederivable
property is carried as prior fact only.

`[LATER OWNER]` Gravity-correction audit-first reconciliation slice (Document A §11) + Seed-Governance
§9/§12 soft-state/O6 seam. Named only; no remedy, no patch, no order.

### 2.2 Derived identity anchors (`_maybe_emit_identity_anchor`)

`[FACT]` Point-in-time behavior (carried from the role-inference cadence trace, pre-substrate Family D,
Gate B H2): `fabric.py::_maybe_emit_identity_anchor` **automatically** emits a derived
`identity_anchor` memory with `canon=False`, provenance `anchor_origin="derived"` /
`anchor_source="motif_cluster"`, reached only under motif-recurrence count/gap gates (role-modulated
cadence; affect-sensitivity tightening), and may retire a prior anchor for the same motif. It is
**cognition-reenterable today**.

`[BOUNDARY]` Read against: Document A **A-O1** (class-bound writer authority; authorization not inferred
from payload flags or source presence) and the **P4 reader-side requirement** (a cognition-reenterable
derived identity row needs source-membership proof or the `diagnostic_only` posture — named as a
requirement only, no P4 mechanic selected); pre-substrate Family D ("reenterable-today →
proof-or-`diagnostic_only`").

`[POSTURE]` Conformance posture **not yet recorded** — it is an automatic identity-family writer reached
under its gates, and no prior artifact records a ratified writer-authority or reader-side conformance
proof.

`[GAP]` **Not-yet-conformant / requires reconciliation** on two already-named axes: write-side
class-bound authority (A-O1) and reader-side reenterability (P4 requirement). `[DISTINCTION]` Ordinary
non-canon derived anchors are **identity-relevant, not identity-authoritative**, and are not a
Seed-Governance crossing by default (Seed-Gov §9).

`[LATER OWNER]` Writer-authority reconciliation + P4 O2 reader-side requirement (Document A §11 / Seed-Gov
§9). Named only; no remedy, no order.

### 2.3 `mood_drift` / drift-centroid / `gravity_correction` interaction — **topology only**

`[FACT]` **Topology / reachability only — no causal, magnitude, "usually-triggers," or decisiveness
claim** (carried from Gate B H4 and Seed-Governance §9). `fabric.py::_maybe_emit_mood_drift` emits a
`mood_drift` memory (`canon=False`) under its own affect/state/gap gates. `character.py::measure_drift`
excludes only `seed_canon` rows and applies a recency filter, so an eligible recent `mood_drift` row
with an embedding is **among the rows eligible to contribute** to the centroid that `measure_drift`
considers; that drift measurement is one of the gates of the `gravity_correction` writer (§2.1). This
records an **input-reachability relationship**, not a separate writer and not a claim that `mood_drift`
causes, moves, usually triggers, or is decisive for any gravity correction.

`[BOUNDARY]` Read against: Seed-Governance **SG-O6** (soft guidance must not silently become seed/canon
authority, directly **or through an automatic chain**); the compound pathway is the explicitly named
`mood_drift → drift centroid → gravity_correction → canon=True` hazard.

`[POSTURE]` Conformance posture **not yet recorded** for the compound pathway, evaluated only as a
soft-guidance-to-identity-canon **reachability** relationship.

`[GAP]` **Not-yet-conformant / requires reconciliation** as an automatic soft-guidance-to-identity-canon
**pathway** (topology level only). Not condemned, not quantified, not a verdict.

`[LATER OWNER]` Gravity-correction audit-first slice + Stage A O6 soft-state seam (Seed-Gov §9/§12).
Named only; no remedy, no order.

### 2.4 Spirit-return warmth / warmup

`[FACT]` Point-in-time behavior (carried from the spirit-return warmth trace, pre-substrate Family F):
warmth is durable-soft, **non-canon**, retrieval/prompt-shaping state tracked per deep-memory EID and
persisted in `warmup_state.jsonl` (append-only with periodic compaction; path-integrity hardened).
Warmth scales spirit-return hit strength via a per-mode multiplier; `return_mode` + warmth thresholds
drive block classification, **including possible identity-block placement** for resonance / high-warmth
hits — characterized as **retrieval-side shaping, not an authority crossing** (it writes no canon and no
graph memory).

`[BOUNDARY]` Read against: Stage A **O6** (durable-soft must-not-pin) and Seed-Governance **SG-O6 /
SG-O8** (soft guidance must not silently become seed/canon authority; recovery preserves without
pinning). The surfaces a reconciliation would weigh are durable-soft persistence and warmth-driven
identity-block placement.

`[POSTURE]` Conformance posture **not yet recorded** against the must-not-pin / soft-guidance-not-
becoming-authority boundary. `[DISTINCTION]` "Durable" ≠ "pinned"; identity-**block placement** is a
prompt-assembly classification, not a canon or admission decision — no overstatement is made.

`[GAP]` **Not-yet-conformant / requires reconciliation** at the O6 soft-guidance seam (durable-soft must
not be pinned into rigid control). No safe/unsafe verdict; the prior characterization that warmth is
retrieval-side shaping, not an authority crossing, is carried unchanged.

`[LATER OWNER]` Stage A O6 soft-guidance seam (pre-substrate Family F; Seed-Gov §12). Named only; no
remedy, no order.

### 2.5 Role inference / identity-anchor cadence

`[FACT]` Point-in-time behavior (carried from the role-inference cadence trace, pre-substrate Family G):
`roles.py` holds a durable-soft, **non-canon**, deterministic/offline/model-free keyword-scored role
profile (per-agent `roles.json`, updated from ingest-summary text via a slow EMA; default explorer
bias). The dominant role maps to `anchor_count_mult` / `anchor_gap_mult`, which scale the base
identity-anchor cadence thresholds (with hard floors) inside `_maybe_emit_identity_anchor`. Its
memory/prompt effect is **indirect**, through derived identity-anchor cadence; it additionally has
diagnostic read-surface exposure (`GET /agent/{id}/roles`, `role_context`, the query-signature
`dominant_role`). Storage caveat carried: `RoleStore.save` is non-atomic (`IDENTITY-NON-ATOMIC-SAVE`
class).

`[BOUNDARY]` Read against: pre-substrate Family G (ungoverned soft writer modulating identity cadence)
and Stage A **O6** soft tier; the matched-writer reconciliation lane is named in the framing.
`[DISTINCTION]` Diagnostic read-surface exposure ≠ prompt authority; the dominant role is **not**
identity authority and **not** persona/voice writing.

`[POSTURE]` Conformance posture **not yet recorded** as an ungoverned soft writer that modulates
identity-anchor cadence.

`[GAP]` **Not-yet-conformant / requires reconciliation** at the soft-guidance (O6) tier as an
ungoverned soft modulator of identity cadence. Indirect-only; no canon, no admission, no verdict.

`[LATER OWNER]` Stage A O6 soft tier + the matched writer-side reconciliation lane (pre-substrate Family
G). Named only; no remedy, no order.

`[FACT]` §2 closes here with **exactly five core runtime rows** (§2.1–§2.5). Authored `seed_text` is
**not** a sixth core runtime row; it is recorded separately as boundary context in §2A below.

---

## 2A. Boundary context — authored `seed_text` (not a core runtime row)

`[DISTINCTION]` Authored `seed_text` is recorded as **boundary context only** — **not** one of the five
core runtime identity-pressure rows in §2, and not to be read structurally as a core runtime row. There
is **no automatic runtime seed-revision writer** to characterize, so there is **no Gate 4
runtime-conformance row** for it; only the requirement-level Seed-Governance §7 boundary stands.

`[FACT]` Point-in-time behavior (carried from the authored seed-content stability lock,
Seed-Governance §2/§7, pre-substrate Family A): authored `seed_text` is prompt-visible and
kernel-modulating **soft influence**. It is **write-once-by-absence** today — no runtime ingest / query
/ drift path reassigns it, and the plant-once gate prevents re-save — and that stability is now
additionally **test-locked** for authored content. The durable store *can* persist a changed seed dict
(`IdentityStore.save`), so write-once is a convention / absent-path, not a structural lock.

`[BOUNDARY]` Read against: Seed-Governance **§7** governed seed-revision boundary (operator-authorized,
lineage-preserving, auditable, contestable, reversible; **never** a side effect of ingest / reflection /
drift / automatic correction) and **SG-O6** (the *influence* is soft guidance). `[DISTINCTION]` **seed
revision ≠ ordinary memory admission**; seed *influence* ≠ seed *mutation*.

`[POSTURE]` Included as **boundary context**, not as an automatic identity-pressure writer: there is
**no automatic runtime seed-revision writer** to characterize today, so there is no automatic-writer
conformance gap to record here. The §7 governed-revision boundary is a **requirement-level** obligation
with no runtime revision path; authored-content stability is presently held by absence-plus-test-lock.

`[GAP]` No automatic-writer gap is asserted. The only standing item is the **requirement-level** §7
seed-revision boundary (governed, never automatic) — a separate later-owned construction question, **not**
a Gate 4 runtime-conformance row and explicitly out of scope for any remedy here.

`[LATER OWNER]` Seed-Governance §7 governed seed-revision boundary (requirement level; construction
separately authorized and substrate-coupled). Named only; no remedy, no order.

---

## 3. Adjacency notes (separate; not core rows)

`[FACT]` These are **not** core Gate 4 identity-pressure rows. They are recorded as adjacencies so they
are not conflated with the automatic identity-pressure paths above.

- **H3 — `POST /promote` force-route — writer-authority-adjacent, not an automatic identity-pressure
  path.** Endpoint-driven (`app.py::promote_chunk_endpoint` → `promotion.py::promote_chunk`); **not**
  ordinary-ingest reachable and **not** automatic. With `force=True` it can write a `canon=True`
  core-identity row while bypassing the endpoint's promotion decision guard (carried from Gate B H3 /
  Seed-Gov §9). It is **writer-authority-adjacent**, recorded here because it is caller-driven authority
  creation — **not** because it is an automatic identity-pressure path. `req.force` is a runtime bypass
  signal, not any authorization. Carried fact: a provenance slice has separately landed that makes a
  force-route promotion durably **distinguishable** from an evaluator-approved one (**provenance only,
  not control**); that does not record a conformance posture for the bypass, which remains a separate
  writer-authority subject and is **not** decided here. No remedy, no order.
- **H5 — AgentRunner Phase-8 gravity route — binding-contingent / parked.** A second potential door to
  the gravity writer **only** for `FabricHandle` implementations bound to `character.gravity_correction`;
  read-only evidence found **no production-service binding** (`fabric.py` defines no `gravity_correction`
  method; `AgentRunner` is constructed only in `examples/` and `tests/`). This **de-risks but does not
  prove** a live route. Carried with its caveat only; no posture recorded.
- **H6 — ordinary-ingest fan-out — eligibility / reachability only, not guaranteed fan-out.** Ordinary
  ingest is the fan-out root into motif / drift / mood / role / deep / SRG / retrieval; content entering
  via ordinary ingest is *eligible to be subject to* the gated writer hazards above. This is a
  reachability envelope, **not** a writer and **not** a claim of guaranteed fan-out. Carried with its
  caveat only; no posture recorded.

---

## 4. Consolidated posture (no ordering)

`[POSTURE]` Read-only consolidation of the **five core runtime rows** (§2.1–§2.5), ordered
**alphabetically by surface, not ranked**. Authored `seed_text` is **excluded** from this core table and
recorded separately below as boundary context:

| Core runtime path | Already-characterized nature (carried) | Conformance posture | Later owner (named; no remedy) |
|---|---|---|---|
| derived identity anchors | automatic, `canon=False`, identity-tier, reenterable | not yet recorded → not-yet-conformant / requires reconciliation | writer-authority + P4 reader-side requirement |
| `gravity_correction` | automatic, `canon=True`, core-identity | not yet recorded → not-yet-conformant / requires reconciliation | gravity audit-first slice + Seed-Gov O6 seam |
| `mood_drift`→centroid→gravity (topology) | reachability relationship, no causal/magnitude claim | not yet recorded → not-yet-conformant / requires reconciliation (pathway) | gravity audit-first slice + Stage A O6 seam |
| role inference / cadence | durable-soft, non-canon, indirect cadence modulation | not yet recorded → not-yet-conformant / requires reconciliation | Stage A O6 soft tier + writer-side lane |
| spirit-return warmth | durable-soft, non-canon, retrieval-shaping (poss. identity-block placement) | not yet recorded → not-yet-conformant / requires reconciliation | Stage A O6 soft-guidance seam |

`[POSTURE]` **Boundary-context note (not a core runtime row).** Authored `seed_text` is listed
separately because it has **no automatic runtime writer**, hence **no Gate 4 runtime-conformance row**:

| Boundary-context item | Already-characterized nature (carried) | Posture | Later owner (named; no remedy) |
|---|---|---|---|
| authored `seed_text` | soft influence; write-once-by-absence + test-locked; no automatic revision writer | **no automatic-writer gap**; requirement-level Seed-Gov §7 boundary only | Seed-Gov §7 (requirement level) |

`[DISTINCTION]` Both tables are a **read-only gap characterization**, not a worklist. The order is
incidental; nothing here selects a first target, ranks severity, or implies a sequence. Listing a path
is **conformance characterization ≠ enforcement**, and **audit visibility ≠ authority**.

---

## 5. No-ordering / no-target / no-verdict note

`[NON-AUTHORIZATION]` This map records postures only. It **does not** rank the paths, select a first
writer, assert which matters more, claim any is safe or unsafe, claim any is a defect, propose a remedy,
demand enforcement, select a governance vehicle, or assert a runtime authority gate. "Not-yet-conformant
/ requires reconciliation" flags an unratified conformance question owned by a later separately
authorized step; per Seed-Governance SG-O5 it is **not** a must-patch order.

---

## 6. What this does NOT authorize

`[NON-AUTHORIZATION]` Authorizes none of, and must not be read as implying:

```
code · tests · runtime · endpoint / service calls · ingest / query · workspace-frequency evidence
writer patch · writer selection for remedy · patch ordering · enforcement behavior · governance vehicle
runtime authority gate · safe/unsafe verdict · defect verdict
schema / store / carrier / serialization / migration · canon_source · source-sameness / P4 mechanics
Seed-Governance mechanics · candidate store · governed admission implementation
seed rewrite · seed-revision API · chamber / private-cognition runtime · dream / incubation runtime
database / substrate · hidden chain-of-thought storage or exposure · hidden output blocker / finalizer
identity pinning · monitoring / autonomy layer · coercive mechanism
```

`[NON-AUTHORIZATION]` It amends no upstream contract (Document A / Document B / Seed-Governance / P4 /
Stage A / Cluster 2 / Ledger / MCP boundary), opens no Stage B, opens no autonomy, **updates no
orientation map**, and makes **no registry amendment**.

---

## 7. Stopping evidence

`[STOP]` Stop, and do not extend this map or draft any follow-up, if continuing would require any of:
runtime / endpoint / service / ingest-query / database / workspace-frequency evidence; a safe/unsafe or
defect verdict rather than a "not-yet-conformant / requires reconciliation" posture; a selected writer
patch, fix order, enforcement behavior, governance vehicle, or runtime authority gate; a schema / carrier
/ `canon_source` / source-sameness / database decision; a chamber representation; a candidate-store
design; a seed-revision construction; governed admission mechanics; a hidden output blocker / finalizer;
a hidden chain-of-thought store; identity pinning; or a monitoring / autonomy / coercive layer. Also stop
if the operator-supplied docs-only state is not `f6d0fb3` (or its named predecessors `dd53b7e` /
`a92dbef`), or if any posture above can no longer be stated read-only and non-substrate.

---

## 8. Next step after this artifact

`[LATER OWNER]` Codex challenge is complete (**ACCEPT WITH CORRECTIONS, applied**); this map is now
pending **operator review / commit** as its promotion action. **No automatic successor gate. No automatic
implementation.** Any later successor still requires **separate authorization** and an explicitly-opened
step; promotion of this read-only characterization authorizes none and schedules none. The orientation
map is **not** updated by this artifact; any pointer is a separate later step. Database / substrate
remains last.

---

## Anti-drift footer

DOCS-ONLY READ-ONLY GAP CHARACTERIZATION — no implementation, no tests, no runtime, no service, no
endpoint, no ingest / query, no workspace-frequency evidence, no remedy, no fix order, no writer-patch
selection, no enforcement, no governance vehicle, no runtime authority gate, no safe/unsafe verdict, no
defect verdict, no schema / store / carrier / serialization / migration, no `canon_source`, no
source-sameness / P4 mechanics, no Seed-Governance mechanics, no candidate store, no governed admission
implementation, no seed rewrite, no seed-revision API, no chamber / private-cognition runtime, no dream /
incubation runtime, no database / substrate, no hidden chain-of-thought storage or exposure, no hidden
output blocker / finalizer, no identity pinning, no monitoring / autonomy / coercive layer, no
orientation-map update, no registry amendment. Each **core runtime** path's conformance posture is **not
yet recorded** and is flagged **not-yet-conformant / requires reconciliation** without verdict, magnitude,
or order; `mood_drift → centroid → gravity` is **topology only**. Authored `seed_text` is **not** a core
runtime row — it is **boundary context** with **no automatic-writer gap** (only the requirement-level
Seed-Gov §7 boundary). H3 is **writer-authority-adjacent, not automatic identity pressure**; H5 is
**binding-contingent / parked**; H6 is **eligibility only**. conformance
characterization ≠ enforcement; audit visibility ≠ authority; inspection ≠ projection; old
identity-pressure paths ≠ chamber / private-cognition design; staging ≠ governed admission; seed revision
≠ ordinary memory admission. Audit observes authority and does not become authority. Memory may shape
context. Memory may not seize authority. Database / substrate remains last.
