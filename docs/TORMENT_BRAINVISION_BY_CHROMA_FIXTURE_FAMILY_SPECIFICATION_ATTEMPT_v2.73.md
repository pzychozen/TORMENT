# TORMENT Brainvision BY/Chroma Fixture-Family Specification Attempt v2.73

## 1. Status and scope

**DOCS-ONLY FIXTURE-FAMILY SPECIFICATION ATTEMPT. NON-AUTHORIZING.** This artifact attempts to convert the poseable
specification frame (v2.72) into a bounded, non-generative fixture-family specification. **The attempt is allowed to
fail, and this document does not optimize for a positive outcome.**

```text
FORMAL HOLD active
Mode 0 active
Brainvision offline / quarantined under research/brainvision/ + tests/research/
route_opened                   = False
implementation_opened          = False
fixture_generation_authorized  = False
gate_design_authorized         = False
experiment_authorized          = False
```

This artifact creates no fixtures, fixture data, arrays, pixels, images, parameter values, case counts, quotas,
allocations, matching tolerances, generator code, implementation recipes, metrics, thresholds, gates, experiments,
real clips, classifier or neural routes, or Brainvision-style readings. It touches no runtime, provider, live-model,
memory-to-prompt, caller, MCP/action/movement/autonomy, database/substrate/carrier/Stage-B, `§0`, registry, doctrine,
or orientation-map surface. No tags. No CodeQL cleanup. No successor is opened, scheduled, owed, or automatic.

Required locks remain unchanged:

```text
flat_field_validated                         = False
role_validated                               = False
schema_validated                             = False
entanglement_resolved                        = False
by_residual_isolated                         = False
generic_chroma_proxy_ruled_out               = False
null_rejected                                = False
artifact_ruled_out                           = False
proxy_ruled_out                              = False
confound_controlled                          = False
control_collapse_ruled_out                   = False
control_collapse_detected                    = False
control_collapse_reachability_validated      = False
candidate_structure_validated                = False
candidate_structure_survived                 = False
candidate_structure_detected                 = False
anti_inevitability_validated                 = False
control_honesty_validated                    = False
first_pass_structure_validity_claim_allowed  = False
temporal_claim_allowed                       = False
descriptor_validity_claim_allowed            = False
geometry_validity_claim_allowed              = False
screen_readiness_claim_allowed               = False
runtime_readiness_claim_allowed              = False
memory_readiness_claim_allowed               = False
integration_readiness_claim_allowed          = False
vision_claim_allowed                         = False
verdict                                      = HOLD
```

## 2. Accepted predecessor standing

```text
ff89a13  docs(research): review BY chroma fixture family specification   (v2.72, accepted edge)
```

Narrow standing of the accepted chain:

```text
v2.69  ROUTE_REMAINS_POSEABLE_UNOPENED. No standalone gate; any future gate family-bound. L2 = future family-bound
       prerequisite decision, not a standalone predecessor.
v2.70  CANDIDATE_LINEAGE_ADMITTED_FOR_FUTURE_REVIEW_ONLY. Neutral non-binding name; latent constructional
       asymmetries may still privilege one interpretation when the question is attached.
v2.71  QUESTION_ATTACHMENT_ADMISSIBLE_FOR_FUTURE_SPECIFICATION_REVIEW_ONLY. Latent asymmetry not ruled out;
       answer-baking neither established nor ruled out; control support and L2 carryability unresolved; the
       question-bearing family, if ever stated, must be statable from generator facts without relabeling existing
       Path B classes.
v2.72  SPECIFICATION_FRAME_POSEABLE_FOR_FUTURE_SPECIFICATION_ONLY. Membership honesty, exclusion honesty, artifact
       reporting, null handling, and entanglement reporting are frameable AS UNRESOLVED OR REPORTING-ONLY
       OBLIGATIONS ONLY; winding-sign coupling carried as BLOCKING QUESTION PRESERVED, not as a carryable
       obligation; control/L2/family-bound-gate feasibility all unestablished.
```

Path B remains **frozen unresolved evidence**. Preserved verbatim in force:

```text
directional_B_strengthened                    = True
per_channel_C_strengthened                    = True
A_descriptor_limitation_supported             = False
directional_proxy_failure_resolved            = False
per_channel_proxy_failure_resolved            = False
verdict                                       = HOLD
```

**Strengthened is not resolved.** Nothing below promotes any predecessor language beyond its accepted standing.

## 3. Specification purpose

The family this attempt tries to specify would exist so that **six coequal interpretations** of any future evidence
remain expressible:

```text
BY-specific behavior
generic chroma proxy
entangled / inseparable
artifact
null
uninformative
```

The purpose is **not** a binary BY-versus-generic-chroma comparison, and the family is not described as containing
known BY cases, known proxy cases, positives, negatives, or balanced scientific classes. The six interpretations are
interpretive endpoints only — not fixture classes, generator categories, result bins, case quotas, balanced
allocations, expected branches, or success categories. No wording, membership rule, exclusion rule, control
requirement, matching obligation, L2 obligation, or reporting rule below may make any endpoint unreachable.

## 4. Constructional lineage identity

The neutral working name remains, non-bindingly:

```text
the constructed opponent-plane matched-trajectory lineage
```

**What the name identifies constructionally** (all committed, all auditable by file identity in the frozen record):

```text
- a frozen opponent-plane coordinate system (the committed transform that defines the RG/BY plane);
- synthetic trajectory construction in that plane, with generation rules committed in frozen scripts;
- a declared-relationship group discipline: predeclared pairs/groups whose matched properties are declared at
  construction time (exact-multiset matching in one frozen family; within-tolerance matching in another);
- provenance-before-evaluation practice: generation rules, seeds, and declared relationships fixed before any
  evaluation, with losers reported;
- carried null / neutral / control members with assigned reporting-only status.
```

**What the name cannot establish scientifically:** that any member measures anything; that the plane's axes carry
perceptual or residual meaning; that matched construction controls any confound; that winding, cancellation, or any
generator contrast has an interpretation; or that a family built from this lineage can distinguish BY-specific
behavior from generic chroma proxy effects. The name is a pointer to construction provenance. **It is not membership
proof, and it is not a family.**

**Determination:** the record contains genuine positive constructional identity **for the lineage as a construction
discipline**. Whether it contains enough to support a **bounded family** is exactly what Sections 5 and 6 test, and
the answer there is negative on the present record.

## 5. Membership specification attempt

**Critical distinction, applied throughout:** constructional identity (generator provenance, declared construction
relationships, lineage traceability, non-interpretive structural properties) is never scientific interpretation
(BY-specific; generic chroma; proxy; positive; negative; target-bearing; non-target-bearing; scientifically
winder-like; scientifically canceller-like). **Constructional properties must never establish scientific
interpretation by definition.**

The attempt, stated honestly in three layers:

**(a) Genuine positive criteria available from the accepted record — discipline-level only:**

```text
P1. Constructed in the frozen opponent-plane coordinate system (committed transform), as a synthetic trajectory.
P2. Generated under provenance-before-evaluation: generation rule, seed, and declared group relationships fixed
    before any evaluation exists.
P3. Belonging to a predeclared construction group whose matched and varied properties are declared at
    construction time.
P4. Accompanied by a declared luminance-handling statement (what the construction does with Y', by construction).
```

P1–P4 are positive, auditable, and constructional. **But they bound the construction discipline, not a fixture
family.** Any honestly built opponent-plane construction whatsoever could satisfy them. A family that could carry the
BY-versus-generic-chroma question needs question-bearing membership content: which constructions, contrasted how,
varying what. That content is **absent from the accepted record by explicit predecessor decision** — v2.71 located
it in "new, separately reviewed constructions" that were deliberately not stated, and v2.72 reclassified membership
honesty as **frameable as an unresolved obligation only**. This attempt may not invent those constructions to satisfy
the success standard, and does not.

**(b) Future membership obligations (not criteria):** any future question-bearing membership statement must declare
driven axes and matched properties at generation time; must not import v2.24 roles as case types; must keep
non-BY-privileged constructions posable; must leave every interpretation losable. These are obligations. **Recording
them specifies nothing.**

**(c) Prohibited membership shortcuts (identified and rejected):**

```text
- "members are the frozen Path B banks"            -- relabels Path B classes; historical success as eligibility.
- "members are winders and matched cancellers"     -- winding sign as scientific identity.
- "members are constructions where BY responds"    -- descriptor output as membership.
- "members are cases separating at matched blockers" -- Path B scores / expected outcome as membership.
- "members are whatever passes the future gate"    -- gate survival as membership.
```

**Finding:** the accepted record supports **no genuine positive membership criterion for a bounded question-bearing
family**. P1–P4 are real but under-determining; everything stronger presently available is either an obligation or a
prohibited shortcut. **No criterion is fabricated to close the gap.**

```text
positive_membership_criteria_stated = False
```

## 6. Exclusion specification attempt

Honest constructional exclusions available now — again discipline-level:

```text
E1. No pre-evaluation provenance (rule, seed, declared relationships absent or reconstructed after the fact).
E2. Constructed outside the frozen opponent-plane system.
E3. Class or group identity assigned or verified by descriptor output rather than by generation rule.
E4. Added, removed, or re-labeled after any evaluation exists (post-hoc selection).
```

E1–E4 exclude dishonest **practice**. They do not draw a family boundary: they cannot say what separates "this
family" from "every other honestly built opponent-plane construction," because the question-bearing membership
content that would define the inside of the boundary is not stated (Section 5). Nothing is excluded for producing
difficult readings, supporting proxy / artifact / null interpretations, producing entangled results, failing to
flatter BY specificity, or failing a future metric or gate — and no such exclusion would be honest.

**Finding:** **no honest positive family-level exclusion boundary is presently available.** Stated directly, without
substitute.

```text
positive_exclusion_boundary_stated = False
```

## 7. Forbidden mappings

The **semantic operation** rejected here — not merely the exact vocabulary — is any move that converts the following
into scientific class truth:

```text
generator mechanics          trajectory direction         winding
cancellation                 bank composition             descriptor response
Path B response              expected scoring behavior
```

Unsafe examples: `winder -> BY-specific`; `canceller -> generic proxy`; `canceller -> null`; `opponent BY axis -> BY
residual`; `descriptor output -> class identity`; `Path B score -> expected outcome`. **Synonyms are equally unsafe
and equally rejected:** "rotationally coherent member," "consistent-turn construction," "direction-holding case," or
any fresh coinage denoting the same generator property inherits the same prohibition, because the operation — reading
a construction fact as a scientific identity — is what is forbidden, and renaming the fact does not change the
operation. The vocabulary-re-import precedent in the frozen record (caught and admitted there) is the standing
demonstration that this failure returns wearing new words.

## 8. Endpoint reachability

Wording or constructional rules that would improperly remove each endpoint, audited separately:

```text
BY-specific behavior     removed by: purpose text that pre-declares the family proxy-explained; or a membership
                         statement under which no construction could carry BY-differential content.
generic chroma proxy     removed by: universal over-matching of proxy-accessible covariates; or treating matched
                         construction as confound control.
entangled / inseparable  removed by: separability presumed as premise; or treating hard cases as malformed and
                         removable.
artifact                 removed by: treating generator success or construction QA as evidence against artifact; or
                         leaving fixture-artifact suspicion without a reporting slot.
null                     removed by: treating null behavior as failed generation; or building every member to carry
                         some contrast.
uninformative            removed by: interpreting target results after a failed required control; or banking any
                         score from an uncertified run.
```

Because this attempt states no membership rule and no exclusion boundary (Sections 5–6), **it removes none of the
six**. All six remain reachable interpretive outcomes for whatever future work is ever separately authorized.

## 9. Artifact, null, and entanglement handling

Carried as first-class unresolved interpretations, exactly as the accepted record holds them: fixture-artifact
suspicion remains reportable and is never closable by construction QA ("not an artifact" is never concludable);
null remains a possible scientific reading, not failed generation and not an automatically passed negative control;
entangled / inseparable remains a substantive endpoint, not an error bucket, with the absence of a deciding rule
load-bearing. Nothing here narrows, resolves, or re-grades any of the three.

## 10. Control obligation

Preserved verbatim in force:

```text
failed required positive control
=> target result uninformative
=> no target score banked
```

```text
control_obligation_frameable    = True
control_feasibility_established = False
```

No control is designed here, and no feasibility is implied. Historical controls, carried nulls, matched construction,
dual reporting, and project-level refusal semantics show only why the control question must be asked; none shows that
honest family-bound controls can be specified for this lineage. With no family specified (Section 16), the control
question has no object yet.

## 11. L2 obligation

Preserved verbatim:

```text
L2 silence exposed a missing control obligation.
It did not establish an intensity or contrast explanation.
```

```text
L2_obligation_frameable    = True
L2_feasibility_established = False
```

No method is selected; no standalone audit is opened; no post-hoc repair is introduced; no practical feasibility is
claimed. P4 (Section 5) obligates a declared luminance-handling statement per construction — a provenance
requirement, not an L2 method and not evidence that L2 can be handled honestly.

## 12. Pooled-versus-matched reporting

Carried **only** as a future reporting obligation: both views would be mandatory in any future family-bound
reporting, because the frozen record shows the two views disagreeing in both directions. Neither view is a control, a
truth source, a family definition, or a membership test, and neither is one here.

## 13. Family-bound gate dependency

Preserved, and only preserved:

```text
Any future gate, if ever proposed, must be bound to the actual fixture family.
```

No metric, threshold, pass/fail rule, or meaningful gate exists, and none is implied. **Because no family is
specified by this attempt, no family-bound gate can yet be posed** — there is nothing for a gate to be bound to.

```text
family_bound_gate_dependency_preserved    = True
family_bound_gate_feasibility_established = False
```

## 14. Winding-sign coupling blocker

Treated as the strongest live blocker, per v2.72's BLOCKING QUESTION PRESERVED standing. Determination of what it
blocks **on the present record**:

```text
scientific interpretation only   NOT ONLY -- the blocker reaches further than interpretation.
concrete membership              BLOCKED IN PRACTICE TODAY: the frozen construction inventory's principal contrast
                                 IS the winding-sign contrast. Any bounded family drawn from that inventory alone
                                 would sit one relabel away from winding-as-class-truth, and the alternative
                                 question-bearing constructions that could carry the family instead are undeclared.
                                 Whether a future declared construction set escapes this is UNDETERMINED here.
concrete exclusions              BLOCKED DERIVATIVELY: an exclusion boundary needs a membership inside; none exists.
controls                         NOT YET REACHED -- downstream of membership; undetermined.
L2 treatment                     NOT YET REACHED -- downstream of membership; undetermined.
future gate logic                NOT YET REACHED -- no family, no gate object (Section 13).
the entire specification frame   NOT SHOWN -- v2.72's poseability finding is not contradicted by this attempt's
                                 failure; the frame awaited constructional input that the accepted record does not
                                 yet contain.
```

**The blocker is not resolved here, and it is not resolved through careful wording:** this attempt's negative
membership finding (Section 5) is partly this blocker in action — the only concrete positive contrast available from
the record is the one that may not be used as identity. `winding_sign_coupling_resolved = False`.

## 15. Abort semantics

```text
NO_HONEST_SPECIFICATION_FRAME        -- when no genuine positive membership and exclusion boundaries can be stated
                                        without manufacturing scientific truth: the terrain itself defeats honest
                                        specification.
PAUSE                                -- when the frame is conceptually understandable but a required element cannot
                                        be represented honestly without inventing generator facts, frozen
                                        declarations, constructions, methods, or feasibility.
MORE_FROZEN_RECORD_AUDIT_REQUIRED    -- only when a load-bearing accepted predecessor fact genuinely cannot be
                                        resolved from the available record.
UNINFORMATIVE                        -- an interpretation or control-failure consequence in future empirical work;
                                        never a substitute verdict for a malformed family specification.
```

## 16. Decision

Available outcomes:

```text
BOUNDED_DOCS_ONLY_FIXTURE_FAMILY_SPECIFICATION_ACHIEVED
NO_HONEST_SPECIFICATION_FRAME
PAUSE
MORE_FROZEN_RECORD_AUDIT_REQUIRED
```

Selected:

```text
decision = PAUSE
```

**Why not the positive outcome:** the success standard requires both a positive, auditable, constructional membership
account and an honest exclusion boundary. Section 5 found the record's genuine positive criteria (P1–P4)
under-determining — they bound a discipline, not a family — and Section 6 found no family-level exclusion boundary.
Recording scope, locks, forbidden mappings, unresolved obligations, reporting requirements, and abort conditions —
which this artifact does — is **explicitly insufficient** under the success standard. `fixture_family_specified`
therefore remains `False`, and the positive decision is unavailable.

**Why PAUSE rather than NO_HONEST_SPECIFICATION_FRAME:** NO_HONEST is the verdict that the terrain itself defeats
honest specification — that any genuine boundaries would manufacture scientific truth. That was not shown. What was
shown is narrower: the question-bearing constructions that a bounded family needs were deliberately deferred by the
accepted predecessors, are absent from the frozen record, and **may not be invented by this artifact**. The frame
remains conceptually understandable (v2.72 stands, uncontradicted); the missing element is declared constructional
input that only a future, separately authorized, separately reviewed step could supply — and the winding-sign blocker
(Section 14) stands as the first test any such input must survive. That is the PAUSE condition, verbatim.

**Why not MORE_FROZEN_RECORD_AUDIT_REQUIRED:** no load-bearing accepted fact was unresolvable. The record is clear —
clear, specifically, that the needed constructions do not yet exist in it. Auditing it again would not create them.

**What PAUSE means here — all of it:** this attempt completed, failed honestly at its success standard, and stops.
**What it does not mean:** not that Brainvision is disproven; not that the lineage is dishonest terrain; not that
v2.72's poseability was wrong; not that a future attempt is scheduled, owed, or expected to succeed; not that any
successor is opened. Any future step requires separate explicit operator authorization and separate external review,
and PAUSE remains available at every such point.

## 17. Conclusion block

```text
specification_attempt_completed                = True
positive_constructional_identity_stated        = True
positive_membership_criteria_stated            = False
positive_exclusion_boundary_stated             = False
scientific_class_truth_created                 = False
six_interpretive_endpoints_preserved           = True

fixture_family_selected                        = False
fixture_family_validated                       = False
fixture_specification_attempt_opened           = True
fixture_specification_open_after_pause         = False
fixture_family_specified                       = False
fixture_generation_authorized                  = False

control_obligation_preserved                   = True
control_feasibility_established                = False

L2_obligation_preserved                        = True
L2_feasibility_established                     = False

family_bound_gate_dependency_preserved         = True
family_bound_gate_feasibility_established      = False
gate_design_authorized                         = False

winding_sign_coupling_resolved                 = False

implementation_opened                          = False
metrics_authorized                             = False
thresholds_authorized                          = False
experiment_authorized                          = False
route_opened                                   = False
successor_scheduled                            = False

decision                                       = PAUSE
verdict                                        = HOLD
OUTCOME_LABEL                                  = BRAINVISION_BY_CHROMA_FIXTURE_FAMILY_SPECIFICATION_ATTEMPT_PAUSE
```

The `True` fields are administrative records of this attempt's completion, of the discipline-level constructional
identity actually found in the record, and of preserved obligations. None is a scientific claim; none authorizes
anything. `positive_constructional_identity_stated = True` refers to the lineage's construction discipline (Section
4) and does **not** imply that family membership or exclusion boundaries were stated — they were not.
`fixture_specification_attempt_opened = True` records that the separately authorized v2.73 docs-only specification
attempt was actually entered and completed. `fixture_specification_open_after_pause = False` records the final paused
standing: no continuing fixture-specification lane, no selected fixture family, no specified fixture family, and no
successor opened, scheduled, owed, or automatic.

## 18. Codex review prompt

```text
Please review docs/TORMENT_BRAINVISION_BY_CHROMA_FIXTURE_FAMILY_SPECIFICATION_ATTEMPT_v2.73.md
(new, docs-only, untracked; over the accepted v2.72 edge "ff89a13 docs(research): review BY chroma fixture family
specification").

Verify that this specification attempt:
- is docs-only and authorizes NOTHING: no fixtures, fixture data, arrays, pixels, images, parameter values, case
  counts, quotas, allocations, matching tolerances, generator code, implementation recipes, metrics, thresholds,
  gates, experiments, real clips, classifier / neural routes, Brainvision-style readings, runtime / provider /
  live-model / memory-to-prompt / caller / MCP / action / autonomy / database / substrate / carrier / Stage-B paths,
  Section 0 / registry / doctrine / orientation-map changes, tags, or CodeQL cleanup; opens, schedules, or owes no
  successor; edits no existing file;
- records FORMAL HOLD, Mode 0, quarantine, and the five §1 posture flags, and keeps all claim locks False with
  verdict = HOLD;
- summarizes v2.69-v2.72 narrowly, preserves strengthened != resolved, keeps Path B frozen unresolved evidence with
  both proxy_failure_resolved lines False, and promotes no predecessor language;
- states the six-endpoint purpose without implying known BY cases, known proxy cases, positives, negatives, or
  balanced classes, and keeps the endpoints as interpretive outcomes only;
- keeps constructional identity strictly separate from scientific interpretation and never lets constructional
  properties establish scientific interpretation by definition;
- attempts membership honestly: states P1-P4 as genuine but discipline-level-only positive criteria, distinguishes
  criteria from obligations from prohibited shortcuts, finds NO genuine positive membership criterion for a bounded
  question-bearing family, and fabricates nothing;
- attempts exclusion honestly: states E1-E4 as practice-level exclusions, finds NO honest family-level exclusion
  boundary, and excludes nothing for difficult readings, unflattering interpretations, or future metric / gate
  failure;
- rejects the forbidden mappings as a semantic operation with synonyms equally unsafe;
- audits all six endpoints for improper removal and removes none;
- preserves artifact / null / entanglement as first-class unresolved interpretations;
- carries the v2.67 control rule verbatim with control_obligation_frameable = True and
  control_feasibility_established = False, designing no control and implying no feasibility;
- preserves both L2 sentences verbatim with L2_obligation_frameable = True and L2_feasibility_established = False,
  choosing no method and opening no standalone audit;
- carries pooled-versus-matched reporting as a future reporting obligation only;
- preserves the family-bound gate dependency, states that no family-bound gate can yet be posed because no family is
  specified, and keeps gate feasibility unestablished;
- treats winding-sign coupling as the strongest live blocker, determines what it blocks today (concrete membership in
  practice; exclusions derivatively; downstream items not yet reached; the frame itself not shown blocked), does not
  resolve it by wording, and keeps winding_sign_coupling_resolved = False;
- includes the four abort semantics with their required meanings;
- selects PAUSE with fixture_family_specified = False, explains why the positive outcome is unavailable under the
  success standard, why NO_HONEST_SPECIFICATION_FRAME would overclaim, and why more audit would not create the
  missing constructions; and describes no successor as opened, scheduled, owed, or automatic;
- carries the full conclusion block with the required booleans, including fixture_specification_attempt_opened = True
  (the attempt was entered and completed) alongside fixture_specification_open_after_pause = False (no continuing
  fixture-specification lane after the pause), the three feasibility fields False, decision = PAUSE, verdict = HOLD,
  and the attempt-specific outcome label.

Flag any invented generator fact, construction rule, parameter, tolerance, count, or method; any membership or
exclusion statement that uses descriptor outputs, Path B scores or strengths, winding sign, cancellation, expected
outcomes, interpretive endpoints, gate survival, or classifier / neural separation; any constructional property
converted into scientific class truth (including by synonym); any endpoint made unreachable; any feasibility claim
for controls, L2, or gates; any resolution of the winding-sign blocker by wording; any positive-decision language
despite the negative membership and exclusion findings; any successor scheduling; or any claim-lock / verdict
movement.
Return ACCEPT AS-IS or MODIFY with exact required changes.
```

All claim locks False, and the frozen verdict **HOLD**, are unchanged. **No `§0` pointer; no tags.**

*End — TORMENT Brainvision BY/Chroma Fixture-Family Specification Attempt v2.73. Docs-only, non-authorizing, over the
accepted v2.72 edge `ff89a13`. An actual specification attempt that was allowed to fail and did: genuine positive
constructional identity exists at the lineage-discipline level (P1–P4; E1–E4), but the accepted record contains no
question-bearing membership content — deliberately deferred by v2.71/v2.72 — and this artifact fabricates none, so no
bounded family is specified (fixture_family_specified = False; positive membership and exclusion findings both
negative). Decision = PAUSE: the frame remains conceptually understandable and v2.72's poseability stands
uncontradicted, but the required constructional input may not be invented here; NO_HONEST_SPECIFICATION_FRAME would
overclaim, and further audit would not create the missing constructions. Winding-sign coupling remains the strongest
live blocker (blocks concrete membership in practice today; unresolved, and not resolved by wording). All six
interpretive endpoints remain reachable; artifact / null / entanglement remain first-class; control and L2 obligations
preserved verbatim with feasibility unestablished; no family-bound gate can yet be posed; Path B remains frozen
unresolved evidence. No fixture, generator, metric, threshold, gate, experiment, route, or successor is created,
opened, scheduled, or owed. All claim locks remain False; verdict = HOLD; outcome label
BRAINVISION_BY_CHROMA_FIXTURE_FAMILY_SPECIFICATION_ATTEMPT_PAUSE; no `§0` pointer added; no tags.*
