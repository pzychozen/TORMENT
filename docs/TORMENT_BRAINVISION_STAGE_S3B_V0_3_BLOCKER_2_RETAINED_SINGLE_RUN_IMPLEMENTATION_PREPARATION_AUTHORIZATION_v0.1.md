# TORMENT Brainvision Stage S3B v0.3
# BLOCKER-2 Retained Single-Run Implementation Preparation Authorization v0.1

## 0. Document Status

Document class:

```text
BLOCKER-2 retained single-run implementation preparation authorization
```

Document version:

```text
v0.1
```

Repository:

```text
C:\TORMENT\TORMENT_repo\TORMENT-fabric_v2\torment_fabric
```

This is a docs-only authorization document. It authorizes a later Codex
implementation phase to add the minimum retained-execution infrastructure
needed before any future authoritative retained BLOCKER-2 absolute-path control
run can be authorized.

This document does not:

```text
modify implementation
modify tests
execute tests
execute a native rename case
create a retained result artifact
create a gate-entry marker
consume an authorization
authorize the authoritative retained run
close BLOCKER-2
start BLOCKER-4
authorize production integration
```

Hilmir remains the sole Windows operator, committer, and pusher. GPT remains
the system-engineering overseer. Claude may independently review this
authorization.

## 1. Authorization Question

Question:

```text
Should a later Codex implementation phase be authorized to add the narrow
retained-execution infrastructure required to support a future truthful,
identity-bound, durable, single-use retained BLOCKER-2 absolute-path control
execution?
```

Answer:

```text
Yes, with narrow file-surface, mode, schema, gate, artifact, and test
constraints. This authorization is for implementation preparation only.
```

The later implementation may make a future retained run technically possible,
but it must not itself perform, consume, or authorize that run.

## 2. Authoritative Baseline

Baseline verification was performed before this document was written.

Branch:

```text
main
```

HEAD:

```text
23504dac6606309257016d1c0ca617f7a6c4524c
```

origin/main:

```text
23504dac6606309257016d1c0ca617f7a6c4524c
```

Working tree before editing:

```text
clean
```

Index lock:

```text
.git\index.lock absent
```

Latest reviewed lineage:

```text
23504da docs(research): assess blocker 2 retained single-run readiness
82d6fce docs(research): record blocker 2 absolute-path control findings
03727e7 research(brainvision): implement blocker 2 absolute-path control
e34d3d4 docs(research): authorize blocker 2 absolute-path control
9ab500f docs(research): assess blocker 2 absolute-path control
8af0ab8 docs(research): record blocker 2 promotion primitive findings
89f41a5 research(brainvision): implement blocker 2 promotion primitive validation
6c8b113 docs(research): authorize blocker 2 promotion primitive validation
5593640 docs(research): research blocker 2 Windows promotion primitive
031b06c docs(research): assess blocker 2 promotion ownership
```

Controlling assessment blob identity verified at HEAD:

```text
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_AUTHORITATIVE_RETAINED_SINGLE_RUN_ASSESSMENT_v0.1.md
git_blob = 103d66aaff200bb7cc35271f2f7d74d11ce5663b
checked_out_sha256 = 71b4e96da222461c16caea6494719183504e758b6e883b44c4db8df9b636f51d
```

Selected source identities observed during review:

```text
research/brainvision/validate_windows_same_volume_no_replace_promotion_v0_1.py
git_blob = 1d892f7c8ab9571eae2ca55d22d1c13d7ba358e8
checked_out_sha256 = d980d778c3cacd43841784cbc1e201f6e92d3bbe7d60f56472999b33913b605d

research/brainvision/test_validate_windows_same_volume_no_replace_promotion_v0_1.py
git_blob = 3eb2486cccf6b9e5daab8e4e5971f593779c7589
checked_out_sha256 = b6a23a227ac36019a32c80d941eadb8e467c4d0ede8ac4175856cb0c5ddf5246

research/brainvision/test_validate_windows_same_volume_no_replace_promotion_integration_v0_1.py
git_blob = 5ccb5ea6bb4bd386f1329a10069b875e5a3daee4
checked_out_sha256 = f904e037854d56f55b9d668392aa9d2c49248f3fd4f19b28ab566ccea4660743

research/brainvision/durable_evidence_schema_v0_3.py
git_blob = af5eb374e8f5f4ee52b6d5d5e9ce23764e654a06
checked_out_sha256 = fdb242c8a929a9fe3b17106c991264390da5cbefacd6e6624cad3a097a39129a

research/brainvision/durable_evidence_primary_writer_v0_3.py
git_blob = 83e93d9a4ab5c95ea881bc84eed01e0debcab5bc
checked_out_sha256 = 4ede6f55f1bf751bfa1da6a98900b3dd41bea9810caff63880641b06b7fafbb5

research/brainvision/durable_evidence_durability_v0_3.py
git_blob = 3066a85fe17f2f2a982087bdf2a3f0bde5f7a7f1
checked_out_sha256 = 9122662304f578260b65d2711b5bdd061358eae16745b3fd55cd09f44a6b30ef

research/brainvision/durable_evidence_windows_adapter_v0_3.py
git_blob = 96a977a21c279068a6073f0274acd07c9941c3b9
checked_out_sha256 = 62f2c639ea86ff2279cea55821aea2b40f1f4ed309809dc32a7a15fbba926fd3

research/brainvision/durable_evidence_authority_v0_3.py
git_blob = bbeb78f9b30506a90c7ce347c77878256318f19e
checked_out_sha256 = 6aeaf948ab4ec2dd2d84544c9eb7340c0b52441aec56bff4d4b27af1c7fe6cd8
```

These identities are review evidence for this authorization. A later
implementation phase must recompute all identities from its own starting
baseline and must not assume these values still match after authorized edits.

## 3. Preserved Boundaries

The following project boundaries remain active:

```text
FORMAL_HOLD = active
Mode_0 = active
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

Brainvision remains:

```text
offline
quarantined
synthetic-only
non-production
non-service
non-kernel
non-memory-integrated
non-cognitive
non-autonomous
```

The later implementation phase must not modify or authorize modification of:

```text
torment_service/kernel/
production TORMENT memory functionality
live service behavior
prompt surfaces
action surfaces
autonomy
identity
truth selection
memory cognition
```

The later implementation phase must not open:

```text
live visual capture
real gameplay
real video execution
production publication
production recovery
general live-test lanes
BLOCKER-4
```

The later implementation phase must not claim:

```text
general Windows filesystem durability
documented rename atomicity
documented rename durability
power-loss persistence
BLOCKER-2 closure
real-world Brainvision readiness
strong order sensitivity
```

Publication remains a projection of the authoritative scientific result. The
authoritative durable scientific result remains:

```text
verified IMMUTABLE_SCIENTIFIC_BUNDLE
+
linked valid SCIENTIFIC_COMPLETION
```

Promotion and durability evidence remain linked platform evidence, not
scientific completion evidence.

J2 recovery must never reconstruct or claim an original J1 completion that was
not durably established.

Current blocker state is preserved:

```text
BLOCKER-1 = BLOCKER_1_CLOSED_WITHIN_AUTHORIZED_SYNTHETIC_OFFLINE_WINDOWS_LOCAL_FIXED_NTFS_TMP_PATH_SCOPE
BLOCKER-2 = OPEN
BLOCKER-3 = CLOSED_WITHIN_AUTHORIZED_SYNTHETIC_OFFLINE_STAGE_S3B_V0_3_SCOPE
BLOCKER-4 = OPEN AND SEPARATE
```

Do not reopen BLOCKER-1 or BLOCKER-3. Do not merge BLOCKER-4 into BLOCKER-2.
Do not begin BLOCKER-4.

## 4. Assessment Basis

The controlling assessment is:

```text
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_AUTHORITATIVE_RETAINED_SINGLE_RUN_ASSESSMENT_v0.1.md
```

Committed at:

```text
23504dac6606309257016d1c0ca617f7a6c4524c
```

Assessment verdict:

```text
B. REQUIRE_NARROW_IMPLEMENTATION_PREPARATION_BEFORE_RUN_AUTHORIZATION
```

Implementation-readiness classification:

```text
NARROW_IMPLEMENTATION_CHANGE_REQUIRED_BEFORE_AUTHORIZATION
```

The assessment found that the current runner lacks at least:

```text
retained artifact persistence
immutable result-directory handling
durability and reread verification
repository and source identity gates
selected case-set binding
durable gate-consumption state
truthful terminal-state handling
single-run enforcement
interruption handling
stable hash admission
failure-taxonomy recording
```

The assessment also fixed the retained case decision:

| Case | Retained decision | Completion role |
|---|---|---|
| A1 | INCLUDE_IN_RETAINED_RUN | completion-gating |
| A2 | INCLUDE_IN_RETAINED_RUN | completion-gating |
| A3 | INCLUDE_IN_RETAINED_RUN | completion-gating |
| A4 | EXCLUDE_FROM_RETAINED_RUN | not included |
| A5 | INCLUDE_IN_RETAINED_RUN | completion-gating |
| A6 | OPTIONAL_BUT_NON_GATING | diagnostic-only if selected |
| A7 | EXCLUDE_FROM_RETAINED_RUN | not included |
| A8 | EXCLUDE_FROM_RETAINED_RUN | not included |

That case decision is controlling for this authorization. This document does
not authorize a full A1-A8 retained run.

Required documents inspected for this authorization:

```text
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_1_WINDOWS_DIRECTORY_DURABILITY_SPECIFICATION_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_1_WINDOWS_DIRECTORY_DURABILITY_IMPLEMENTATION_AUTHORIZATION_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_1_WINDOWS_DIRECTORY_DURABILITY_IMPLEMENTATION_FINDINGS_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_1_WINDOWS_DIRECTORY_DURABILITY_CLOSURE_ASSESSMENT_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_SAME_VOLUME_NO_REPLACE_PROMOTION_AND_FINAL_OWNERSHIP_ASSESSMENT_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_WINDOWS_SAME_VOLUME_NO_REPLACE_DIRECTORY_PROMOTION_PRIMARY_SOURCE_PRIMITIVE_RESEARCH_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_BOUNDED_WINDOWS_SAME_VOLUME_NO_REPLACE_DIRECTORY_PROMOTION_PRIMITIVE_VALIDATION_AUTHORIZATION_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_BOUNDED_WINDOWS_PROMOTION_PRIMITIVE_VALIDATION_IMPLEMENTATION_FINDINGS_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_ABSOLUTE_PATH_FILERENAMEINFO_ISOLATING_CONTROL_ASSESSMENT_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_NARROW_ABSOLUTE_PATH_FILERENAMEINFO_CONTROL_SPECIFICATION_AND_IMPLEMENTATION_AUTHORIZATION_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_NARROW_ABSOLUTE_PATH_FILERENAMEINFO_CONTROL_IMPLEMENTATION_FINDINGS_v0.1.md
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_AUTHORITATIVE_RETAINED_SINGLE_RUN_ASSESSMENT_v0.1.md
```

Required implementation and test files inspected:

```text
research/brainvision/validate_windows_same_volume_no_replace_promotion_v0_1.py
research/brainvision/test_validate_windows_same_volume_no_replace_promotion_v0_1.py
research/brainvision/test_validate_windows_same_volume_no_replace_promotion_integration_v0_1.py
```

Durable-evidence modules inspected for reusable primitives:

```text
research/brainvision/durable_evidence_schema_v0_3.py
research/brainvision/durable_evidence_primary_writer_v0_3.py
research/brainvision/durable_evidence_durability_v0_3.py
research/brainvision/durable_evidence_windows_adapter_v0_3.py
research/brainvision/durable_evidence_authority_v0_3.py
research/brainvision/durable_evidence_scientific_result_v0_3.py
research/brainvision/durable_evidence_replay_v0_3.py
research/brainvision/durable_evidence_publication_v0_3.py
research/brainvision/durable_evidence_publication_recovery_v0_3.py
```

## 5. Existing Implementation State

The current BLOCKER-2 runner contains two distinct validation modes:

```text
ROOTDIRECTORY_RELATIVE
ABSOLUTE_PATH_CONTROL
```

The existing absolute-path control identity is:

```text
3d9b66a180fabf00c8bb6695c74fc9d69d21cd3ac9335cc5d2dc3a1169417531
```

The prior RootDirectory-relative policy identity remains distinct:

```text
df91a9bcc3c5b37e938a086801dd2bca42f0290533a6cf2682055df475f663f3
```

The current absolute-path control uses:

```text
SetFileInformationByHandle
FileRenameInfo
RootDirectory = NULL
ReplaceIfExists = FALSE
canonical fully qualified drive-qualified Win32 DOS destination text
bounded same-volume Windows local fixed NTFS fixture
```

The current absolute-path control matrix contains A1-A8. It currently builds an
ephemeral in-memory control record with:

```text
retained_execution = False
```

The current implementation has no authoritative retained mode, no durable
gate-entry marker, no retained terminal artifact, no authorization-consumption
state, and no single-use enforcement.

Reusable primitives identified:

```text
validate_windows_same_volume_no_replace_promotion_v0_1.canonical_json_bytes
validate_windows_same_volume_no_replace_promotion_v0_1.sha256_hex
validate_windows_same_volume_no_replace_promotion_v0_1.absolute_path_control_policy_identity
validate_windows_same_volume_no_replace_promotion_v0_1.run_absolute_path_control_matrix
validate_windows_same_volume_no_replace_promotion_v0_1.execute_absolute_path_control
validate_windows_same_volume_no_replace_promotion_v0_1.identity_from_handle
validate_windows_same_volume_no_replace_promotion_v0_1.identity_from_path
validate_windows_same_volume_no_replace_promotion_v0_1.build_content_manifest
validate_windows_same_volume_no_replace_promotion_v0_1.admit_support_profile
validate_windows_same_volume_no_replace_promotion_v0_1.validate_fixture_root
validate_windows_same_volume_no_replace_promotion_v0_1.validate_child_path
durable_evidence_schema_v0_3.sha256_hex
durable_evidence_schema_v0_3.canonical_json_bytes
durable_evidence_schema_v0_3.canonical_json_bytes_bounded
durable_evidence_schema_v0_3.load_canonical_json_bytes
durable_evidence_schema_v0_3.read_file_bytes_bounded
durable_evidence_schema_v0_3.directory_durability_policy_identity
durable_evidence_schema_v0_3.validate_directory_durability_policy_identity
durable_evidence_windows_adapter_v0_3.Win32DirectoryDurabilityAdapter.sync_directory_entry
durable_evidence_windows_adapter_v0_3.DirectoryDurabilityContext
durable_evidence_authority_v0_3 authority-consumption naming and replay pattern
```

Duplication risks identified:

```text
canonicalization must not fork into a third incompatible JSON identity discipline
directory durability must not be reimplemented when the BLOCKER-1 adapter applies
scientific/publication stored-record schemas must not be reused for BLOCKER-2
platform evidence merely to avoid writing a retained-specific schema
private helper functions in durable_evidence_primary_writer_v0_3.py must not be
imported as if they were a public contract
```

The later implementation must reuse public primitives where their semantics
match, and must justify any retained-specific persistence helper as a boundary
preservation measure rather than convenience duplication.

## 6. Authorization Decision

Decision:

```text
Authorize a later narrow implementation-preparation phase for retained
BLOCKER-2 absolute-path control infrastructure.
```

This authorization is limited to infrastructure that can later support:

```text
one identity-bound authorization
one admitted Windows fixture root
one exact selected case set
one durable gate-entry transition
one native attempt sequence
one canonical retained result artifact
one truthful terminal result
no automatic rerun
```

This authorization does not authorize:

```text
the authoritative retained run itself
an authoritative gate entry
an authoritative retained artifact
BLOCKER-2 closure
production integration
```

The later implementation must make these states distinguishable:

```text
preflight rejection
gate entry
successful completion
post-gate failure
interruption
artifact persistence failure
artifact verification failure
```

## 7. Authorized Architecture

Selected architecture:

```text
B. add one narrow retained-execution module plus focused tests and minimally
integrate it into the existing runner
```

Architecture B is selected with constrained reuse from existing S3B durable
evidence modules. Architecture A is rejected because the existing runner would
absorb too much authority and persistence behavior. Architecture C is not
selected as the primary architecture because the existing durable-evidence
schemas are tied to scientific, publication, and recovery records; the retained
BLOCKER-2 evidence must remain platform evidence. Architecture D is unnecessary
because B is narrow enough when file surface and imports are explicitly fixed.

The new retained module must own:

```text
retained-mode constants
retained schema declarations
case-set declaration
fixture-profile declaration
assessment and future authorization input validation
repository-state admission checks
source-byte identity collection
gate-entry record construction and persistence
single-run enforcement checks
terminal-state construction and validation
retained artifact envelope construction
artifact write, durability, reread, and reverification orchestration
failure taxonomy recording
interruption-state recording
synthetic fault injection for retained infrastructure tests
```

The existing runner may own only:

```text
explicit retained-mode entry point wiring
export of existing A1, A2, A3, A5, and optional A6 execution helpers
default-mode preservation
proof that ABSOLUTE_PATH_CONTROL remains ephemeral unless retained mode is
explicitly selected
```

Existing durable-evidence modules may be imported only through public functions
and classes. No durable-evidence module modification is authorized by this
document.

## 8. Authorized File Surface

The later implementation phase may create:

```text
research/brainvision/blocker2_retained_absolute_path_control_v0_1.py
research/brainvision/test_blocker2_retained_absolute_path_control_v0_1.py
research/brainvision/test_blocker2_retained_absolute_path_control_integration_v0_1.py
```

The later implementation phase may modify:

```text
research/brainvision/validate_windows_same_volume_no_replace_promotion_v0_1.py
research/brainvision/test_validate_windows_same_volume_no_replace_promotion_v0_1.py
research/brainvision/test_validate_windows_same_volume_no_replace_promotion_integration_v0_1.py
```

Allowed modification purpose by file:

| File | Allowed purpose |
|---|---|
| `research/brainvision/blocker2_retained_absolute_path_control_v0_1.py` | New retained infrastructure module and schema implementation. |
| `research/brainvision/test_blocker2_retained_absolute_path_control_v0_1.py` | New unit tests for retained schema, identities, preflight, gate, artifact, failure, interruption, and synthetic fault injection. |
| `research/brainvision/test_blocker2_retained_absolute_path_control_integration_v0_1.py` | New Windows integration tests using non-authoritative synthetic authorization fixtures and temporary roots outside the repository. |
| `research/brainvision/validate_windows_same_volume_no_replace_promotion_v0_1.py` | Minimal explicit retained-mode wiring and exports needed to call already implemented absolute-path A-case helpers under retained orchestration. |
| `research/brainvision/test_validate_windows_same_volume_no_replace_promotion_v0_1.py` | Minimal updates proving default and absolute-path control modes remain ephemeral and cannot enter retained execution accidentally. |
| `research/brainvision/test_validate_windows_same_volume_no_replace_promotion_integration_v0_1.py` | Minimal updates proving existing Windows integration tests remain non-authoritative and retain `retained_execution = false`; no authoritative retained fixture. |

No other file may be created or modified in the later implementation phase.
Specifically, this authorization does not allow changes to:

```text
research/brainvision/durable_evidence_schema_v0_3.py
research/brainvision/durable_evidence_primary_writer_v0_3.py
research/brainvision/durable_evidence_durability_v0_3.py
research/brainvision/durable_evidence_windows_adapter_v0_3.py
research/brainvision/durable_evidence_authority_v0_3.py
torment_service/kernel/
```

If implementation cannot be completed within the authorized file surface, the
later Codex phase must stop and request a new docs-only authorization.

## 9. Retained Mode Contract

Authorize a new explicit mode selector:

```text
BLOCKER2_ABSOLUTE_PATH_CONTROL_RETAINED_SINGLE_RUN_V0_1
```

This mode must be separate from:

```text
ROOTDIRECTORY_RELATIVE
ABSOLUTE_PATH_CONTROL
```

The retained mode must:

```text
fail closed unless this exact selector is supplied
never be entered by default
never be entered by ABSOLUTE_PATH_CONTROL
never be entered by ROOTDIRECTORY_RELATIVE
require explicit future authorization identity input
require explicit assessment identity input
require explicit retained result parent input
require explicit fixture root input
require explicit selected case-set declaration
```

No alias, abbreviation, environment-variable fallback, config-file default, or
interactive prompt may enter retained execution.

All tests using synthetic retained infrastructure must clearly record:

```text
authoritative = false
retained_execution = false
```

unless testing terminal-record validation entirely in memory.

## 10. Schema and Canonicalization Contract

Authorize a retained schema family with these schema identity strings:

```text
blocker2-retained-absolute-path-control-authorization-input-v0.1
blocker2-retained-absolute-path-control-case-set-v0.1
blocker2-retained-absolute-path-control-fixture-profile-v0.1
blocker2-retained-absolute-path-control-gate-entry-record-v0.1
blocker2-retained-absolute-path-control-terminal-record-v0.1
blocker2-retained-absolute-path-control-artifact-envelope-v0.1
```

These strings are schema names only. Their canonical SHA-256 identities must be
computed by the later implementation from exact canonical declarations and
reported in the later implementation findings.

The retained schema must distinguish:

```text
authorization identity
assessment identity
implementation identity
policy identity
case-set identity
fixture-profile identity
repository state
source identities
preflight state
gate state
case outcomes
artifact state
terminal state
retained_execution state
failure taxonomy
boundary state
```

Required terminal-record top-level fields:

```text
schema
schema_version
record_kind
authorization_identity
assessment_identity
implementation_identity
policy_identity
case_set_identity
fixture_profile_identity
repository_state
source_identities
preflight_state
gate_state
case_outcomes
artifact_state
terminal_state
retained_execution
failure_taxonomy
boundary_state
```

The schema must reject:

```text
unknown required fields
missing mandatory identities
wrong versions
invalid terminal-state combinations
retained_execution truth contradictions
non-finite numeric data
floating-point data
null identity values
non-canonical path declarations
non-canonical fixture declarations
unknown case identifiers
duplicate JSON keys on reread
oversized canonical payloads
```

Canonical retained-mode identity serialization must use the existing
absolute-path control canonical JSON discipline where compatible:

```text
sorted keys
compact separators
UTF-8
allow_nan = false
stable primitive values
no self-referential hash field
```

Existing durable-evidence schema canonicalization may be used only for
validating or recording imported BLOCKER-1 directory-durability policy evidence
and must not be mixed into retained-mode content hashes unless the hash purpose
explicitly names that imported durable-evidence identity.

Stored artifact form:

```text
The retained terminal artifact is a canonical JSON wrapper containing the
canonical terminal record as an object plus non-recursive metadata about that
terminal record.
```

The artifact wrapper must include at minimum:

```text
artifact_envelope_schema
artifact_envelope_version
authorization_identity
assessment_identity
terminal_record_schema
terminal_record_sha256
terminal_record_byte_length
terminal_record
```

The terminal record itself must not contain the hash of the wrapper that
contains it. The wrapper may record `terminal_record_sha256`, computed from
canonical bytes of `terminal_record`. The implementation may compute and return
the wrapper file SHA-256 after reread, but that hash must be external to the
wrapper content to avoid recursion.

## 11. Repository and Source Admission

The retained mode must perform read-only repository preflight before any gate
entry. Required checks:

```text
expected branch
expected HEAD
expected origin/main
HEAD == origin/main
no .git/index.lock
authorized source identity
authorized test identity where bound
authorized retained module identity where bound
authorized policy identity
authorized assessment identity
future authorization identity
case-set identity
fixture-profile identity
```

The implementation must not perform Git mutations. Git inspection commands
must be read-only.

Repository working-tree rule:

```text
Any modified, staged, deleted, or untracked file inside an authorized source,
test, policy, schema, output, retained-result, docs-evidence, or
repository-evidence surface fails closed before gate entry.
```

Unrelated files outside those surfaces may be tolerated only if all of the
following are true:

```text
the future authorization explicitly permits unrelated out-of-surface files
the implementation records the full git status porcelain output
the implementation records the classification rule used
the implementation records that no tolerated path can affect executed bytes,
policy bytes, schema bytes, test bytes, retained output, fixture input, or
repository evidence
```

If classification is ambiguous, the implementation must fail closed before gate
entry. A later retained-run authorization may choose a stricter fully clean
tree rule.

Source-byte binding must use both:

```text
Git blob identity where applicable
SHA-256 of checked-out execution bytes
```

The retained record must distinguish these identities. Commit identity alone is
insufficient.

Line-ending and checkout-filter rule:

```text
Git blob identity binds repository object bytes.
Checked-out-byte SHA-256 binds the actual filesystem bytes supplied to Python
after checkout filters, line-ending conversion, and working-tree materialization.
Both identities must be recorded and verified before gate entry.
```

The implementation must calculate checked-out-byte SHA-256 from the actual file
bytes read from disk at execution time.

## 12. Policy, Case-Set, and Fixture Binding

Policy binding:

```text
required_policy_schema = blocker-2-absolute-path-filerenameinfo-control-policy-v0.1
required_policy_sha256 = 3d9b66a180fabf00c8bb6695c74fc9d69d21cd3ac9335cc5d2dc3a1169417531
required_control_mode = ABSOLUTE_PATH_CONTROL
forbidden_substitute_policy_sha256 = df91a9bcc3c5b37e938a086801dd2bca42f0290533a6cf2682055df475f663f3
```

The retained mode must fail closed if:

```text
the absolute-path control declaration differs
the canonical policy identity differs
the mode is not the absolute-path control
the prior RootDirectory-relative policy is substituted
```

Case-set binding:

```text
completion_gating_cases = A1,A2,A3,A5
optional_non_gating_cases = A6
excluded_cases = A4,A7,A8
```

A6 may be executed only if explicitly selected by a later authorization. A6
must never be required for `RUN_COMPLETE`.

A4, A7, and A8 must not be selected, executed, skipped, or summarized as part
of the retained run. If a future input selects A4, A7, A8, unknown cases, or a
semantic reorder that changes meaning, preflight must fail closed.

The retained case-set declaration must be canonicalized and identity-bound. A
canonical order may be used for hashing, but execution ordering must remain the
explicit authorized sequence:

```text
A1
A2
A3
A5
optional A6 if selected
```

Fixture-profile binding must describe exactly:

```text
Windows 10/11 workstation
local fixed drive
NTFS
ordinary existing non-reparse admitted root
isolated temporary execution root outside the repository
same-volume source and destination
canonical fully qualified drive-qualified Win32 DOS destination
RootDirectory = NULL
ReplaceIfExists = FALSE
```

The implementation must record exact observed OS, drive, filesystem, volume,
root, and path observations without converting them into broad compatibility
claims. The fixture profile must be canonicalized and identity-bound.

## 13. Durable Gate-Entry Contract

The gate-entry boundary is:

```text
durable gate-entry marker successfully established immediately before the first
authorized native transition
```

Gate-entry semantics:

```text
preflight rejected before durable gate entry:
authority remains unconsumed

durable gate-entry marker establishment fails:
authority remains unconsumed and native invocation is prohibited

durable gate-entry marker successfully established:
authority becomes consumed

native or post-gate failure:
authority remains consumed

interruption after durable gate entry:
authority remains consumed
```

The durable gate-entry marker must be:

```text
canonical
identity-bound
authorization-bound
assessment-bound
policy-bound
case-set-bound
fixture-bound
repository-bound
source-byte-bound
durably persisted
reread
schema-verified
hash-verified
```

The gate marker must not claim completion. It must record:

```text
gate_consumed = true
native_invocation_started = false
retained_execution = false
terminal_state = GATE_ENTERED
```

The implementation must not proceed to the first native transition unless
durable gate-entry establishment succeeds completely.

## 14. Single-Run Enforcement

The retained mode must enforce local single-use authority before gate entry.

Required checks:

```text
authorized retained result directory does not exist
authorized gate-entry object does not exist
authorized terminal object does not exist
no prior consumed marker exists for the authorization identity
no prior terminal object exists for the authorization identity
```

The implementation must fail closed if any associated object exists. It must
not overwrite, replace, merge, reset, repair, resume, or reclaim an authority.

No option is authorized for:

```text
force
retry
resume
ignore existing gate
reuse authorization
overwrite result
merge result
```

An existing durable gate-entry marker with no valid terminal artifact is
evidence of consumed but incomplete authority.

## 15. Native Execution Boundary

The retained infrastructure may invoke only the already implemented native
behavior required by the selected absolute-path cases.

Authorized native case helpers:

```text
validate_a1_absolute_path_positive
validate_a2_existing_destination_directory_absolute_path
validate_a3_existing_destination_file_absolute_path
validate_a5_absolute_path_identity_continuity
validate_a6_absolute_path_native_error_characterization
```

A6 is authorized only when selected as optional non-gating diagnostic evidence
by a later authorization.

No new native primitive is authorized:

```text
FileRenameInfoEx
MoveFileW
MoveFileExW
ReplaceFileW
copy/delete fallback
replace-existing behavior
cross-volume behavior
new source rights
new destination-parent rights
live data
```

The retained infrastructure may call existing internal helpers only after
verifying that the call preserves:

```text
SetFileInformationByHandle
FileRenameInfo
RootDirectory = NULL
ReplaceIfExists = FALSE
same-volume fixture
ordinary non-reparse paths
synthetic offline material
```

Unexpected native errors must remain recorded as native errors and must not be
normalized into success.

## 16. Terminal-State and Failure Contract

Required terminal-state vocabulary:

```text
PREFLIGHT_REJECTED
GATE_ENTRY_FAILED
GATE_ENTERED
RUN_COMPLETE
RUN_FAILED
RUN_INTERRUPTED
ARTIFACT_PERSISTENCE_FAILED
ARTIFACT_REVERIFY_FAILED
```

The exact implementation vocabulary may differ only if it is explicitly mapped
to the states above.

Invalid combinations must fail schema validation. Examples:

```text
retained_execution = true with terminal failure
gate_consumed = false after recorded native invocation
RUN_COMPLETE without all gating cases satisfied
RUN_COMPLETE without A5 identity continuity
RUN_COMPLETE without A2 and A3 destination preservation
RUN_COMPLETE without successful artifact persistence
RUN_COMPLETE without successful artifact reread verification
```

Required failure taxonomy:

```text
PREFLIGHT_REJECTED
IDENTITY_MISMATCH
POLICY_MISMATCH
SOURCE_MISMATCH
REPOSITORY_STATE_INVALID
FIXTURE_INVALID
UNSUPPORTED_PLATFORM_PROFILE
GATE_ENTRY_PERSISTENCE_FAILURE
NATIVE_ERROR_INDETERMINATE
NATIVE_UNSUPPORTED
COLLISION_EXPECTATION_MISMATCH
DESTINATION_PRESERVATION_FAILURE
IDENTITY_CONTINUITY_FAILURE
CONTENT_CONTINUITY_FAILURE
ARTIFACT_SERIALIZATION_FAILURE
ARTIFACT_HASH_FAILURE
ARTIFACT_PERSISTENCE_FAILURE
ARTIFACT_REVERIFY_FAILURE
RUN_INTERRUPTED
UNEXPECTED_INTERNAL_ERROR
```

The implementation must preserve the first authoritative failure. Narrowly
structured secondary cleanup observations may be recorded, but cleanup failure
must not overwrite the primary failure.

Raw native outcome rules:

```text
ERROR_INVALID_PARAMETER / 87 remains indeterminate
ERROR_NOT_SUPPORTED / 50 remains unsupported
ERROR_ALREADY_EXISTS / 183 may count as expected only in exact authorized
collision fixtures with destination preservation verified
unknown native errors remain fail-closed
```

## 17. Artifact Persistence and Verification

Authorize one isolated retained result directory outside the repository. It
must be:

```text
new
absent before execution
ordinary
non-reparse
local fixed NTFS
inside an explicitly admitted parent
bound to the future authorization identity
```

The implementation must fail closed if the directory, gate-entry object,
terminal object, or retained artifact already exists. No overwrite or merge
behavior is authorized.

Minimum retained result directory contents:

```text
gate_entry.canonical.json
retained_terminal_artifact.canonical.json
```

The future implementation may choose stricter filenames that include the
authorization identity, but it must reject any final path collision and must
record the exact names in its findings.

Artifact durability method:

```text
write canonical bytes to a new file only
flush file bytes
close
reread
compare bytes
compute SHA-256
sync containing directory entry through the BLOCKER-1 directory-durability
adapter where the bounded Windows profile admits it
reread again after directory sync
parse canonical JSON
schema-validate
recompute record hash
recompute artifact-envelope observations
verify terminal consistency
```

BLOCKER-1 directory durability may support only retained evidence persistence.
It must not be described as native rename durability, native rename atomicity,
source-parent persistence, destination-parent persistence, or power-loss
persistence for the tested rename operation.

Hash model:

```text
case_set_sha256 = SHA-256(canonical_json_bytes(case_set_declaration))
fixture_profile_sha256 = SHA-256(canonical_json_bytes(fixture_profile_declaration))
gate_entry_record_sha256 = SHA-256(canonical_json_bytes(gate_entry_record))
terminal_record_sha256 = SHA-256(canonical_json_bytes(terminal_record))
artifact_wrapper_sha256 = SHA-256(reread bytes of retained_terminal_artifact.canonical.json)
source_checked_out_sha256 = SHA-256(actual checked-out bytes supplied to Python)
```

The artifact wrapper records `terminal_record_sha256` and
`terminal_record_byte_length`. It must not record `artifact_wrapper_sha256`
inside the same wrapper. The wrapper file SHA-256 is an external admission
observation returned by the implementation and included in the later findings.

`retained_execution = true` is authorized only when all of the following are
true:

```text
durable gate entry was established
all selected gating cases reached expected bounded outcomes
A5 identity continuity succeeded
A2 and A3 destination preservation succeeded
all required raw errors were retained
canonical terminal artifact was written
required file durability completed
required directory-entry durability completed under BLOCKER-1's bounded profile
artifact was reread
artifact was schema-verified
artifact hash was recomputed and matched
terminal state was internally consistent
```

Native execution beginning is insufficient. Native execution finishing is
insufficient. Process exit code zero is insufficient.

## 18. Interruption Semantics

The implementation may add best-effort handling for:

```text
KeyboardInterrupt
ordinary Python exceptions
known process-level failure paths inside Python control
```

Before gate entry, interruption or exception must not consume authority unless
the durable gate-entry marker has already been established.

After gate entry, the implementation should attempt to durably record a
truthful terminal failure object when possible:

```text
terminal_state = RUN_INTERRUPTED
gate_consumed = true
retained_execution = false
primary_failure = RUN_INTERRUPTED
```

This authorization does not claim universal capture of:

```text
power removal
kernel termination
process kill beyond catchable handling
machine crash
filesystem failure
```

An existing durable gate marker without a valid completion artifact must remain
evidence of consumed but incomplete authority. It must not be silently resumed.

## 19. Test and Fault-Injection Requirements

The later implementation must include unit tests for at least:

```text
default mode cannot enter retained execution
unknown mode fails closed
wrong authorization identity fails preflight
wrong assessment identity fails preflight
wrong repository identity fails preflight
dirty relevant source fails preflight
source byte hash mismatch fails preflight
wrong policy identity fails preflight
wrong case-set identity fails preflight
wrong fixture-profile identity fails preflight
existing retained directory fails closed
existing gate marker fails closed
preflight rejection does not consume authority
gate-entry persistence failure prevents native invocation
durable gate entry precedes first native invocation
post-gate failure leaves authority consumed
A1/A2/A3/A5 are gating
A6 is optional and non-gating
A4/A7/A8 cannot be selected
unexpected native error cannot become completion
collision success requires destination preservation
A5 requires all identity comparisons
artifact serialization failure cannot complete
artifact durability failure cannot complete
artifact reread failure cannot complete
artifact hash mismatch cannot complete
invalid terminal-state combinations are rejected
retained_execution cannot become true early
completed record rereads and verifies
interrupted post-gate run remains consumed
same authorization cannot be reused
BLOCKER-2 is not automatically closed
```

Windows integration tests must:

```text
use isolated temporary roots outside the repository
use synthetic authorization fixtures
use non-authoritative identities
use temporary output roots
avoid authoritative retained result locations
avoid authoritative authorization identities
declare authoritative = false
declare retained_execution = false unless final record assembly is entirely
in memory
```

No test may consume authoritative authority. No test may create an
authoritative retained artifact.

Fault injection is authorized only at exact retained-infrastructure points:

```text
before gate persistence
during gate file write
during gate directory flush
after gate verification but before native call
after selected case execution
during terminal serialization
during terminal file write
during terminal directory flush
during terminal reread
during terminal hash verification
```

Fault injection must:

```text
be disabled by default
be impossible to confuse with authoritative execution
be recorded explicitly in test results
never be accepted in a successful retained artifact
```

Broad arbitrary monkey-patching surfaces are not authorized.

## 20. Claims Supported

This authorization supports these claims:

```text
A later implementation-preparation phase is justified.
The implementation surface can be bounded to one retained module, focused tests,
and minimal existing-runner wiring.
Existing absolute-path control policy identity remains preserved and distinct
from the RootDirectory-relative policy identity.
Existing public canonicalization, hash, identity, fixture, A-case, and
BLOCKER-1 directory-durability primitives can be reused where their semantics
match the retained evidence boundary.
```

This authorization supports preparing infrastructure for a future retained run.
It does not support executing that run.

## 21. Claims Not Supported

This authorization does not support these claims:

```text
the authoritative retained run has been authorized
the authoritative retained run has been executed
an authoritative gate has been consumed
an authoritative retained artifact has been created
BLOCKER-2 closed
general FileRenameInfo support exists
RootDirectory-relative FileRenameInfo is invalid
rename is atomic
rename is durable
parent flush proves rename persistence
power-loss persistence is established
production readiness exists
real-world Brainvision readiness exists
strong order sensitivity exists
BLOCKER-4 progress exists
```

The retained artifact remains platform evidence only. It is not:

```text
IMMUTABLE_SCIENTIFIC_BUNDLE
SCIENTIFIC_COMPLETION
publication readiness
```

## 22. Prohibited Actions

The later implementation phase must not:

```text
execute the authoritative retained run
consume authoritative retained-run authority
create an authoritative retained artifact
create an authoritative gate-entry marker
close BLOCKER-2
start BLOCKER-4
modify production code
modify torment_service/kernel/
publish
create SCIENTIFIC_COMPLETION
create IMMUTABLE_SCIENTIFIC_BUNDLE
alter publication state
alter scientific truth
add FileRenameInfoEx
add MoveFileW
add MoveFileExW
add ReplaceFileW
add copy/delete fallback
add replace-existing native behavior
add cross-volume retained behavior
add live-data behavior
add force, retry, resume, or ignore-existing-gate behavior
commit
push
tag
stash
reset
clean
checkout another branch
delete files
delete .git/index.lock
```

The implementation must not encode:

```text
BLOCKER_2_CLOSED
```

A successful future retained run may support a later docs-only closure
assessment. It cannot close the blocker automatically.

## 23. Required Implementation Report

The later implementation report must include:

```text
BASELINE
- branch
- HEAD
- origin/main
- initial working-tree state

AUTHORIZED FILES
- exact modified and created paths
- confirmation no other files changed

IMPLEMENTATION
- retained mode
- schema
- canonicalization
- repository/source admission
- policy and case-set binding
- fixture-profile binding
- gate-entry mechanism
- single-run enforcement
- terminal-state model
- interruption handling
- artifact durability
- reread verification
- hash model
- failure taxonomy

IDENTITIES
- source Git blob identities
- checked-out-byte SHA-256 identities
- schema identity
- case-set identity
- fixture-profile identity
- policy identity
- any new declaration identities

TESTS
- exact commands
- exact pass/fail counts
- Windows versus non-Windows results
- skipped tests and reasons
- fault-injection coverage

BOUNDARY CONFIRMATION
- no authoritative retained run executed
- no authoritative gate consumed
- no authoritative artifact created
- no production files modified
- BLOCKER-2 remains open
- BLOCKER-4 not started

GIT STATE
- git status --short
- git diff --name-only
- git diff --stat

LIMITATIONS
- claims not established
- unresolved implementation questions
```

The implementation report must not commit or push.

## 24. Authorization Verdict

Machine-readable decision block:

```text
authorization_verdict = "AUTHORIZE_NARROW_RETAINED_SINGLE_RUN_IMPLEMENTATION_PREPARATION"
authorized_task = "NARROW_IMPLEMENTATION_PREPARATION"
authoritative_retained_run_authorized = false
authoritative_retained_run_executed = false
authoritative_gate_consumed = false
authoritative_artifact_created = false
blocker_2_state = "OPEN"
blocker_4_started = false
production_integration_authorized = false
authorized_architecture = "B"
authorized_retained_mode = "BLOCKER2_ABSOLUTE_PATH_CONTROL_RETAINED_SINGLE_RUN_V0_1"
authorized_completion_gating_cases = "A1,A2,A3,A5"
authorized_optional_non_gating_cases = "A6"
excluded_cases = "A4,A7,A8"
```

The authorization is justified because it defines:

```text
a bounded architecture
an exact file surface
truthful gate semantics
truthful terminal semantics
no retained execution authority
adequate test requirements
no production widening
```

## 25. Exact Next Step

Exact next step:

```text
Codex may implement the narrow retained-execution preparation infrastructure
within only the file surface authorized in this document, then report findings
without executing or authorizing an authoritative retained run.
```

A. AUTHORIZE_NARROW_RETAINED_SINGLE_RUN_IMPLEMENTATION_PREPARATION
