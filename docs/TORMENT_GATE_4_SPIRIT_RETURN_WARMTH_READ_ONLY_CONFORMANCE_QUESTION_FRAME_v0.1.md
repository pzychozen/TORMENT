# TORMENT Gate 4 — Spirit-Return Warmth / Warmup Read-Only Conformance-Question Frame v0.1

**DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — QUESTION FRAMING ONLY, NO REMEDY. NO IMPLEMENTATION,
NO TESTS, NO RUNTIME, NO VERDICT.**

This frame poses — read-only — the conformance question for exactly one Gate 4 core runtime row:
**spirit-return warmth / warmup**. **It poses the question; it does not answer it.** It selects no remedy,
no patch, no fix order, no enforcement, no governance vehicle, no runtime authority gate, no O6 mechanic,
no must-not-pin implementation, no schema/carrier, no `canon_source`, no source-sameness mechanic, and no
database/substrate decision. It makes no safe/unsafe verdict, no defect verdict, no canon claim, and no
authority claim.

**Operator-supplied docs-only state:** `8faabd8` (*docs(gate-4): point orientation map to derived anchors
frame*), treated as the current operator-supplied state; the older Gate 4 stop preconditions are
superseded **only** by the operator-named docs-only commits in the Gate 4 lane (`a92dbef` → `dd53b7e` →
`f6d0fb3` → `41c4349` → `f4ee79a` → `03af2c3` → `7090a11` → `cc6cac4` → `f1500f6` → `8953b90` →
`8faabd8`). No `git` was run. **Date:** 2026-06-20. Windows-visible tracked repo state remains
authoritative.

> Memory may shape context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.

**Tags:** `[FACT]` point-in-time, carried from prior read-only artifacts · `[BOUNDARY]` already-promoted
requirement obligation · `[QUESTION]` posed only, not answered · `[POSTURE]` conformance posture ·
`[DISTINCTION]` · `[CONTEXT]` carried context, not scope · `[NON-CLAIM]` · `[NON-AUTHORIZATION]` ·
`[STOP]`.

---

## 0. Provenance, precondition, and review status

`[FACT]` **Lineage.** Gate 4 selection frame → conformance-gap map → `gravity_correction` (A) →
mood-topology (B) → derived identity anchors (C) → **this spirit-return warmth / warmup frame.** In the
reviewed framing sequence (A → B → C → **D**), row **D** ("warmth / role-cadence soft O6 paths") is split
into **D1 = spirit-return warmth (this frame)** and **D2 = role inference / identity-anchor cadence (a
separate frame)**. This is a **framing sequence, not a fix order**. It is a **question frame**, not
reconciliation work and not a "row to reconcile"; it poses the Gate-4-lane conformance question and
answers nothing.

`[DISTINCTION]` **D1 and D2 are distinct runtime surfaces and must not be conflated.** D1 (spirit-return
warmth / warmup) is retrieval-side shaping of deep / spirit-return hits; D2 (role inference → identity-
anchor cadence) modulates the separate derived-anchor emitter (the row-C surface). This frame is **D1
only** and treats D2 as out-of-scope (§1.2).

`[FACT]` **Review status: Codex challenge completed: ACCEPT WITH CORRECTIONS; corrections applied;
pending operator review / promotion.** The file remains **docs-only and non-authorizing** and changes no
behavior; nothing downstream may treat it as ratified, or as authorizing implementation, remedy, writer
patch, enforcement, substrate, or any successor gate, until operator review / promotion.

`[FACT]` **Precondition honored.** This frame is expressible **read-only and non-substrate** (§1); no
forbidden item (§6) was required to write it. Per the Gate 4 lane `[STOP]` conditions, had posing the
question required runtime/endpoint/service/ingest-query/database/workspace-frequency evidence, a verdict,
a remedy, a writer selection, a fix order, an enforcement path, a governance vehicle, an authority gate,
an O6 / must-not-pin mechanic, or any substrate decision, drafting would have stopped instead. It did not.

### 0.1 Relationship to prior artifacts (load-bearing)

`[DISTINCTION]` This frame **adds no scope beyond the conformance question** and duplicates nothing:

- **Gate 4 conformance-gap map §2.4** already recorded this row's posture as *not-yet-conformant /
  requires reconciliation* at the O6 soft-guidance seam, topology of effect characterized as retrieval-
  side shaping. This frame states the questions that posture leaves open.
- **Spirit-return warmth / warmup trace** and **pre-substrate Family F** are the carried point-in-time
  characterization (durable-soft, non-canon, O6-parked). They are not re-opened or re-measured.
- The **gravity (A)**, **mood-topology (B)**, and **derived-anchors (C)** frames are separate rows; this
  frame **does not re-answer** their questions and shares no scope with them.

---

## 1. Status, scope, method

`[FACT]` This is a **docs-only read-only conformance-question frame** scoped to **exactly one row**:
spirit-return warmth / warmup. It is **no implementation, no tests, no runtime, no service, no endpoint,
no ingest/query, no instrumentation, no remedy, no verdict, no database/substrate.** Every `[FACT]` below
is **point-in-time and carried** from prior read-only artifacts (gap map §2.4, the spirit-return warmth /
warmup trace, pre-substrate Family F, Stage A O6, Seed-Gov SG-O6/SG-O8). **No new tracing, execution,
measurement, or frequency/workspace observation was performed**; specifics live in those artifacts at
their baselines and are not re-asserted as current here.

`[POSTURE]` **Required posture, carried verbatim in intent:** this is a *read-only conformance-question
frame*; warmth is a *durable-soft, non-canon* signal whose *conformance posture is not yet resolved*
(*not-yet-conformant / requires reconciliation* at the O6 soft-guidance seam); this is *question framing
only, no remedy*. Per Stage A O6 / Seed-Gov posture, the not-yet-conformant flag is **not** a must-patch
order; audit observes the seam without becoming authority.

`[NON-CLAIM]` **Carried boundary classification (load-bearing).** Warmth is **retrieval-side / prompt-
shaping only**: it writes **no canon** and **no graph memory**, and makes **no authority crossing**.
**"Durable" ≠ "pinned."** Warmth-influenced block classification — including possible **identity-block
placement** for resonance / high-warmth spirit-return hits — is a **prompt-assembly classification**, not
a canon, admission, promotion, or authority decision. This frame asserts none of "warmth pins identity",
"warmth is authority", "patch warmth", "block warmth", or "filter warmth".

### 1.2 Context that is not scope

`[CONTEXT]` Excluded from scope and **not** posed here: role inference / role cadence (**D2**, a separate
frame and a distinct surface); derived identity anchors (**C**); `gravity_correction` (**A**);
mood-topology (**B**); authored `seed_text`; H3 / H5 / H6; chamber / private cognition; database /
substrate. They appear, if at all, only as out-of-scope notes, never as scope.

---

## 2. Carried facts (point-in-time; no new claims)

`[FACT]` Carried from the spirit-return warmth / warmup trace, gap map §2.4, and pre-substrate Family F
(point-in-time, not re-measured):

- Warmth is a **durable-soft, non-canon** retrieval / prompt-shaping signal **tracked per deep-memory
  EID** (retrieval-history-based).
- It is **persisted in `warmup_state.jsonl`**, **append-only with periodic compaction**, and
  **path-integrity hardened** (storage path derived through the canonical sanitizer); persistence is
  already characterized in prior artifacts.
- It **scales spirit-return hit strength through a per-mode multiplier** (resonance / surfacing /
  recollection), and attaches `spirit_return_mode` / `warmth_score`.
- **`return_mode` + warmth thresholds may influence block classification, including possible
  identity-block placement** for resonance / high-warmth hits (and warmth is a secondary ordering key).
- This is **retrieval-side shaping only** — **no canon**, **no graph-memory write**, **no authority
  crossing**.

`[NON-CLAIM]` This records the shaping surface only. It asserts no magnitude, no frequency, and no claim
that warmth pins identity, carries authority, or should be patched, blocked, or filtered.

---

## 3. Already-promoted boundaries this row is read against

`[BOUNDARY]` Named as the obligations the question is posed against — naming a boundary is not
implementing it; **no O6 mechanic, no must-not-pin implementation**, and no Seed-Gov mechanic is selected:

- **Stage A — O6** (preserve-without-pinning: durable-soft guidance must **not** be durably pinned into
  rigid control) — warmth is recorded as **O6-parked** (Family F).
- **Seed-Governance — SG-O6** (soft guidance must not silently become seed/canon authority, directly or
  through an automatic chain) and **SG-O8** (recovery preserves authored canonical state without pinning
  or locking; reversibility and future governed change remain open) — named as the preserve-without-
  pinning posture warmth's persistence/recovery is read against.
- **Pre-substrate Family F** (durable spirit-return warmth: non-canonical; durable-soft, **O6 PARKED**;
  "durable-soft must-not-pin"; later owner Stage A O6 soft-guidance seam) and **gap map §2.4** (this
  row's recorded posture). The Ledger Observational-Boundary ("audit observes authority; audit does not
  become authority") governs this frame's own posture.

---

## 4. The conformance questions (posed only — not answered)

`[QUESTION]` Each item is **posed and left open**. None is answered; no remedy, fix order, writer-patch
selection, enforcement, governance vehicle, authority gate, canon claim, authority claim, O6 mechanic, or
must-not-pin implementation is offered or implied. "Conformance posture not yet resolved" stands for every
one.

- **Q-W1 — durable-soft persistence vs must-not-pin.** Does **durable-soft warmth persistence** (per-EID
  state in `warmup_state.jsonl`) require a **resolved must-not-pin posture under Stage A O6**? *Posed
  only; not answered here; no O6 mechanic or must-not-pin implementation selected.*
- **Q-W2 — warmth-influenced identity-block placement.** Does **warmth-influenced identity-block
  placement** require a **resolved soft-guidance posture** — **without treating block placement as canon,
  admission, or authority** (it remains a prompt-assembly classification)? *Posed only; not answered
  here.*
- **Q-W3 — recovery / persistence preserve-without-pinning.** Does warmth's **recovery / persistence
  model** preserve **without pinning** under the **SG-O8** posture (preserve without pinning or locking;
  reversibility open)? *Posed only; not answered here.*
- **Q-W4 — already-recorded routing.** Which **later-owner routings for this row are already recorded** by
  prior artifacts — **without this frame creating, selecting, or re-picking any**? *Posed only.*
  `[POSTURE]` As carried fact (not a selection): pre-substrate Family F and gap map §2.4 route this row to
  the **Stage A O6 soft-guidance seam**; the warmth / warmup trace records it as **O6-parked**. This frame
  **re-picks none, selects none, creates none, and resolves none** of them.

---

## 5. Sequence / surface-distinction note (framing sequence, not a fix order)

`[DISTINCTION]` This is **row D1**. **D1 (warmth) and D2 (role inference / identity-anchor cadence) are
distinct runtime surfaces** and must not be treated as the same surface: D1 shapes deep / spirit-return
retrieval per deep-memory EID; D2 modulates the derived-anchor emitter's cadence (the row-C surface).
The **D2 role-cadence frame would be eligible only as a later separately authorized frame**; this frame
neither opens nor schedules it. The reviewed order — A → B → C → **D (D1 here; D2 separate)** — is a
**framing sequence, not a fix order**, and selects, schedules, and prioritizes no remedy.

---

## 6. What this frame does NOT do or authorize

`[NON-AUTHORIZATION]` This frame poses a question and stops. It does **not** answer it, and authorizes
none of, and must not be read as implying:

```
answer / resolution of the question · safe/unsafe verdict · defect verdict · canon claim · authority claim
"warmth pins identity" · "warmth is authority" · "patch warmth" · "block warmth" · "filter warmth"
remedy · writer patch · fix order · enforcement behavior · governance vehicle · runtime authority gate
O6 mechanics · must-not-pin implementation · Seed-Governance mechanics · canon_source · source-sameness / P4 mechanics
re-answering the A / B / C frames' questions · treating D1 and D2 as the same runtime surface
code · tests · runtime · endpoint / service calls · ingest / query · workspace-frequency evidence
schema / store / carrier / serialization / migration · candidate store · governed admission implementation
seed rewrite · seed-revision API · chamber / private-cognition runtime · dream / incubation runtime
database / substrate · hidden chain-of-thought storage or exposure · hidden output blocker / finalizer
identity pinning · monitoring / autonomy layer · coercive mechanism
broadening to role-cadence (D2) / derived anchors (C) / gravity (A) / mood-topology (B) / seed_text / H3 / H5 / H6
```

`[NON-AUTHORIZATION]` It amends no upstream contract (Document A / Document B / Seed-Governance / P4 /
Stage A / Cluster 2 / Ledger / MCP boundary), opens no Stage B, opens no autonomy, **updates no
orientation map**, and makes **no registry amendment**.

---

## 7. Stopping evidence

`[STOP]` Stop, and do not extend this frame or draft any follow-up, if continuing would require: an
answer/resolution rather than a posed-and-open question; a safe/unsafe or defect verdict; a canon or
authority claim; any "warmth pins identity" / "warmth is authority" / "patch warmth" / "block warmth" / "filter warmth"
statement; any runtime / endpoint / service / ingest-query / database / workspace-frequency evidence; a
selected writer patch, fix order, enforcement behavior, governance vehicle, or runtime authority gate; an
O6 mechanic, must-not-pin implementation, Seed-Gov mechanic, `canon_source`, or source-sameness mechanic;
a schema / carrier / migration / database decision; any chamber / candidate-store / seed-revision /
governed-admission construction; a hidden output blocker / finalizer; a hidden chain-of-thought store;
identity pinning; a monitoring / autonomy / coercive layer; re-answering the A/B/C frames' questions; or
treating D1 and D2 as the same runtime surface. Also stop if the operator-supplied docs-only state is not
`8faabd8` (or its named Gate-4-lane predecessors), or if the question can no longer be posed read-only and
non-substrate.

---

## 8. Next step after this frame

`[NON-AUTHORIZATION]` This frame is a **draft for Codex challenge (Codex as first reviewer) and operator
review**. **No automatic successor gate. No automatic implementation.** Any later step — including whether
the role-cadence (D2) frame is ever drafted, or whether any owner ever resolves Q-W1–Q-W4 — requires
**separate authorization** and an explicitly-opened step; nothing here selects or schedules it. The
orientation map is **not** updated by this artifact; any pointer is a separate later step. Database /
substrate remains last.

---

## Anti-drift footer

DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — question framing only, no remedy; no implementation, no
tests, no runtime, no service, no endpoint, no ingest / query, no workspace-frequency evidence, no
answer/resolution, no safe/unsafe verdict, no defect verdict, no canon claim, no authority claim, no
"warmth pins identity" / "warmth is authority" / "patch warmth" / "block warmth" / "filter warmth", no writer-patch
selection, no fix order, no enforcement, no governance vehicle, no runtime authority gate, no O6
mechanics, no must-not-pin implementation, no Seed-Governance mechanics, no `canon_source`, no
source-sameness / P4 mechanics, no schema / store / carrier / serialization / migration, no candidate
store, no governed admission implementation, no seed rewrite, no seed-revision API, no chamber /
private-cognition runtime, no dream / incubation runtime, no database / substrate, no hidden
chain-of-thought storage or exposure, no hidden output blocker / finalizer, no identity pinning, no
monitoring / autonomy / coercive layer, no orientation-map update, no registry amendment. Scoped to one
row — spirit-return warmth / warmup — a **durable-soft, non-canon** retrieval / prompt-shaping signal
tracked **per deep-memory EID**, persisted in `warmup_state.jsonl` (append-only with compaction;
path-integrity hardened), scaling spirit-return hit strength through a per-mode multiplier, where
`return_mode` + warmth thresholds **may influence block classification including possible identity-block
placement**. This is **retrieval-side shaping only — no canon, no graph-memory write, no authority
crossing**; **"durable" ≠ "pinned"**, and identity-block placement is a **prompt-assembly classification**,
not canon / admission / authority. Posed as **not-yet-conformant / requires reconciliation** at the O6
soft-guidance seam whose **conformance posture is not yet resolved**; Q-W1–Q-W4 are **posed only, not
answered**. **D1 (warmth) and D2 (role-cadence) are distinct runtime surfaces** and are not conflated;
D2 would be eligible only as a later separately authorized frame; the A→B→C→D order is a **framing
sequence, not a fix order**. This frame **does not re-answer** the A / B / C frames' questions.
conformance characterization ≠ enforcement; audit visibility ≠ authority; inspection ≠ projection. Audit
observes authority and does not become authority. Memory may shape context. Memory may not seize
authority. Database / substrate remains last.
