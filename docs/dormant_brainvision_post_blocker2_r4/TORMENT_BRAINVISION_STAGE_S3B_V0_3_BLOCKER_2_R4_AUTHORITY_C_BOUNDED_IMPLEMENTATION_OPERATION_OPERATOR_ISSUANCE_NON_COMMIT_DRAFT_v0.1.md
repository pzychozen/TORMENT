# TORMENT Brainvision Stage S3B v0.3 BLOCKER-2 R4 Authority-C Bounded Implementation Operation Operator Issuance Non-Commit Draft v0.1

## 1. Document Status

```text
classification:
AUTHORITY_C_BOUNDED_IMPLEMENTATION_OPERATION_OPERATOR_ISSUANCE_NON_COMMIT_DRAFT_COMPLETE_FOR_INDEPENDENT_REVIEW

document state:
DRAFT ONLY
NOT ISSUED
NOT ACTIVE
NOT COMMITTED
NOT PUSHED

implementation contact:
NOT STARTED

implementation opportunity:
NOT CONSUMED

operator:
Hilmir
```

This document is an operator-issuance draft only. It prepares a later explicit
non-commit operator issuance act for Hilmir. It does not issue itself, does not
authorize implementation contact, does not start implementation, does not
consume the implementation opportunity, does not activate Authority C, D, or E,
does not contact the canonical-input path, and does not stage, commit, or push.

The distinct acts are:

```text
this document:
issuance draft only

future explicit operator issuance:
may issue the accepted bounded implementation-operation authorization

future bounded implementation operation:
may begin only after valid issuance and successful pre-contact verification

later implementation-result capture:
records operation evidence after implementation edit closure
```

## 2. Schema And Classification

```text
schema:
torment.brainvision.blocker2.r4.authority_c.bounded_implementation_operation_operator_issuance_non_commit_draft.v0.1

operation_label:
AUTHORITY_C_BOUNDED_IMPLEMENTATION_OPERATION_OPERATOR_ISSUANCE_NON_COMMIT_DRAFT

draft_classification:
AUTHORITY_C_BOUNDED_IMPLEMENTATION_OPERATION_OPERATOR_ISSUANCE_NON_COMMIT_DRAFT_COMPLETE_FOR_INDEPENDENT_REVIEW
```

This draft contains no whole-document byte count, SHA-256, line count, or
formatting identity for itself. Its identity must be computed externally only
after the bytes are complete and frozen.

## 3. Authority-Chain Binding

Repository:

```text
repository root:
C:\TORMENT\TORMENT_repo\TORMENT-fabric_v2\torment_fabric

branch:
main

accepted invocation HEAD:
1f915e29119cd58ea39e8cf355f7364118c71043

expected local origin/main:
1f915e29119cd58ea39e8cf355f7364118c71043
```

Implementation-authorization design identity:

```text
path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_IMPLEMENTATION_NON_COMMIT_AUTHORIZATION_DESIGN_v0.1.md

byte count:
48193

SHA-256:
2a06ee4aae319d72e3447195b1adbba8703ea6d23a49ae4fec6d08460e2e749a
```

Invocation-form design identity:

```text
path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_INVOCATION_FORM_DESIGN_v0.1.md

byte count:
59456

SHA-256:
bde299d389572c8cd25d2a0e9a7aa60f56009edd8cf96311f2c1abc100749f58
```

Issued opening-declaration identity:

```text
path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_IMPLEMENTATION_OPENING_NON_COMMIT_DECLARATION_DRAFT_v0.1.md

byte count:
20088

SHA-256:
76afe53c19b1dd1115732c7835c61743eed2975e1cc18d4ba6c99f9836dec6b7

state:
ISSUED_NON_COMMIT
```

Acceptance-for-issuance record identity:

```text
path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_IMPLEMENTATION_OPENING_ACCEPTANCE_FOR_ISSUANCE_NON_COMMIT_RECORD_DRAFT_v0.1.md

byte count:
9964

SHA-256:
5666b8623de28f99b137b83f2a82a60bbcf10229cd4fe08be4e2fa2faac882aa
```

Opening operator-issuance packet identity:

```text
path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_IMPLEMENTATION_OPENING_OPERATOR_ISSUANCE_PACKET_DRAFT_v0.1.md

byte count:
12420

SHA-256:
2f8276fc6501e6f839314706a8b7cda4d40060f3279b30d88e09af7411a407ae
```

Accepted corrected bounded-operation authorization identity:

```text
path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_BOUNDED_IMPLEMENTATION_OPERATION_NON_COMMIT_AUTHORIZATION_DRAFT_v0.1.md

byte count:
26785

SHA-256:
67b8f17aafe000290607016ca646cfb778dc9a75cc1165e2d3bff2988d3b368f

independent review classification:
B. AUTHORITY_C_BOUNDED_IMPLEMENTATION_OPERATION_AUTHORIZATION_CORRECTED_DRAFT_ACCEPTED_WITH_NON_BLOCKING_CLARIFICATIONS
```

Implementation opportunity key:

```text
implementation_opportunity_key:
4749e68ef567e0eebad6b64db8a00e47ac7df83b6ece6cd63523e6c522042f43

canonical JSON byte length:
646
```

Exact canonical opportunity-key preimage:

```json
{"accepted_invocation_head":"1f915e29119cd58ea39e8cf355f7364118c71043","authorised_modified_files":["research/brainvision/run_blocker2_authoritative_retained_single_run_v0_1.py"],"authorised_new_files":["research/brainvision/blocker2_r4_authority_c_candidate_v0_1.py","research/brainvision/test_blocker2_r4_authority_c_candidate_v0_1.py"],"implementation_authorisation_sha256":"2a06ee4aae319d72e3447195b1adbba8703ea6d23a49ae4fec6d08460e2e749a","implementation_opening_declaration_sha256":"76afe53c19b1dd1115732c7835c61743eed2975e1cc18d4ba6c99f9836dec6b7","schema":"torment.brainvision.blocker2.r4.authority_c.implementation_opportunity_key.v0.1"}
```

Runner identity:

```text
path:
research/brainvision/run_blocker2_authoritative_retained_single_run_v0_1.py

Git blob OID:
79d0c89575919c8506c8b9f1278efd5d63b1e813

checked-out byte SHA-256:
c61de2f7829e3eb2bb938701f98bbafe54f09f03dab8abd718d421a81b56e976

checked-out byte length:
46788
```

Retained-control identity:

```text
path:
research/brainvision/blocker2_retained_absolute_path_control_v0_1.py

Git blob OID:
1779715ed17fffe3a927d24eb445eec51f3d42d6

checked-out byte SHA-256:
dc4a6e3f1169c33a2379c3506d107893cb8a48c977300c22bda14db0bf19e3d5

checked-out byte length:
144698

status:
REFERENCE ONLY
MUST REMAIN BYTE-IDENTICAL
```

Current governance state bound by this draft:

```text
FORMAL_HOLD:
ACTIVE

BLOCKER-2:
OPEN

BLOCKER-4:
INACTIVE

Authority C:
INACTIVE

Authority D:
INACTIVE

Authority E:
INACTIVE

canonical-input path:
NOT CONTACTED
```

## 4. Prepared Future Operator Issuance Text

```text
PREPARED ONLY
NOT ISSUED
```

Prepared future non-commit operator act:

```text
I, Hilmir, acting as the authoritative Windows operator, explicitly issue the
accepted bounded implementation-operation authorization as a non-commit
operator act.

accepted bounded implementation-operation authorization path:
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_BOUNDED_IMPLEMENTATION_OPERATION_NON_COMMIT_AUTHORIZATION_DRAFT_v0.1.md

accepted bounded implementation-operation authorization byte count:
26785

accepted bounded implementation-operation authorization SHA-256:
67b8f17aafe000290607016ca646cfb778dc9a75cc1165e2d3bff2988d3b368f

implementation_opportunity_key:
4749e68ef567e0eebad6b64db8a00e47ac7df83b6ece6cd63523e6c522042f43

accepted invocation HEAD:
1f915e29119cd58ea39e8cf355f7364118c71043

branch:
main

bounded implementation operation authorization transitions from:
DRAFT / NOT ISSUED

to:
ISSUED_NON_COMMIT

authorized operation count:
ONE

implementation contact:
NOT STARTED BY THIS ISSUANCE

implementation opportunity:
NOT CONSUMED BY THIS ISSUANCE

Authority C:
INACTIVE

Authority D:
INACTIVE

Authority E:
INACTIVE

canonical-input path:
NOT CONTACTED
```

Valid future issuance authorizes exactly one future bounded implementation
operation. It does not itself cross implementation contact and does not itself
consume the implementation opportunity.

## 5. First-Contact And Consumption Binding

The unique authorized first-contact event is the earliest of:

```text
direct creation or direct opening-for-write of:
research/brainvision/blocker2_r4_authority_c_candidate_v0_1.py

direct creation or direct opening-for-write of:
research/brainvision/test_blocker2_r4_authority_c_candidate_v0_1.py

direct mutation or direct opening-for-write of:
research/brainvision/run_blocker2_authoritative_retained_single_run_v0_1.py
```

Immediately before that first authorized write event:

```text
implementation_contact_started:
true

implementation_opportunity_consumed:
true
```

The transition is atomic and irreversible.

The following do not consume the implementation opportunity:

```text
read-only inspection
identity verification
repository-state verification
governance drafting
independent review
operator issuance
implementation planning
test planning
```

## 6. Direct-Write Rule

Authorized implementation writes must be direct writes to the authorized path
itself. Implementation writes through repository temporary or sidecar paths are
prohibited, including:

```text
.tmp
.bak
~
.swp
.orig
.rej
temporary implementation files
atomic-write temporary files
temp-then-rename files
editor backup files
swap files
patch scratch files
implementation-sidecar files
```

The examples are not exhaustive.

A prohibited temporary or sidecar path is not a valid first-contact event. It
is an unauthorized-path contact and implementation-surface violation. If a
selected tool cannot write directly to the authorized paths without repository
artefacts, the operation must stop before first contact and report scope
expansion. Later deletion does not cure a violation.

The implementation opportunity state follows the accepted authorization based
on whether an authorized qualifying first-contact event had already occurred
before the violation.

## 7. Authorized Write Surface

The future issued operation may authorize only:

```text
NEW:
research/brainvision/blocker2_r4_authority_c_candidate_v0_1.py

NEW:
research/brainvision/test_blocker2_r4_authority_c_candidate_v0_1.py

MODIFIED:
research/brainvision/run_blocker2_authoritative_retained_single_run_v0_1.py
```

The issued operation may separately authorize exactly one implementation-result
governance document:

```text
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_IMPLEMENTATION_RESULT_v0.1.md
```

That result document is not a fourth Python implementation path.

The retained-control source remains:

```text
research/brainvision/blocker2_retained_absolute_path_control_v0_1.py
```

with status:

```text
REFERENCE ONLY
MUST REMAIN BYTE-IDENTICAL
```

## 8. Pre-Contact Verification Condition

This issuance grants no permission to cross first contact unless the full
pre-write verification in the accepted bounded-operation authorization passes
immediately before contact.

Minimum fail-closed checks:

```text
repository root exact
branch main
HEAD exact
local origin/main exact
HEAD equals local origin/main
index lock absent
staged entries none
tracked deletions none
unmerged entries none
expected untracked inventory only
no unexpected untracked entry
no genuine tracked content modification
known 303-file CRLF-only state re-proved unchanged
governance identities exact
opportunity key and canonical preimage exact
runner identity exact
retained-control identity exact
authorized new Python paths absent
runner unmodified
Authority C/D/E inactive
FORMAL_HOLD active
canonical-input path untouched
```

Any failure must stop before first contact and leave the implementation
opportunity unconsumed, provided no qualifying authorized write or
opening-for-write has occurred.

## 9. Prohibited Operations

The future issued operation preserves all prohibitions in the accepted bounded
operation authorization, including:

```text
Authority-C activation
Authority-C invocation
calling run_authority_c_candidate_construction
candidate construction
Authority-D activation or action
Authority-E activation or action
canonical-input path lookup
canonical-input existence probing
canonical-input stat
canonical-input open
canonical-input creation
canonical-input write
governed-runner execution
PREPARE_PATHS
PREFLIGHT_ONLY
EXECUTE_EXACT_SINGLE_RUN
candidate-byte publication
production kernel contact
torment_service/kernel modification
service/runtime integration
network or external-service contact
Git staging
commit
push
checkout
branch change
reset
clean
stash
rebase
merge
repository-wide formatter
line-ending normalization
modification of any unauthorized path
```

## 10. Testing Condition

The issuance may authorize only the focused synthetic and non-authoritative
tests permitted by the accepted authorization.

Where Python is run:

```text
PYTHONDONTWRITEBYTECODE=1
python -B
```

Every focused test command must be recorded verbatim in the
implementation-result document's `test_commands` field, with its result
recorded in `test_results`.

No broad repository-wide test command is authorized. No test may contact a
governed path.

## 11. Terminal-State Binding

The operation terminates under exactly one primary terminal classification.

Accepted secondary co-classifications:

```text
IMPLEMENTATION_SCOPE_EXPANSION_REQUIRED
```

after first contact must also be classified:

```text
IMPLEMENTATION_ABORTED_AFTER_MODIFICATION
```

and:

```text
IMPLEMENTATION_REPOSITORY_STATE_INVALID_POST_CONTACT
```

must also be treated or co-classified as:

```text
IMPLEMENTATION_ABORTED_AFTER_MODIFICATION
```

No other secondary classification is created by this issuance draft.

All post-contact failure states leave the opportunity consumed or UNKNOWN
according to evidence.

## 12. Corrected Commit-Free Window Binding

For the successful sequence:

```text
corrected commit-free window:
AWAITING_RESTORATION_DECLARATION
```

For every other post-issuance terminal condition:

```text
corrected commit-free window:
AWAITING_INDEPENDENT_DISPOSITION
```

`AWAITING_INDEPENDENT_DISPOSITION` is not restoration, reopening, retry
permission, or opportunity reset. It may be resolved only by a separate
governed act.

Inherited `NOT_REACHED` checkpoint semantics:

```text
For an unreached checkpoint:

corrected_commit_free_window_status:
NOT_REACHED

implementation_window_status:
NOT_REACHED

observation_basis:
must explain why the checkpoint was not reached
```

No later checkpoint may be marked reached if an earlier required checkpoint is
`NOT_REACHED`, except that:

```text
RESULT_CAPTURE
```

may be reached to record an early terminal result.

The terminal corrected-window value must match the `RESULT_CAPTURE` entry.

## 13. Implementation-Result Requirement

The operation must produce the implementation-result document before the
operation stops:

```text
docs/TORMENT_BRAINVISION_STAGE_S3B_V0_3_BLOCKER_2_R4_AUTHORITY_C_IMPLEMENTATION_RESULT_v0.1.md
```

The implementation-result document must record the complete evidence required
by the accepted authorization, including:

```text
issued operation-act identity
implementation opportunity key
first-contact occurrence
opportunity-consumption state
exact first-contact event
pre-write verification
created files
modified files
unexpected files
prohibited-path preservation
test commands
test results
repository state after operation
terminal classification
secondary co-classification where required
window_status_history
Authority C/D/E state
canonical-input non-contact
FORMAL_HOLD
BLOCKER-2
BLOCKER-4
mandatory stopping-boundary confirmation
```

Its whole-document identity must be computed externally after bytes are
complete and frozen. It must contain no self-referential whole-document
identity field.

## 14. Mandatory Stop

The issued act must require an unconditional stop after implementation, tests,
and result capture, before:

```text
Authority-C activation
Authority-C invocation
candidate construction
Authority-D/E action
canonical-input preparation
canonical-input publication
governed execution
staging
commit
push
BLOCKER-2 closure
BLOCKER-4
```

The next permitted action must be:

```text
independent Claude review of the implementation and implementation-result
evidence
```

No activation or execution follows automatically.

## 15. Current Drafting Boundary

Creating this draft does not:

```text
issue the act
authorize implementation contact
start implementation
consume the opportunity
activate any authority
contact the canonical-input path
create the implementation-result document
create or modify Python files
stage
commit
push
```

Current state remains:

```text
bounded implementation operation operator issuance:
DRAFT ONLY
NOT ISSUED
NOT ACTIVE

implementation contact:
NOT STARTED

implementation opportunity:
NOT CONSUMED

Authority C:
INACTIVE

Authority D:
INACTIVE

Authority E:
INACTIVE

canonical-input path:
NOT CONTACTED

FORMAL_HOLD:
ACTIVE

BLOCKER-2:
OPEN

BLOCKER-4:
INACTIVE
```

## 16. Formatting

```text
UTF-8
LF only
no BOM
final newline present
```
