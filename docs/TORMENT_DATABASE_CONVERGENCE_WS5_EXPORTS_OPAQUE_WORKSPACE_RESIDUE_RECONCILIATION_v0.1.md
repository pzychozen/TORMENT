# TORMENT DATABASE CONVERGENCE

## WS5 `exports/` Opaque Workspace Residue Reconciliation v0.1

Starting head: `9bc954d189b8b8e9c50c97bd77ce7240cc70ee29`

The only classification frozen here is `ws5/exports` as
`OPAQUE_PREEXISTING_WORKSPACE_RESIDUE`. No producer identity or positive
semantic ownership is asserted.

The direct source reader recognizes only the exact `(ws5, exports)` pair and
only as a real, non-link, non-reparse directory. It validates the directory
itself without enumerating, opening, hashing, parsing, or otherwise reading
any descendant. The residue contributes no source evidence, manifest entry,
external-owner observation, geometry fact, runtime scope, or copy action.
Historical corrective capture remains strict, and any `exports` child in a
different workspace remains unclassified.

Qualification evidence uses disposable roots only. It covers the exact
direct-only acceptance, descendant iteration/read guards, proposition equality
with and without opaque descendants, rejection of other workspace and WS5
unknown children, non-directory rejection, Windows link/reparse rejection when
the platform permits it, historical strictness, and source nonmutation.

WS5_EXPORTS_RESIDUE_RECONCILIATION = QUALIFIED

WS5_EXPORTS_CLASS = OPAQUE_PREEXISTING_WORKSPACE_RESIDUE

WS5_EXPORTS_SOURCE_AUTHORITY = NONE

WS5_EXPORTS_EXTERNAL_OWNER_AUTHORITY = NONE

WS5_EXPORTS_MANIFEST_MEMBERSHIP = NONE

WS5_EXPORTS_DESCENDANT_ENUMERATION = NONE

WS5_EXPORTS_DESCENDANT_READ = NONE

OTHER_WORKSPACE_EXPORTS_ALLOWED = NO

UNKNOWN_WORKSPACE_CHILD_REFUSAL_PRESERVED = YES

HISTORICAL_CORRECTIVE_SEMANTICS_CHANGED = NO

LEGACY_SOURCE_MUTATION = NONE

P1_SEMANTICS_CHANGED = NO

P2_SEMANTICS_CHANGED = NO

REAL_ROOT_CONTACT = NONE

PRODUCTION_SQLITE_WRITE = NONE

P2_EXECUTED = NO
