# TORMENT Brainvision BY-Aware Closure Audit Preregistration v1.4

## 1. Status / non-claims

**DOCS-ONLY preregistration. Non-authorizing, non-implementing. Opens no code, no tests, no runtime, no
integration lane.** It PREREGISTERS — for a future, separately-gated finite BY-aware closure audit — the exact
structure the audit must have: the primary spine (selected candidates A + D + G) and the report-only support
(B / C / E), the required output fields, and the protocol-acceptance checks. It **names reporting obligations
only — it adopts no metric beyond those named reporting obligations, no equation, no threshold, no pass/fail
validity rule, and no decision rule**, and implements nothing. It **authorizes no code and no tests**, invents no
threshold, **redefines no `TOL`**, changes no formula / §7 anti-proxy logic / §8 verdict logic, deletes or
weakens no control, redesigns no descriptor, reopens no spectral group as a closure group, expands no generator
family, and opens **no classifier (form B) and no neural encoder (form C)**. It does **not** pivot to flat /
screen geometry and opens **no flat-geometry / screen-analysis implementation**. Everything stays offline under
`research/brainvision/` + `tests/research/`, HELD per v0.6.

This note makes **no** vision claim, **no** "Brainvision sees" claim, **no** temporal-order claim, **no**
descriptor-validity claim, **no** memory-readiness claim, **no** runtime-readiness claim, and **no**
integration-readiness claim. It touches no `torment_service/`, runtime, camera / sensor / live-capture /
screen-capture / streaming, or prompt / context / memory / action / render-body / autonomy paths, and makes
**no real-clip / local-clip move** and **no memory-system integration**. A preregistration alone moves nothing:
**no claim lock and no verdict changes here.**

```text
first_pass_structure_validity_claim_allowed = False
temporal_claim_allowed                      = False
descriptor_validity_claim_allowed           = False
verdict                                      = HOLD
```

**No `§0` pointer; no tags.**

## 2. Relation to v1.3a selection plan

```text
v1.2f (72293cc)  BY-aware closure FINDINGS: BY_aware_closure_gap_visible; closure_achieved False; HOLD.
v1.3  (a0f9b17)  candidate DESIGN: shapes A-E, adopting none.
v1.3a (8f1706e)  SELECTION plan: PRIMARY A + D + G; SUPPORT (report-only) B / C / E; adopting none.
v1.4  (this doc) finite audit PREREGISTRATION of the selected A + D + G spine (B / C / E report-only): required
                 fields + acceptance checks + non-closure conditions; adopting none; authorizing no code.
```

v1.3a selected the safest set; v1.4 preregisters the finite audit built on it — what it must report, what makes
its protocol acceptable, and what it may never claim — still without adopting a metric, an equation, a threshold,
or a decision rule, and without authorizing any implementation. It changes nothing v0.4b / v0.4c / v0.7a froze or
v0.7b / v0.8a / v0.9b / v1.0b / v1.2 produced.

## 3. Audit objective

```text
OBJECTIVE:
  Pre-commit, in finite detail, the structure of a future reporting-only BY-aware closure audit built on the
  selected A + D + G spine (B / C / E report-only), so the systematic BY opponent-axis offset is REPRESENTED as a
  first-class element and CANNOT hide inside residual / TOL matching -- WITHOUT this preregistration adopting any
  metric / equation / threshold / pass-fail rule, and WITHOUT authorizing any code.

The audit this preregisters would REPORT (not decide):
  A  the signed, sign-consistent BY offset (per by_centroid / by_spread / by_std), |offset| relative to frozen TOL;
  D  whether group residual / TOL matching COEXISTS with a systematic BY signed ordering, as an anti-hiding record;
  G  a standing non-authorization guard (nine flags False; any True -> protocol_ok False);
  B/C/E  report-only context (BY-vs-RG balance; residual annotated by binding stat; region/family stratification).
```

## 4. Frozen inputs and constants

All inputs are reused **by identity**; none are recomputed, replaced, reseeded, or expanded by this
preregistration.

```text
- Frozen constants (referenced, NOT re-thresholded): TOL = 0.0634; PSC_FLOOR = AIC_FLOOR = 0.30; CHANCE_BAND = 0.60.
- The frozen evaluator (structure iff PSC >= PSC_FLOOR and AIC >= AIC_FLOOR).
- The frozen descriptor / _stats / GROUPS / best-threshold BA / robustness lens; proxy_match_residual = L-inf over
  the ten matched stats (spectral excluded); PSC < PSC_FLOOR feasibility.
- The frozen sealed matching (v0.7b, reproduced via v0.8a): 19 matched / 5 unmatched pairs
  (unmatched: w_sp3.00, w_sp3.50, w_r0.4, w_r0.3, w_r0.2); single matching family segment_paired_canceller.
- The closed F1-F5 family set; spectral audit-note-only (NOT a closure group).
- The reused BY-aware quantities (v0.8a / v0.9b / v1.0b / v1.2 panels A-G) by identity; the natural reference
  chance level 0.5 (NOT a new threshold).
- Claim locks and verdict HOLD.
The audit reuses ALL of the above by identity; this preregistration redefines none of it.
```

## 5. Finite audit scope

```text
- FINITE and BOUNDED: reuse the v0.7b / v0.8a / v0.9b / v1.0b / v1.2 records BY IDENTITY over exactly the 19
  matched pairs (and 5 unmatched, for context), the ten matched stats, the three BY stats (by_centroid,
  by_spread, by_std), the RG and directional comparison features, and the frozen regions / single family.
- REPORTING-ONLY (form A, non-learning): re-organize and re-present frozen quantities; compute NO new statistic
  that is not a re-presentation of an existing frozen quantity; make NO decision.
- NO new data / samples / seeds / families / candidate generation; NO sample rerun / replacement; NO spectral
  reopening as a closure group.
- Non-finite / extreme values inadmissible: excluded upstream by the frozen _is_clean; a breach / non-reproduction /
  incomplete panels / an authorizing guard force protocol_ok False and an invalid_protocol_breach outcome.
- Outcome vocabulary is a REPORTING LABEL only (e.g. BY_aware_closure_gap_visible / reporting_generated /
  invalid_protocol_breach) and NEVER a Brainvision pass, a closure decision, or a claim-lock / verdict movement.
```

## 6. Primary audit spine: A + D + G

The audit MUST report all of A, D, G. Each is a concrete **reporting obligation**; none is a metric, an equation,
a threshold, or a decision.

```text
A. Signed BY offset component
   MUST report, per BY stat (by_centroid, by_spread, by_std):
     - the SIGN DIRECTION of the across-pair mean signed offset (dominant + / -);
     - the SIGN CONSISTENCY (fraction of pairs sharing the dominant sign; natural reference is chance 0.5);
     - the MAGNITUDE relative to the frozen TOL as a DESCRIPTIVE effect-size reference (the existing |smd|/TOL,
       reused by identity).
   MUST NOT create an offset-vs-TOL pass/fail gate: TOL stays the frozen residual-match tolerance, used here only
   as a descriptive unit. Reporting the offset is not closing it.

D. Aggregation anti-hiding rule
   MUST explicitly report WHETHER group-level residual / TOL matching COEXISTS with a systematic BY signed
   ordering (the aggregation / compression warning), so the coexistence is visible in one place and HIDDEN CLOSURE
   is impossible in the reporting (a passing per-pair / group residual is never silently re-described as closure of
   the BY offset).
   MUST NOT create a validity pass/fail gate: the record surfaces the coexistence and decides nothing.

G. Non-authorizing guard
   MUST carry all NINE authorization flags, all present and False:
     authorizes_descriptor_validity  authorizes_temporal_order   authorizes_pass_fail
     authorizes_closure              authorizes_runtime          authorizes_memory
     authorizes_integration          authorizes_live_or_screen_use  authorizes_vision
   ANY authorization flag set True (or absent) MUST make protocol_ok False (invalid_protocol_breach). Reporting
   cannot authorize descriptor validity, temporal order, runtime, memory, integration, live / screen use, or vision.
```

## 7. Support reporting: B/C/E

The audit MAY report B / C / E as **report-only context**. None may become a closure gate or a decision input.

```text
B. BY/RG opponent-balance (REPORT-ONLY)
   MAY report BY effect magnitudes compared against RG and directional comparison features. Visibility context
   only; MUST NOT adopt a dominance ratio / cutoff and MUST NOT become a gate.

C. BY binding-aware residual partition (REPORT-ONLY)
   MAY annotate the per-pair residual by its L-inf-binding stat (by_std / by_spread / by_centroid separated, vs
   proportional share). ANNOTATION ONLY; proxy_match_residual and TOL stay frozen; MUST NOT re-weight or redefine
   the residual and MUST NOT become a binding gate.

E. Region/family stratified reporting (REPORT-ONLY)
   MAY stratify reporting by region and by matching family, preserving the single-matching-family caveat
   (segment_paired_canceller) and per-region BY visibility. Reporting discipline only; MUST NOT expand the family
   set and MUST NOT become a gate.
```

## 8. Required output fields

The audit's result object MUST expose (field names indicative; reusing the v1.0b / v1.2 shape by identity):

```text
- panels: A_signed_offset (per-BY signed_offset, sign_consistency, dominant_sign, magnitude_frac_TOL,
  offset_vs_tol_gate=False); the aggregation record (aggregation_warning, hidden_closure_claim=False);
  the guard (visibility_is_diagnostic_only=True + the nine authorization flags, all False);
  and B / C / E report-only panels.
- reporting_obligations: the A / D / G spine mapping + B / C / E marked support/report-only.
- protocol_ok (bool); breaches (list); outcome_label (reporting label); by_wall_persists (bool);
  closure_achieved = False (hard-wired).
- provenance / freeze flags, all as specified: reuses_*_by_identity = True; TOL = 0.0634; tol_redefined = False;
  new_closure_metric_adopted = False; pass_fail_gate_introduced = False; new_threshold_introduced = False;
  descriptor_redesign_authorized = False; generator_family_expansion_authorized = False;
  spectral_closure_reopened = False; flat_geometry_authorized = False; screen_analysis_authorized = False;
  runtime_authorized = False; memory_authorized = False; vision_claim_allowed = False;
  reporting_only = True; visibility_is_non_authorizing = True.
- claim locks (all False) + frozen_brainvision_verdict = HOLD.
These are REQUIRED FIELDS, not equations. No field is a threshold on the offset; magnitude_frac_TOL is descriptive.
```

## 9. Protocol acceptance requirements

`protocol_ok = True` **only if all** of the following hold; otherwise the audit MUST return
`invalid_protocol_breach` and can produce no evidence:

```text
[ ] reuses v0.7b / v0.8a / v0.9b / v1.0b / v1.2 records BY IDENTITY; reproduces the v0.7b sealed matching; no new
    data / seeds / families / candidate generation.
[ ] A / D / G spine panels ALL present; B / C / E support panels present if reported.
[ ] A reports sign direction + sign consistency + magnitude_frac_TOL, with offset_vs_tol_gate = False.
[ ] D reports the residual/TOL-vs-BY-ordering coexistence with hidden_closure_claim = False.
[ ] G present with all nine authorization flags False and visibility_is_diagnostic_only = True; ANY authorizing
    flag True (or absent) -> protocol_ok False.
[ ] B / C / E, if present, are report-only (no gate; residual definition frozen for C; no family expansion for E).
[ ] TOL = 0.0634 and all freeze flags as in §8; no new metric / equation / threshold / pass-fail rule.
[ ] non-finite / breach / non-reproduction / incomplete panels -> invalid_protocol_breach (never evidence).
[ ] claim locks False; verdict HOLD.
Acceptance checks are STRUCTURAL (presence, provenance, non-authorization); none is a threshold on the BY offset.
```

## 10. Explicit non-closure conditions

```text
- closure_achieved is ALWAYS False; the outcome label set contains NO closure-positive label.
- A passing per-pair / group residual / TOL match is NEVER re-described as closure of the BY offset (D obligation).
- The audit adopts NO metric / equation / threshold / ratio / sign-consistency cutoff / pass-fail rule; it decides
  nothing about similarity or validity.
- The evidence base is a SINGLE matching family (segment_paired_canceller); the caveat is preserved, not removed.
- No outcome label, under any run, moves a claim lock or the verdict (HOLD under every outcome).
Under these conditions the audit can make the gap VISIBLE and non-hideable; it cannot, by construction, assert closure.
```

## 11. What would count as useful evidence

A future audit built to this preregistration would provide useful evidence **as a design-conformant reporting
artifact — not** a pass, a threshold, or a validity statement:

```text
- The A + D + G spine is REPRESENTABLE over the frozen records without a new threshold (A reuses signed offset +
  sign consistency vs chance 0.5; D reuses the aggregation coexistence; G is a standing guard) -- confirming the
  offset can be made non-hideable by construction.
- If the spine reports by_wall_persists = True (the expected honest reading), that CONFIRMS the gap persists under
  the selected structure -- an in-vitro, single-family, descriptor-surface observation, non-authorizing (G).
- If A + D CANNOT be represented without a threshold / family, THAT is itself useful evidence: it is the signal to
  escalate to the flat opponent-plane / spatial-field question (Branch B), not to force a metric.
Useful evidence is that the selected structure is representable and non-hideable -- not that closure was achieved.
```

## 12. What still would not be proven

Even a fully conformant future audit would leave all of the following **unproven**:

```text
not vision                     not "Brainvision sees"
not descriptor validity        not temporal order
not closure                    not a pass / a met threshold / an adopted metric
not real-video understanding   not a unique real-world color-structure advantage
not generator-family generalization (single-family caveat stands)
not memory readiness           not runtime readiness           not integration readiness
not live / screen / camera use
```

Preregistering and later reporting the A + D + G spine is an in-vitro, single-matching-family, design-layer step;
it says nothing about real clips or screens and does not validate the descriptor or adopt a metric. The proof
route remains **HELD / HOLD** because the offset is visible but not closed. The claim locks and `verdict = HOLD`
remain in force.

## 13. Implementation authorization status

```text
IMPLEMENTATION IS NOT AUTHORIZED BY THIS DOCUMENT.
- v1.4 preregisters the audit STRUCTURE only. It opens no code, no tests, and no runtime.
- A future v1.5 reporting-only implementation harness may be considered ONLY AFTER: (1) v1.4 is reviewed and
  accepted by Codex, and (2) the operator explicitly authorizes implementation. Both are SEPARATE gates.
- Any implementation, when/if authorized, MUST conform to §6-§10 exactly: reporting-only, reuse by identity,
  A + D + G spine, B / C / E report-only, non-authorizing guard, no metric / equation / threshold / pass-fail,
  closure_achieved False, claim locks False, verdict HOLD.
```

## 14. Review checklist

A future v1.4-conformant audit design (and later implementation) is acceptable iff a reviewer can check all of:

```text
[ ] docs/records only at THIS stage; implementation only after separate v1.4a authorization.
[ ] reuses v0.7b/v0.8a/v0.9b/v1.0b/v1.2 by identity; reproduces v0.7b matching; no new data/seeds/families.
[ ] primary spine A + D + G present exactly as §6; support B / C / E report-only exactly as §7.
[ ] A: sign direction + consistency + magnitude_frac_TOL; offset_vs_tol_gate False; no offset-vs-TOL gate.
[ ] D: residual/TOL-vs-BY-ordering coexistence reported; hidden_closure_claim False; no validity gate.
[ ] G: nine authorization flags present and False; any True -> protocol_ok False.
[ ] B / C / E do not become gates; proxy_match_residual frozen (C); no family expansion (E).
[ ] required output fields (§8) all present; freeze flags all False/as specified; closure_achieved False.
[ ] protocol acceptance (§9) structural only; no metric / equation / threshold / pass-fail adopted; TOL frozen.
[ ] non-closure conditions (§10) hold; claim locks False; verdict HOLD; no §0 pointer; no tags.
```

## 15. Candidate next branches

Docs-first / gate candidates only; **none opened or authorized here**:

```text
A. v1.4a IMPLEMENTATION AUTHORIZATION review (docs-only or direct operator / Codex approval)
   Decide whether to authorize a v1.5 reporting-only implementation harness of THIS prereg. (Recommended next.)
B. FLAT OPPONENT-PLANE / SPATIAL-FIELD proposal (docs-only)
   If the A + D + G spine cannot be represented without a new threshold / family, question the fixture geometry.
   Conceptual only; NO flat-geometry / screen-analysis implementation.
C. Operator / new-math NOTE (docs-only).
D. Pause Brainvision and return to TORMENT memory / kernel work.
```

## 16. Recommended next step

**Recommend Branch A (v1.4a implementation authorization review, docs-only or direct operator / Codex approval)
next.** v1.4 preregisters the finite audit structure; the clean move is an explicit authorization review — **only
after v1.4 is accepted should a v1.5 implementation harness be considered**, and only with Codex + operator
explicit approval. If review finds the A + D + G spine cannot be represented without an invented threshold or a
family expansion, escalate to Branch B (flat opponent-plane / spatial-field) rather than force a metric.

```text
1. Codex review THIS preregistration (docs-only; over committed edge 8f1706e).
2. If accepted, the operator commits this doc. No §0 pointer; no tags.
3. If the operator chooses to proceed, open Branch A as a SEPARATE, future v1.4a implementation-authorization
   review. A v1.5 reporting-only implementation harness is considered ONLY after v1.4 acceptance + explicit
   operator / Codex approval, and MUST conform to §6-§10.
4. Otherwise the Brainvision prototype line stays HELD. No code, classifier (B), neural (C), runtime, memory,
   real-clip, screen, flat-geometry, §0, or tag work is recommended or authorized here.
```

Claim locks and verdict are unchanged: `first_pass_structure_validity_claim_allowed = False`,
`temporal_claim_allowed = False`, `descriptor_validity_claim_allowed = False`, `verdict = HOLD`.

## 17. Codex review prompt

```text
Please review docs/TORMENT_BRAINVISION_BY_AWARE_CLOSURE_AUDIT_PREREG_v1.4.md
(new, docs-only, untracked; over committed edge 8f1706e, preregistering the finite BY-aware closure audit on the
selected A + D + G spine with B / C / E report-only, adopting none, authorizing no code).

Verify that this preregistration:
- is docs-only and authorizes no implementation (no code/tests, no torment_service/, no runtime, no memory, no
  camera/live/sensor/screen/streaming, no real clips); keeps form B (classifier) and form C (neural) CLOSED; and
  authorizes NO flat-geometry and NO screen-analysis implementation;
- names REPORTING OBLIGATIONS + acceptance checks ONLY and ADOPTS NO metric beyond those obligations, NO equation,
  NO threshold, NO pass/fail validity rule, REDEFINES NO TOL, redesigns no descriptor, expands no family, and
  reopens no spectral group (incl. not as a closure group);
- preregisters the PRIMARY spine A + D + G exactly: A (per-BY sign direction + sign consistency + magnitude relative
  to frozen TOL, offset_vs_tol_gate False, NO offset-vs-TOL gate); D (residual/TOL-vs-BY-ordering coexistence
  reported, hidden closure impossible, NO validity gate); G (nine authorization flags present and False, ANY True ->
  protocol_ok False);
- keeps B / C / E REPORT-ONLY (BY-vs-RG; residual annotated by binding stat with proxy_match_residual frozen; region/
  family stratification preserving the single-family caveat) and forbids any of them becoming a gate;
- specifies required output fields (§8) and STRUCTURAL protocol-acceptance checks (§9) that are presence/provenance/
  non-authorization only -- none a threshold on the BY offset -- with closure_achieved hard-wired False and an
  invalid_protocol_breach on non-finite / breach / non-reproduction / incomplete panels / authorizing guard;
- states explicit NON-CLOSURE conditions (§10), frames useful evidence (§11) as design-conformance not a pass, leaves
  vision / descriptor validity / temporal order / closure / memory / runtime / integration UNPROVEN (§12), and marks
  implementation NOT AUTHORIZED (§13: v1.5 only after v1.4 acceptance + explicit approval);
- recommends Branch A (v1.4a implementation authorization review) next, opening no code, and escalates to Branch B
  only if A + D + G cannot be represented without a new threshold / family; lists C/D;
- preserves all claim locks (first_pass_structure_validity_claim_allowed = False; temporal_claim_allowed = False;
  descriptor_validity_claim_allowed = False) and verdict = HOLD; adds no §0 pointer and no tags.

Flag any adopted metric / equation / threshold / pass-fail rule, any TOL redefinition, any descriptor redesign, any
family expansion, any spectral reopening as a closure group, any promotion of B / C / E to a gate, any offset-vs-TOL
or binding gate, any flat-geometry / screen-analysis / runtime / memory / real-clip authorization, any code
authorization, any claim that closure is ACHIEVED (vs the gap being visible), any descriptor-validity / vision /
temporal-order claim, or any claim-lock/verdict movement.
Return ACCEPT AS-IS or MODIFY with exact required changes.
```

Brainvision remains **offline / quarantined**, HELD per v0.6.
`first_pass_structure_validity_claim_allowed = False`, `temporal_claim_allowed = False`,
`descriptor_validity_claim_allowed = False`, and the frozen verdict **HOLD** are unchanged.
**No `§0` pointer; no tags.**

*End — TORMENT Brainvision BY-Aware Closure Audit Preregistration v1.4. Docs-only preregistration, non-authorizing.
Opens no implementation lane; opens no classifier / neural / screen / flat-geometry work; changes no frozen formula,
gate, evaluator, or verdict; deletes or weakens no control; redesigns no descriptor; invents no threshold; redefines
no TOL; adopts no closure metric or equation; names reporting obligations + acceptance checks only; preregisters the
A + D + G spine (B / C / E report-only) for a future finite audit, adopting none and authorizing no code; keeps the
gap visible not closed; makes no vision / descriptor-validity / temporal-order / memory / runtime / integration
claim; no `§0` pointer added; no tags.*
