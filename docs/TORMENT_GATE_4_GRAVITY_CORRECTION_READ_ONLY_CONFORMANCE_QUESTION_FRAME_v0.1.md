# TORMENT Gate 4 — `gravity_correction` Read-Only Conformance-Question Frame v0.1

**DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — QUESTION FRAMING ONLY, NO REMEDY. NO IMPLEMENTATION,
NO TESTS, NO RUNTIME, NO VERDICT.**

This is the **first separately authorized Gate 4 row-specific conformance-question frame**. It poses —
read-only — the conformance question for exactly one core runtime row, the live automatic writer
`torment_service/character.py::gravity_correction`, against already-promoted boundaries. **It poses the
question; it does not answer it.** It selects no remedy, no patch, no fix order, no enforcement behavior,
no governance vehicle, no runtime authority gate, no schema/carrier, no `canon_source`, no
source-sameness mechanic, and no database/substrate decision. It makes no safe/unsafe verdict and no
defect verdict.

**Operator-supplied docs-only state:** `f4ee79a` (*docs(gate-4): point orientation map to gap map*),
treated as the current operator-supplied state; the older Gate 4 stop preconditions are superseded
**only** by the operator-named docs-only commits in the Gate 4 lane (`a92dbef` → `dd53b7e` → `f6d0fb3`
→ `41c4349` → `f4ee79a`). No `git` was run. **Date:** 2026-06-20. Windows-visible tracked repo state
remains authoritative.

> Memory may shape context. Memory may not seize authority.
> Audit observes authority. Audit does not become authority.

**Tags:** `[FACT]` point-in-time, carried from prior read-only artifacts · `[BOUNDARY]` already-promoted
requirement obligation · `[QUESTION]` posed only, not answered · `[POSTURE]` conformance posture ·
`[DISTINCTION]` · `[DEPENDENCY]` · `[OUT-OF-SCOPE]` · `[NON-AUTHORIZATION]` · `[STOP]`.

---

## 0. Provenance, precondition, and review status

`[FACT]` **Lineage.** Gate 4 selection frame
(`TORMENT_GATE_4_READ_ONLY_RUNTIME_CONFORMANCE_SELECTION_FRAME_v0.1`) → Gate 4 read-only conformance-gap
map (`TORMENT_GATE_4_READ_ONLY_CONFORMANCE_GAP_MAP_v0.1`, Codex ACCEPT WITH CORRECTIONS, applied) →
**this row-specific conformance-question frame** for the `gravity_correction` core runtime row. It is a
**question frame**, not reconciliation work and not a "first row to reconcile"; it poses the Gate-4-lane
conformance question and answers nothing.

`[FACT]` **Review status: Codex challenge completed: ACCEPT WITH CORRECTIONS; corrections applied;
pending operator review / promotion.** The file remains **docs-only and non-authorizing** and changes no
behavior; nothing downstream may treat it as ratified, or as authorizing implementation, remedy, writer
patch, enforcement, substrate, or any successor gate, until operator review / promotion.

`[FACT]` **Precondition honored.** This frame is expressible **read-only and non-substrate** (§1); no
forbidden item (§6) was required to write it. Per the Gate 4 lane `[STOP]` conditions, had posing the
question required runtime/endpoint/service/ingest-query/database/workspace-frequency evidence, a verdict,
a remedy, a writer selection, a fix order, an enforcement path, a governance vehicle, an authority gate,
or any substrate decision, drafting would have stopped instead. It did not.

### 0.1 Relationship to the existing §N14 audit-first reconciliation memo (load-bearing)

`[DISTINCTION]` A promoted docs-only memo already exists for this seam:
`TORMENT_GRAVITY_CORRECTION_AUTOMATIC_CANON_AUDIT_FIRST_RECONCILIATION_v0.1.md` (promoted 2026-06-15;
Decision Registry §N14). That memo already recorded the seam's **doctrine status and later-owner
routing**, mapped the live write-surface and ingest wiring, listed tensions and entanglements, and
parked routed questions **Q-G1–Q-G7**. This frame **does not duplicate, re-open, re-scope, amend, or
answer** that memo. In particular it does **not** answer §N14's parked questions Q-G1–Q-G7 and does not
re-pick any of their routed later owners.

`[DISTINCTION]` Division of labor, stated plainly: **§N14 = doctrine-status + later-owner routing**
(already done). **This frame = the Gate-4-lane conformance *question* articulation** — naming, against
already-promoted boundaries, the specific conformance questions this writer raises, **posed and left
open**. The Gate 4 gap map already recorded this row's posture as *not-yet-conformant / requires
reconciliation*; this frame states the questions that posture leaves open, and stops there.

---

## 1. Status, scope, method

`[FACT]` This is a **docs-only read-only conformance-question frame** scoped to **exactly one row**: the
live writer `torment_service/character.py::gravity_correction`. It is **no implementation, no tests, no
runtime, no service, no endpoint, no ingest/query, no instrumentation, no remedy, no verdict, no
database/substrate.** Every `[FACT]` below is **point-in-time and carried** from prior read-only
artifacts (the §N14 reconciliation memo, the Gate B hazard inventory H1, the Gate 4 conformance-gap map
§2.1, Document A, Seed-Governance, the pre-substrate framing). **No new tracing, execution, measurement,
or frequency/workspace observation was performed**, and none is asserted; line-anchored specifics live in
§N14 at its baseline and are not re-asserted as current here.

`[POSTURE]` **Required posture, carried verbatim in intent:** this is a *read-only conformance-question
frame*; the writer is a *not-yet-conformant / requires reconciliation* automatic drift-correction-canon
writer; its *conformance posture is not yet resolved*; this is *question framing only, no remedy*.

`[POSTURE]` **No verdict.** "Not-yet-conformant / requires reconciliation" and "conformance posture not
yet resolved" are governance-conformance postures, **not** a claim that the writer is wrong, right,
safe, unsafe, compliant, non-compliant, or defective. Per Seed-Governance SG-O5 the not-yet-conformant
flag is **not** a must-patch order; per §N14 wording locks, pairing/routing/classification/audit are not
conformance, and audit observes the seam without becoming authority.

### 1.1 Preserved distinctions and out-of-scope

`[DISTINCTION]` Carried: `conformance characterization ≠ enforcement`; `audit visibility ≠ authority`;
`inspection ≠ projection`; `automatic allowed only where separately ratified; autonomous unopened`;
`not canon does not mean harmless`; `identity-relevant ≠ identity-authoritative`.

`[OUT-OF-SCOPE]` This frame is **one row only**. The other Gate 4 rows and adjacencies appear **only** as
carried boundaries / dependency / out-of-scope notes, never as scope: derived identity anchors; warmth /
warmup; role inference / identity-anchor cadence; authored `seed_text` (boundary context, no
automatic-writer gap); H3 (`POST /promote` force-route, writer-authority-adjacent, **not** automatic
identity-pressure); H5 (binding-contingent / parked); H6 (ordinary-ingest eligibility / reachability
only). None is posed, evaluated, or broadened here.

---

## 2. Carried write-surface facts (point-in-time; no new claims)

`[FACT]` Carried from §N14 §3–§4, Gate B hazard inventory H1, and Gate 4 gap map §2.1 (point-in-time,
not re-measured): `character.py::gravity_correction` is **live** (wired into the fabric ingest band) and
**automatic but conditional** — it fires only on `away_seed` drift past the seed's drift-correction
threshold, reached through the periodic, non-blocking post-store character drift check inside
`TormentFabric.ingest()` (gated by: character enabled; a memory stored this call; positive step; step
divisible by the drift interval; seed and seed-motif resolvable; high-away drift). When it fires it mints
a **new** memory `mtype="drift_correction"` with **`canon=True`** at **`tier="core_identity"`**, attaches
it to the seed motif, and flushes it durably. It is **purely additive** (it never rewrites or deletes
existing memories). Prior observation / content / frequency of its firing was recorded in §N14 / Gate B
as **not determinable read-only — deferred**, and is **not** gathered or claimed here.

---

## 3. Already-promoted boundaries this row is read against

`[BOUNDARY]` Named as the obligations the question is posed against — naming a boundary is not
implementing it, and no P4/Seed-Gov mechanic is selected:

- **Document A — A-O1** (class-bound writer authority; authorization must not be inferred solely from
  payload flags `canon`/`mtype`/`tier`/half-life or from source presence); **A-O2** (no automatic canon,
  identity-tier, seed, or long-half-life identity material produced directly without a governed promotion
  crossing); **A-O5** (existing automatic writers are named **unreconciled seams, not exemptions**;
  Document A patches none).
- **Seed-Governance — SG-O4** (a single `canon` boolean is not sufficient governance truth; governance
  must be able to distinguish canon by **source class**; v0.1 selects no storage representation);
  **SG-O5** (automatic identity/seed/canon writers require later governed reconciliation — a
  not-yet-conformant flag, not a must-patch order); **SG-O6** (soft guidance must not silently become
  seed/canon authority, **including through an automatic chain**); **§8 canon-source taxonomy**
  (**drift-correction-canon** = `gravity_correction`, automatic, "requires §9 reconciliation").
- **Pre-substrate framing — Family C** (gravity-correction canon memories: automatic, canonical today
  **contested**, "automatic-canon vs 'canon never automatic'"); standing posture "automatic remains
  allowed only where separately ratified; autonomous remains unopened"; "not canon does not mean
  harmless."
- **Ledger Observational-Boundary** ("audit observes authority; audit does not become authority") —
  governs this frame's own posture.

---

## 4. The conformance questions (posed only — not answered)

`[QUESTION]` Each item is **posed and left open**. None is answered; no remedy, fix order, writer-patch
selection, enforcement, governance vehicle, or authority gate is offered or implied. "Conformance posture
not yet resolved" stands for every one.

- **Q-C1 — automatic canon vs governed crossing.** Does an automatic, threshold-gated mint of
  `canon=True` / `tier="core_identity"` drift-correction material — produced on ingest without a governed
  admission or promotion crossing — sit conformant with Document A **A-O2** and the standing "automatic
  allowed only where separately ratified" posture? *Posed only; not answered here.*
- **Q-C2 — writer authority vs payload flag / source presence.** Does this write's authority, resting on
  its call-site plus payload flags (`canon`/`mtype`/`tier`) rather than a class-bound writer-authority
  check, sit conformant with Document A **A-O1**? *Posed only; not answered here.*
- **Q-C3 — single canon flag vs canon-by-source.** Is a single `canon=True` boolean sufficient governance
  truth for this **drift-correction-canon** source class under Seed-Governance **SG-O4** (which itself
  selects no storage representation)? *Posed only; not answered here; no `canon_source` representation
  selected or implied.*
- **Q-C4 — soft-guidance-to-canon chain (gravity end only).** Does the existence of an automatic pathway
  in which a soft, non-canon signal can be upstream of this hard canon mint sit conformant with
  Seed-Governance **SG-O6** (soft guidance must not silently become canon authority through an automatic
  chain)? This row poses only the **gravity end** of that chain; the `mood_drift → drift centroid →
  gravity_correction` topology is a **dependent** path framed separately (§5). *Posed only; not answered
  here.*

`[POSTURE]` **Resolution ownership is not decided here.** Who decides whether/how this writer can record
a resolved conformance posture, and on what basis, is already **routed (not picked)** in §N14 (Q-G1–Q-G7)
to later separately-authorized owners (Document A writer-authority / P2.5 track; Seed-Governance
later-owner; Stage B/P6; Stage A/P5a; later P4 runtime conformance). This frame neither re-picks those
owners nor states resolution criteria; doing so would be answering, and is out of scope.

---

## 5. Dependency note (framing sequence, not a fix order)

`[DEPENDENCY]` The `mood_drift → drift centroid → gravity_correction → canon=True` pathway is a
**dependent compound path ending in this writer** (topology only — no causal, magnitude,
"usually-triggers," or decisiveness claim; carried from Gate B H4 / Seed-Gov §9 / §N14 tension 4). Per
the reviewed framing order, the mood-topology question **would be eligible only as a later separately
authorized frame after this** `gravity_correction` row, because it depends on it — this implies no
scheduling, priority, remedy, or automatic successor. The reviewed order — **A.** `gravity_correction`
alone → **B.** mood-topology (dependent compound) → **C.** derived identity anchors → **D.** warmth /
role-cadence soft O6 paths — is a **framing sequence, not a fix order**, and selects, schedules, and
prioritizes no remedy.

---

## 6. What this frame does NOT do or authorize

`[NON-AUTHORIZATION]` This frame poses a question and stops. It does **not** answer it, and authorizes
none of, and must not be read as implying:

```
answer / resolution of the conformance question · safe/unsafe verdict · defect verdict
magnitude / frequency / workspace-observation claim · runtime evidence
remedy · writer patch · writer selection for remedy · fix order · enforcement behavior
governance vehicle · runtime authority gate
code · tests · runtime · endpoint / service calls · ingest / query
schema / store / carrier / serialization / migration · canon_source · source-sameness / P4 mechanics
Seed-Governance mechanics · candidate store · governed admission implementation
seed rewrite · seed-revision API · chamber / private-cognition runtime · dream / incubation runtime
database / substrate · hidden chain-of-thought storage or exposure · hidden output blocker / finalizer
identity pinning · monitoring / autonomy layer · coercive mechanism
duplication / re-opening / re-scoping / amendment of the §N14 memo · answering §N14 Q-G1–Q-G7
broadening to derived anchors / mood-topology / warmth / role-cadence / seed_text / H3 / H5 / H6
```

`[NON-AUTHORIZATION]` It amends no upstream contract (Document A / Document B / Seed-Governance / P4 /
Stage A / Cluster 2 / Ledger / MCP boundary), opens no Stage B, opens no autonomy, **updates no
orientation map**, and makes **no registry amendment**.

---

## 7. Stopping evidence

`[STOP]` Stop, and do not extend this frame or draft any follow-up, if continuing would require: an
answer/resolution rather than a posed-and-open question; a safe/unsafe or defect verdict; a magnitude /
frequency / workspace observation or any runtime / endpoint / service / ingest-query / database evidence;
a selected writer patch, fix order, enforcement behavior, governance vehicle, or runtime authority gate;
a schema / carrier / `canon_source` / source-sameness / database decision; any chamber / candidate-store
/ seed-revision / governed-admission construction; a hidden output blocker / finalizer; a hidden
chain-of-thought store; identity pinning; a monitoring / autonomy / coercive layer; or any
duplication/re-opening of §N14. Also stop if the operator-supplied docs-only state is not `f4ee79a` (or
its named Gate-4-lane predecessors), or if the question can no longer be posed read-only and
non-substrate.

---

## 8. Next step after this frame

`[NON-AUTHORIZATION]` This frame is a **draft for Codex challenge (Codex as first reviewer) and operator
review**. **No automatic successor gate. No automatic implementation.** Any later step — including
whether the mood-topology question is framed next, or whether any owner ever resolves Q-C1–Q-C4 — requires
**separate authorization** and an explicitly-opened step; nothing here selects or schedules it. The
orientation map is **not** updated by this artifact; any pointer is a separate later step. Database /
substrate remains last.

---

## Anti-drift footer

DOCS-ONLY READ-ONLY CONFORMANCE-QUESTION FRAME — question framing only, no remedy; no implementation, no
tests, no runtime, no service, no endpoint, no ingest / query, no workspace-frequency evidence, no
answer/resolution, no safe/unsafe verdict, no defect verdict, no magnitude/frequency claim, no
writer-patch selection, no fix order, no enforcement, no governance vehicle, no runtime authority gate, no
schema / store / carrier / serialization / migration, no `canon_source`, no source-sameness / P4
mechanics, no Seed-Governance mechanics, no candidate store, no governed admission implementation, no seed
rewrite, no seed-revision API, no chamber / private-cognition runtime, no dream / incubation runtime, no
database / substrate, no hidden chain-of-thought storage or exposure, no hidden output blocker /
finalizer, no identity pinning, no monitoring / autonomy / coercive layer, no orientation-map update, no
registry amendment. Scoped to one row — `torment_service/character.py::gravity_correction` — posed as a
**not-yet-conformant / requires reconciliation** automatic drift-correction-canon writer whose
**conformance posture is not yet resolved**; the four conformance questions (Q-C1–Q-C4) are **posed only,
not answered**. This frame **does not duplicate, re-open, re-scope, amend, or answer** the §N14 audit-first
reconciliation memo or its Q-G1–Q-G7. `mood_drift → centroid → gravity` is a **dependent** compound path,
**topology only**, framed separately; the A→B→C→D order is a **framing sequence, not a fix order**.
Authored `seed_text`, derived anchors, warmth, role cadence, H3, H5, H6 are out-of-scope here except as
carried boundaries / adjacencies. conformance characterization ≠ enforcement; audit visibility ≠
authority; inspection ≠ projection. Audit observes authority and does not become authority. Memory may
shape context. Memory may not seize authority. Database / substrate remains last.
