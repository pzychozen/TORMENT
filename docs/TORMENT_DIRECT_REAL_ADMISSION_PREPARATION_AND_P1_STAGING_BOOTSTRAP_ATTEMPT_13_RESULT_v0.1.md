# TORMENT — Direct Real Admission Preparation and P1 Staging Bootstrap Attempt 13 Result

**Date:** 2026-09-06
**Authorization:** `DIRECT_REAL_ADMISSION_PREPARATION_AND_P1_STAGING_BOOTSTRAP_ATTEMPT_13 = YES`
**Required start revision:** `88af2a3eb38ea8453392b189aa623a3b5536f414` (`HEAD == origin/main`)

## Authoritative result

```text
DIRECT_REAL_ADMISSION_PREPARATION_ATTEMPT_13 = PRECHECK_REFUSED
ADMINISTRATION_OPERATION_ID = direct-real-admission-p1-attempt-13-20260906
ADMINISTRATION_FINAL_STATE = FINAL_STOP
```

The required first Python process used the repository-owned module bridge. The
exact import probe passed with Python module mode, repository package context,
and detached flags `520`.

After `RUNNER_STARTED` and `PRECHECK_STARTED`, the one authorized direct WMIC
CSV observation did not satisfy the required complete positive-PID/current
administration record contract. The durable precheck exception is:

```text
RuntimeError: WMIC_POSITIVE_PID_CONTRACT_FAILED
```

No second WMIC census, parsing change, collector substitution, PowerShell
launch, capture child, source contact, SQLite action, or P1 action followed.

## Durable ledger

```text
1  RUNNER_STARTED
2  PRECHECK_STARTED
3  PRECHECK_REFUSED
4  FINAL_STOP
```

## Authority ledger

```text
IMPORT_PROBE = PASS
OUTER_BOOTSTRAP_MODE = REPOSITORY_PYTHON_MODULE
WMIC_PROCESS_COLLECTION = REFUSED
WRITER_CENSUS = NOT_EXECUTED
CAPTURE = NOT_EXECUTED
DIRECT_SOURCE_PREPARATION = NOT_EXECUTED
P1_STARTED = NO
REAL_SOURCE_CONTACT = NONE
SQLITE_CREATE_API_INVOKED = NO
REAL_NATIVE_STAGING_CORE_CREATED = NO
P2_EXECUTED = NO
P5_EXECUTED = NO
P6_EXECUTED = NO
P7_EXECUTED = NO
LEGACY_SOURCE_MUTATION = NONE
POWERSHELL_EXECUTION = NONE
```

Attempt 13 is closed at durable `FINAL_STOP`. Its external ledger, probe
files, WMIC output, and external bodies are not repository artifacts.
