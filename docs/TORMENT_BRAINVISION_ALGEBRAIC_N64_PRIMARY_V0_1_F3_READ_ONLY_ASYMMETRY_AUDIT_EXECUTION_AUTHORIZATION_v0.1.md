# TORMENT Brainvision Algebraic N=64 PRIMARY_V0_1 Frozen-Family F3 Read-Only Asymmetry Audit Execution Authorization v0.1

## 0. Decision

```text
A. AUTHORIZE EXACT ONE ACCEPTED RETAINED-F3 READ-ONLY ASYMMETRY AUDIT EXECUTION
```

This document authorizes exactly one operator-controlled accepted execution of the committed standalone read-only asymmetry audit analyzer over the exact retained canonical algebraic N=64 `PRIMARY_V0_1` frozen-family F3 evaluation result.

The authorized operation is:

```text
offline
read-only with respect to retained F3 evidence
descriptive
non-gating
non-rescue
standard-library only
quarantined
non-production
zero-argument
operator-controlled
```

This document is:

```text
docs-only
non-executing during preparation
non-executing during review
non-executing during commit
non-executing during push
```

Preparing, reviewing, committing, or pushing this document does not:

```text
set the audit gate
run the analyzer
load the retained F3 result through the real execution path
perform the retained audit calculations
create final or staging audit output
call PsiTRS
invoke the F3 evaluator
recompute descriptors
modify retained evidence
authorize scientific inference
contact the production kernel
```

This authorization becomes operational only after this document is:

```text
accepted
committed as the sole changed file
pushed to main
synchronized so HEAD == origin/main
followed by a clean working tree, including untracked files
```

The authoritative F3 verdict remains permanently:

```text
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

The authorized audit cannot amend, weaken, reinterpret, reconsider, replace, or rescue that verdict.

## 1. Governing documents and preserved contracts

The following governing documents remain accepted and are not amended by this authorization:

```text
docs/TORMENT_BRAINVISION_ALGEBRAIC_N64_PRIMARY_V0_1_F3_EVALUATION_FINDINGS_v0.1.md

docs/TORMENT_BRAINVISION_ALGEBRAIC_N64_PRIMARY_V0_1_F3_READ_ONLY_ASYMMETRY_AUDIT_SPECIFICATION_v0.1.md

docs/TORMENT_BRAINVISION_ALGEBRAIC_N64_PRIMARY_V0_1_F3_READ_ONLY_ASYMMETRY_AUDIT_IMPLEMENTATION_AUTHORIZATION_v0.1.md
```

Bound commits:

```text
F3 findings commit =
f0c5fdbc6f75e3749323de9e2b13c77b3710df82

audit specification commit =
db82ec06faf398d52a002ba56f976e91045d12a4

audit implementation-authorization commit =
75ba05485ae3d1dafcbe64d8330920459d54fa7e
```

The governing specification fixes:

```text
the exact retained input
the input identity checks
the six frozen audit questions
the canonical q=1..32 class convention
the inverse-shift validation
the retained aggregate semantics
the deterministic ordering and tie rules
the A/B/C/D descriptive disposition rules
the non-gating and non-rescue interpretation boundary
the separate derived-output location
the publication and failure-retention contract
```

The implementation authorization fixes:

```text
the standalone analyzer boundary
the standard-library-only boundary
the forbidden-import and forbidden-call boundary
the zero-argument execution surface
the audit-specific environment gate
the repository-state validation
the analyzer source-identity validation
the retained-input preflight
the exact exit-code classes
the exact two-file publication contract
the requirement for a later source-frozen execution authorization
```

This document is that later source-frozen execution authorization.

It grants only the exact later operation defined in this document.

## 2. Reviewed implementation baseline

This authorization was prepared against the completed and accepted implementation commit:

```text
branch =
main

implementation commit =
8325bff376a646d1ca2c54dd6aa7f14eb84a9282

implementation subject =
research(brainvision): implement frozen N64 F3 asymmetry audit
```

The implementation commit is the reviewed source baseline.

It is not the later execution `HEAD`.

The execution `HEAD` will be the docs-only commit that adds this authorization document directly on top of the implementation commit.

Therefore:

```text
the authorization commit identity must not be guessed
the authorization commit identity must not be precomputed
the full authorization commit must be captured after commit and push
the authorization commit must have 8325bff376a646d1ca2c54dd6aa7f14eb84a9282 as its direct parent
the execution must occur only while the authorization commit is synchronized HEAD
```

The committed analyzer dynamically records:

```text
audit_execution_commit_identity =
the synchronized execution-time HEAD
```

Do not confuse:

```text
implementation_commit_identity =
8325bff376a646d1ca2c54dd6aa7f14eb84a9282

input_execution_commit_identity =
c4f489c439d4190611e8e0c5b3034ead3353c26d

audit_execution_commit_identity =
the later synchronized docs-only authorization commit
```

## 3. Frozen committed source identities

The following two committed source identities are frozen by this authorization.

### 3.1 Analyzer

```text
path =
research/brainvision/analyze_algebraic_n64_f3_asymmetry_v0_1.py

Git blob SHA =
8f093f01535d2bcb04c88a87be426a78e795752a

raw-file SHA-256 =
59006c6b6089db68da4e75ff4aab86755870df40c3a77bf6c28402971f41e395
```

### 3.2 Focused test module

```text
path =
tests/research/test_brainvision_analyze_algebraic_n64_f3_asymmetry_v0_1.py

Git blob SHA =
2ea93a66480170fcccbac1b7b775bd10c8601fda

raw-file SHA-256 =
dbb5f2ba3bc485d7edecf28fc399acd18edcf84bd9887ecf6f9670577b561a18
```

The commit adding this authorization must:

```text
add exactly one new documentation file
leave the analyzer unchanged
leave the test module unchanged
leave all retained evidence unchanged
leave all production code unchanged
```

Any change to either frozen source file closes this authorization.

Any mismatch in:

```text
path
Git blob SHA
raw-file SHA-256
local bytes
committed HEAD bytes
```

requires:

```text
no audit execution
new focused source review
new source-identity freeze
new docs-only execution authorization
```

The test source is manually frozen by this document even though the runtime analyzer validates only its own committed/local source identity.

## 4. Completed implementation acceptance

Authoritative focused Windows result:

```text
76 passed
```

GPT focused review:

```text
ACCEPT
```

Codex final source review:

```text
ACCEPT AS-IS
```

Completed implementation state:

```text
READ_ONLY_ASYMMETRY_AUDIT_SPECIFIED = True
READ_ONLY_ASYMMETRY_AUDIT_IMPLEMENTED = True
READ_ONLY_ASYMMETRY_AUDIT_TESTS_PASSED = True
```

At preparation time:

```text
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZED = False
REAL_AUDIT_OUTPUT_CREATION_AUTHORIZED = False
```

The engineering acceptance records above are not retained-audit findings and do not themselves authorize execution.

## 5. Exact retained input and evidence identities

### 5.1 Canonical retained F3 result

Exact retained input path:

```text
research/brainvision/results/algebraic_n64_primary_v0_1_f3_evaluation_v0_1/
  algebraic_n64_primary_v0_1_f3_evaluation_result.json
```

Expected size:

```text
10,784,993 bytes
```

Expected whole-file SHA-256:

```text
51e7cd8087050428c2559262764044624fcb84e19576b5f682bae3ca5b59fd7b
```

Expected canonical payload SHA-256:

```text
f996869ffa58d2ac6e6cdcc2ffc8bbd5769f1e5568ac71c6f63faadd4a5a4125
```

Expected historical F3 execution identity:

```text
c4f489c439d4190611e8e0c5b3034ead3353c26d
```

Expected accepted candidate order:

```text
[478, 479, 480]
```

Expected valid-run state:

```text
valid_run = True
replay byte_identical = True
```

Expected authoritative verdict:

```text
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

### 5.2 Retained F3 operator summary

Exact sibling retained summary path:

```text
research/brainvision/results/algebraic_n64_primary_v0_1_f3_evaluation_v0_1/
  algebraic_n64_primary_v0_1_f3_evaluation_summary.txt
```

Expected retained summary SHA-256:

```text
43a19f5ca7d1e22491bfae209603b4f595b17c886a9d4320346ef2aa4a129daf
```

The summary is not an analyzer input.

Its identity is bound as an additional retained-evidence immutability check.

### 5.3 Retained-evidence boundary

This authorization permits the analyzer to open and load the canonical retained JSON read-only.

It does not authorize:

```text
editing
rewriting
reserializing
renaming
moving
deleting
replacing
regenerating
rerunning
repairing
normalizing
truncating
touching timestamps intentionally
writing any file into the retained F3 directory
```

Any retained-result or retained-summary identity mismatch closes this authority.

## 6. Exact derived-output boundary

Exact final directory:

```text
research/brainvision/results/
  algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1/
```

Exact staging directory:

```text
research/brainvision/results/
  .algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1.staging/
```

Exact successful final file set:

```text
algebraic_n64_primary_v0_1_f3_asymmetry_audit_result.json

algebraic_n64_primary_v0_1_f3_asymmetry_audit_summary.txt
```

The output is derived audit evidence.

It is not a replacement for the retained F3 evidence.

Publication must preserve the committed implementation contract:

```text
all calculations completed in memory before staging creation
canonical result bytes completed before staging creation
summary bytes completed before staging creation
exclusive staging-directory creation
exclusive file creation
exact two-file staged set
single staging-to-final rename
no overwrite
no merge
no automatic resume
no automatic retry
```

No write is authorized inside:

```text
research/brainvision/results/algebraic_n64_primary_v0_1_f3_evaluation_v0_1/
```

## 7. Required pre-execution repository state

Immediately before the authorized invocation, all of the following must hold:

```text
authoritative repository root =
C:\TORMENT\TORMENT_repo\TORMENT-fabric_v2\torment_fabric

conda environment =
torment

branch =
main

HEAD =
origin/main

working tree =
clean, including ordinary untracked files

authorization commit direct parent =
8325bff376a646d1ca2c54dd6aa7f14eb84a9282

authorization commit changed-file set =
exactly this one documentation file

analyzer Git blob =
8f093f01535d2bcb04c88a87be426a78e795752a

test Git blob =
2ea93a66480170fcccbac1b7b775bd10c8601fda

analyzer raw SHA-256 =
59006c6b6089db68da4e75ff4aab86755870df40c3a77bf6c28402971f41e395

test raw SHA-256 =
dbb5f2ba3bc485d7edecf28fc399acd18edcf84bd9887ecf6f9670577b561a18

retained result size and hashes =
exactly §5

retained summary hash =
exactly §5

audit environment gate =
unset

final audit directory =
absent

staging audit directory =
absent
```

Do not invoke the analyzer when any required state differs.

## 8. Exact authorized operation

The only authorized analyzer invocation is:

```bat
set ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED=1
python research\brainvision\analyze_algebraic_n64_f3_asymmetry_v0_1.py
```

The invocation must be issued:

```text
from the authoritative Windows repository root
inside the torment conda environment
using Command Prompt
using python, not py
with zero command-line arguments
with no path override
with no output override
with no configuration override
with no direct function call
with no import-and-call wrapper
with no alternate script
with no shell redirection
with no piping
with no command chaining
with no background execution
with no parallel execution
```

No alternative invocation is authorized.

This authorization does not cover:

```text
calling run_execution(...) directly
calling run_pure_audit(...) directly
loading the retained payload through a custom script
creating alternate outputs
rerunning the F3 evaluator
calling PsiTRS
calling build_production_feature_cache
calling evaluate_from_feature_cache
calling psi_trs_features
modifying the analyzer
modifying the retained evidence
```

## 9. Exact gate lifecycle

The audit gate is:

```text
ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED
```

The only accepted value is:

```text
1
```

Before setting the gate:

```bat
if defined ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED (
  echo GATE_ALREADY_SET=[%ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED%]
) else (
  echo GATE_UNSET_GOOD
)
```

The required result is:

```text
GATE_UNSET_GOOD
```

Do not continue if the gate is already defined.

Set the gate only immediately before the exact invocation:

```bat
set ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED=1
python research\brainvision\analyze_algebraic_n64_f3_asymmetry_v0_1.py
```

Immediately capture the exit code:

```bat
set F3_ASYMMETRY_AUDIT_EXIT_CODE=%ERRORLEVEL%
echo F3_ASYMMETRY_AUDIT_EXIT_CODE=%F3_ASYMMETRY_AUDIT_EXIT_CODE%
```

Immediately clear the gate:

```bat
set ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED=
```

Verify that it is cleared:

```bat
if defined ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED (
  echo GATE_STILL_SET
) else (
  echo GATE_CLEARED
)
```

The required result is:

```text
GATE_CLEARED
```

The gate:

```text
must not be persisted
must not be set in another terminal
must not be placed in an activation script
must not remain set after execution
must not be reused for a second accepted audit
grants no F3 evaluation authority
grants no PsiTRS authority
grants no descriptor authority
```

## 10. Exact internal audit authority

Through the committed analyzer only, this document permits:

```text
audit-gate validation
zero-argument validation
repository-root resolution
main-branch validation
HEAD/origin-main validation
clean-working-tree validation
committed analyzer-path validation
analyzer Git blob resolution
analyzer local-byte versus committed-byte validation
retained input path validation
regular-file and non-symlink validation
retained input size validation
whole-file SHA-256 validation
strict JSON loading
exact envelope validation
canonical payload SHA-256 validation
historical F3 identity validation
validity and replay validation
pair-order validation
authoritative-verdict validation
retained aggregate consistency validation
inverse-shift distance-multiset validation
the six specified read-only audit calculations
deterministic A/B/C/D descriptive disposition
canonical result-envelope serialization
operator-summary rendering
exclusive staging publication
atomic staging-to-final rename
stdout summary mirror
```

The authorization permits no calculation outside the committed analyzer.

It permits no recomputation of:

```text
PsiTRS features
descriptor vectors
F3 responses
self-orbit responses
cross responses
frozen witnesses
candidate generation
witness certificates
```

All audit calculations must operate only on the already-retained F3 JSON objects.

## 11. One-run authority and consumption boundary

This authorization grants exactly one accepted retained-audit execution.

The exact consumption threshold is:

```text
The one-run authority is consumed when the committed analyzer begins:

run_pure_audit(loaded["payload"], identities)

after the retained input has passed the complete pre-audit identity,
envelope, historical-result, validity, replay, pair-order, verdict,
repository, source, and output-path validation sequence.
```

This boundary distinguishes:

```text
pre-audit refusal
from
accepted retained-audit execution
```

### 11.1 Pre-audit refusal

An analyzer exit code of:

```text
2
```

represents a refusal before the accepted retained audit begins.

Examples include:

```text
gate absent or incorrect
unexpected CLI arguments
repository-state failure
wrong branch
HEAD/origin-main mismatch
dirty working tree
source-identity failure
input path invalid
input missing
input not regular
input symlink
input size mismatch
input whole-file hash mismatch
invalid JSON
invalid envelope
payload-hash mismatch
schema mismatch
historical execution-identity mismatch
replay-status mismatch
validity mismatch
pair-order mismatch
family-verdict mismatch
final path already exists
staging path already exists
```

A pre-audit refusal does not consume the accepted-audit authority only when it is definitively established that:

```text
run_pure_audit(...) did not begin
no final audit directory was created
no evidence-bearing staging directory was created
no derived audit artifact byte was written
the gate was cleared
the refusal cause is understood
the source identities remain exact
the retained evidence identities remain exact
the authorization semantics remain unchanged
the exact required state can be restored without deleting or altering evidence
```

A later invocation under the same authorization is permissible only under all those conditions.

When there is uncertainty about whether the consumption threshold was crossed, treat the authority conservatively as consumed.

### 11.2 Consumed execution

The authority is consumed once `run_pure_audit(...)` begins, regardless of:

```text
disposition A
disposition B
disposition C
disposition D
audit_valid True
audit_valid False
calculation exception
canonical serialization failure
publication failure
rename failure
stdout failure
process interruption
system interruption
absence of final publication
presence of evidence-bearing staging
positive-looking descriptive structure
negative-looking descriptive structure
mixed descriptive structure
```

After consumption:

```text
do not rerun
do not retry
do not call run_pure_audit directly
do not create replacement output
do not delete final output
do not delete evidence-bearing staging
do not edit output
do not repair output
do not reinterpret the result as a new F3 verdict
```

Any second accepted retained-audit execution requires a new docs-only authorization.

## 12. Exit, publication, and retention contract

Exact exit-code meanings:

```text
exit 0 =
complete exact two-file atomic publication
with descriptive family disposition A, B, C, or D

exit 1 =
post-consumption process failure, including audit-calculation,
serialization, publication, rename, or stdout failure

exit 2 =
pre-audit refusal before run_pure_audit(...) begins
```

Important:

```text
exit 0 does not mean scientific success
exit 0 does not change the authoritative F3 verdict
disposition A is not a rescue
disposition B is not a rescue
disposition C is not a rescue
disposition D is not a new F3 verdict
```

Failure retention:

```text
an empty staging directory created before any artifact byte is written may be removed only by the committed analyzer behavior

once any artifact byte is written, staging is evidence-bearing and must be retained

complete staging must be retained after rename failure

a published final directory must never be rolled back or deleted

stdout failure after successful publication preserves the final directory
```

No manual deletion, cleanup, repair, merge, or replacement is authorized after the command.

## 13. Exact pre-run operator capture

All commands use Command Prompt syntax.

### 13.1 Repository state and commit lineage

```bat
git status --short --branch

git rev-parse HEAD

git rev-parse origin/main

git rev-parse HEAD~1

git log --oneline --decorate -6

git show --pretty=format: --name-only HEAD
```

Required interpretation:

```text
HEAD == origin/main

HEAD~1 ==
8325bff376a646d1ca2c54dd6aa7f14eb84a9282

git show lists exactly:
docs/TORMENT_BRAINVISION_ALGEBRAIC_N64_PRIMARY_V0_1_F3_READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZATION_v0.1.md

git status shows:
## main...origin/main

with no changed or untracked paths
```

### 13.2 Frozen Git blobs

```bat
git rev-parse HEAD:research/brainvision/analyze_algebraic_n64_f3_asymmetry_v0_1.py

git rev-parse HEAD:tests/research/test_brainvision_analyze_algebraic_n64_f3_asymmetry_v0_1.py
```

Expected:

```text
analyzer =
8f093f01535d2bcb04c88a87be426a78e795752a

test =
2ea93a66480170fcccbac1b7b775bd10c8601fda
```

### 13.3 Frozen raw source hashes

```bat
certutil -hashfile research\brainvision\analyze_algebraic_n64_f3_asymmetry_v0_1.py SHA256

certutil -hashfile tests\research\test_brainvision_analyze_algebraic_n64_f3_asymmetry_v0_1.py SHA256
```

Expected:

```text
analyzer =
59006c6b6089db68da4e75ff4aab86755870df40c3a77bf6c28402971f41e395

test =
dbb5f2ba3bc485d7edecf28fc399acd18edcf84bd9887ecf6f9670577b561a18
```

### 13.4 Retained evidence hashes

```bat
certutil -hashfile research\brainvision\results\algebraic_n64_primary_v0_1_f3_evaluation_v0_1\algebraic_n64_primary_v0_1_f3_evaluation_result.json SHA256

certutil -hashfile research\brainvision\results\algebraic_n64_primary_v0_1_f3_evaluation_v0_1\algebraic_n64_primary_v0_1_f3_evaluation_summary.txt SHA256
```

Expected:

```text
result =
51e7cd8087050428c2559262764044624fcb84e19576b5f682bae3ca5b59fd7b

summary =
43a19f5ca7d1e22491bfae209603b4f595b17c886a9d4320346ef2aa4a129daf
```

### 13.5 Gate-state check

```bat
if defined ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED (
  echo GATE_ALREADY_SET=[%ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED%]
) else (
  echo GATE_UNSET_GOOD
)
```

Required:

```text
GATE_UNSET_GOOD
```

### 13.6 Final and staging path absence

```bat
if exist research\brainvision\results\algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1 (
  echo FINAL_DIRECTORY_EXISTS
) else (
  echo FINAL_DIRECTORY_ABSENT
)

if exist research\brainvision\results\.algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1.staging (
  echo STAGING_DIRECTORY_EXISTS
) else (
  echo STAGING_DIRECTORY_ABSENT
)
```

Required:

```text
FINAL_DIRECTORY_ABSENT
STAGING_DIRECTORY_ABSENT
```

Do not invoke when either path exists.

## 14. Exact invocation and immediate capture

Run the following exact invocation only after every §13 requirement passes:

```bat
set ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED=1
python research\brainvision\analyze_algebraic_n64_f3_asymmetry_v0_1.py
```

Immediately capture the exit code:

```bat
set F3_ASYMMETRY_AUDIT_EXIT_CODE=%ERRORLEVEL%
echo F3_ASYMMETRY_AUDIT_EXIT_CODE=%F3_ASYMMETRY_AUDIT_EXIT_CODE%
```

Immediately clear and verify the gate:

```bat
set ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED=

if defined ALGEBRAIC_N64_F3_ASYMMETRY_AUDIT_AUTHORIZED (
  echo GATE_STILL_SET
) else (
  echo GATE_CLEARED
)
```

Do not issue the analyzer command again after the consumption threshold has been crossed.

## 15. Required post-run capture

### 15.1 Repository status

```bat
git status --short --branch
```

The derived result directory is expected to appear according to repository ignore policy.

No source or documentation modification is authorized during execution.

### 15.2 Final and staging listings

```bat
dir /a research\brainvision\results\algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1

dir /a research\brainvision\results\.algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1.staging
```

Record which of these states exists:

```text
complete final publication
evidence-bearing retained staging
neither final nor evidence-bearing staging
```

Do not delete or alter either directory.

### 15.3 Successful final artifact hashes

When final publication exists:

```bat
certutil -hashfile research\brainvision\results\algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1\algebraic_n64_primary_v0_1_f3_asymmetry_audit_result.json SHA256

certutil -hashfile research\brainvision\results\algebraic_n64_primary_v0_1_f3_asymmetry_audit_v0_1\algebraic_n64_primary_v0_1_f3_asymmetry_audit_summary.txt SHA256
```

Record both hashes exactly.

### 15.4 Evidence-bearing staging hashes

When final publication does not exist but staging exists:

```text
list every staging file
hash every staging file with certutil
retain the directory exactly as produced
do not rename it
do not complete publication manually
do not recreate missing files
```

### 15.5 Retained evidence recheck

After execution, hash the retained input and retained summary again:

```bat
certutil -hashfile research\brainvision\results\algebraic_n64_primary_v0_1_f3_evaluation_v0_1\algebraic_n64_primary_v0_1_f3_evaluation_result.json SHA256

certutil -hashfile research\brainvision\results\algebraic_n64_primary_v0_1_f3_evaluation_v0_1\algebraic_n64_primary_v0_1_f3_evaluation_summary.txt SHA256
```

Required unchanged identities:

```text
result =
51e7cd8087050428c2559262764044624fcb84e19576b5f682bae3ca5b59fd7b

summary =
43a19f5ca7d1e22491bfae209603b4f595b17c886a9d4320346ef2aa4a129daf
```

## 16. Required operator report

The operator must return the following without scientific interpretation:

```text
authorization execution HEAD
HEAD/origin-main agreement
authorization commit parent
authorization commit changed-file list
pre-run git status
analyzer Git blob
test Git blob
analyzer raw SHA-256
test raw SHA-256
pre-run retained result SHA-256
pre-run retained summary SHA-256
confirmation that the gate was initially unset
confirmation that final and staging paths were initially absent
exact analyzer console transcript
F3_ASYMMETRY_AUDIT_EXIT_CODE
confirmation that the gate was cleared
post-run git status
final-directory listing
staging-directory listing
final artifact hashes, when final exists
all retained staging hashes, when staging evidence exists
post-run retained result SHA-256
post-run retained summary SHA-256
confirmation that no second accepted invocation occurred
```

When complete final publication exists, also report the exact retained machine fields:

```text
asymmetry_audit_result_sha256
audit_valid
ordered_failure_codes
pair_order
pair_classifications
family_disposition
authoritative_f3_verdict_preserved
audit_execution_commit_identity
source_findings_commit_identity
source_audit_specification_commit_identity
source_implementation_authorization_commit_identity
analyzer_git_blob_sha
analyzer_raw_file_sha256
input_whole_file_sha256
input_payload_sha256
```

The operator summary may be pasted for convenience.

The canonical JSON remains authoritative.

No interpretation beyond exact field reporting is authorized during the execution phase.

## 17. Interpretation and non-rescue boundary

The audit is:

```text
descriptive
read-only
non-gating
non-rescue
post-result
```

Possible family dispositions:

```text
A. BLOCKING SELF-ORBIT MAXIMUM IS NARROWLY CONCENTRATED

B. BLOCKING SELF-ORBIT ELEVATION IS BROAD

C. MIXED BLOCKING STRUCTURE ACROSS THE FROZEN FAMILY

D. RETAINED EVIDENCE IS INSUFFICIENT OR INTERNALLY INCONSISTENT
```

None of A, B, C, or D:

```text
changes the F3 verdict
rescues the frozen family
authorizes an F3 rerun
authorizes descriptor recomputation
establishes true vision
establishes perception
establishes temporal-order perception
establishes recursive-time mechanism
establishes scientific significance
establishes physics emergence
establishes production readiness
generalizes beyond the exact frozen family
```

The authoritative result remains:

```text
STRONG_ORDER_HYPOTHESIS_NOT_SUPPORTED_BY_FROZEN_FAMILY
```

Any later findings document must preserve that verdict exactly.

## 18. Closed project and production boundaries

Brainvision remains:

```text
offline
quarantined
non-runtime
non-production
non-live
non-scientific-claim lane
```

Preserve:

```text
FORMAL_HOLD active
Mode_0 active
```

This authorization explicitly prohibits modification of, or operational contact with:

```text
torment_service/kernel/
torment_service/memory_kernel.py
torment_service/fabric.py

production memory
prompt construction
model context
actions
tools
live models
network services
camera capture
screen capture
live sensors
service integration
```

The production TORMENT memory kernel remains immutable.

The authorized audit imports no `torment_service` code and contacts no production surface.

## 19. Authority state

Before this document is committed and synchronized:

```text
F3_EVALUATION_COMPLETE = True
F3_RERUN_AUTHORIZED = False

READ_ONLY_ASYMMETRY_AUDIT_SPECIFIED = True
READ_ONLY_ASYMMETRY_AUDIT_IMPLEMENTED = True
READ_ONLY_ASYMMETRY_AUDIT_TESTS_PASSED = True

READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZATION_DOCUMENTED = False
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZED = False
REAL_AUDIT_OUTPUT_CREATION_AUTHORIZED = False

PSITRS_CONTACT_AUTHORIZED = False
DESCRIPTOR_RECOMPUTATION_AUTHORIZED = False
RETAINED_EVIDENCE_MODIFICATION_AUTHORIZED = False
SCIENTIFIC_INFERENCE_AUTHORIZED = False
PRODUCTION_INTEGRATION_AUTHORIZED = False
KERNEL_MODIFICATION_AUTHORIZED = False
```

After this document is accepted, committed as the sole changed file, pushed, and synchronized:

```text
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZATION_DOCUMENTED = True
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZED = True
READ_ONLY_ASYMMETRY_AUDIT_ACCEPTED_EXECUTION_COUNT = 1
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_BEGUN = False
REAL_AUDIT_OUTPUT_CREATION_AUTHORIZED = True

PSITRS_CONTACT_AUTHORIZED = False
DESCRIPTOR_RECOMPUTATION_AUTHORIZED = False
RETAINED_EVIDENCE_MODIFICATION_AUTHORIZED = False
SCIENTIFIC_INFERENCE_AUTHORIZED = False
PRODUCTION_INTEGRATION_AUTHORIZED = False
KERNEL_MODIFICATION_AUTHORIZED = False
```

After `run_pure_audit(...)` begins:

```text
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZED = consumed
READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_BEGUN = True
READ_ONLY_ASYMMETRY_AUDIT_ACCEPTED_EXECUTION_COUNT = 0 remaining
```

Final artifact state must be determined from:

```text
the captured exit code
the final-directory state
the staging-directory state
the retained artifact bytes
the committed analyzer publication contract
```

`FORMAL_HOLD` and `Mode_0` remain active throughout.

## 20. Disposition

```text
A. AUTHORIZE EXACT ONE ACCEPTED RETAINED-F3 READ-ONLY ASYMMETRY AUDIT EXECUTION
```

Recommended authorization-document path:

```text
docs/TORMENT_BRAINVISION_ALGEBRAIC_N64_PRIMARY_V0_1_F3_READ_ONLY_ASYMMETRY_AUDIT_EXECUTION_AUTHORIZATION_v0.1.md
```

Recommended commit subject:

```text
docs(research): authorize frozen N64 F3 asymmetry audit execution
```

The authorization is operational only after:

```text
this document is accepted
this document is committed as the sole changed file
the commit is pushed to main
HEAD == origin/main
the working tree is clean, including untracked files
HEAD~1 is the exact implementation commit
the analyzer and test Git blobs match
the analyzer and test raw hashes match
the retained result identities match
the retained summary identity matches
the audit gate is unset
the final audit path is absent
the staging audit path is absent
```

## 21. Preparation-time assurances

```text
No retained asymmetry audit execution occurred while preparing this authorization.

The real audit gate was not set.

The real zero-argument analyzer path was not invoked.

No final audit output directory was created.

No staging audit output directory was created.

No retained F3 evidence was modified.

No PsiTRS function was called.

No descriptor was recomputed.

The F3 evaluator was not rerun.

The production TORMENT kernel remained untouched.
```

*End — TORMENT Brainvision Algebraic N=64 PRIMARY_V0_1 Frozen-Family F3 Read-Only Asymmetry Audit Execution Authorization v0.1. Docs-only during preparation, review, commit, and push. Authorizes exactly one later accepted execution of the frozen committed analyzer after synchronized docs-only commit and complete source/evidence preflight. The accepted one-run authority is consumed when the committed analyzer begins `run_pure_audit(...)`. The audit remains descriptive, read-only, non-gating, non-rescue, quarantined, non-production, and incapable of amending the authoritative frozen-family F3 verdict.*
